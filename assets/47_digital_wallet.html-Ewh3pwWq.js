import{_ as e,o as t,c as a,a as i}from"./app-rLUZugrR.js";const s="/assets/system-design-400-rsPK_at8.png",n="/assets/system-design-401-r4IWmdD9.png",o="/assets/system-design-402-6xdmGzW-.png",r="/assets/system-design-403-gcClOhro.png",l="/assets/system-design-404-C_FnhV9z.png",d="/assets/system-design-405-JcmwcKoM.png",c="/assets/system-design-406-qk8z90_R.png",h="/assets/system-design-407-zLi0068J.png",p="/assets/system-design-408-TdQGdKk_.png",u="/assets/system-design-409-cyuFKQhm.png",g="/assets/system-design-410-ViNqmM8-.png",m="/assets/system-design-411-BDYgBUER.png",f="/assets/system-design-412-IZbw2BcF.png",b="/assets/system-design-413-DDC8akmO.png",y="/assets/system-design-414-W5mhYiZt.png",w="/assets/system-design-415-TAjbZTWj.png",v="/assets/system-design-416-PSD83sJL.png",x="/assets/system-design-417-BX1Xsk93.png",C="/assets/system-design-418-vSocLDxw.png",_="/assets/system-design-419-bdhisGQb.png",k="/assets/system-design-420-UnJEJ_2-.png",T="/assets/system-design-421-nHMC1Mtf.png",q="/assets/system-design-422-wbznheii.png",z="/assets/system-design-423-u5CmkmbA.png",S="/assets/system-design-424-J6aSfr87.png",D={},P=i('<h1 id="digital-wallet" tabindex="-1"><a class="header-anchor" href="#digital-wallet" aria-hidden="true">#</a> Digital Wallet</h1><p>Payment platforms usually have a wallet service, where they allow clients to store funds within the application, which they can withdraw later.</p><p>You can also use it to pay for goods &amp; services or transfer money to other users, who use the digital wallet service. That can be faster and cheaper than doing it via normal payment rails.</p><figure><img src="'+s+`" alt="digital-wallet" tabindex="0" loading="lazy"><figcaption>digital-wallet</figcaption></figure><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><ul><li>C: Should we only focus on transfers between digital wallets? Should we support any other operations?</li><li>I: Let&#39;s focus on transfers between digital wallets for now.</li><li>C: How many transactions per second does the system need to support?</li><li>I: Let&#39;s assume 1mil TPS</li><li>C: A digital wallet has strict correctness requirements. Can we assume transactional guarantees are sufficient?</li><li>I: Sounds good</li><li>C: Do we need to prove correctness?</li><li>I: We can do that via reconciliation, but that only detects discrepancies vs. showing us the root cause for them. Instead, we want to be able to replay data from the beginning to reconstruct the history.</li><li>C: Can we assume availability requirement is 99.99%?</li><li>I: Yes</li><li>C: Do we need to take foreign exchange into consideration?</li><li>I: No, it&#39;s out of scope</li></ul><p>Here&#39;s what we have to support in summary:</p><ul><li>Support balance transfers between two accounts</li><li>Support 1mil TPS</li><li>Reliability is 99.99%</li><li>Support transactions</li><li>Support reproducibility</li></ul><h3 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back-of-the-envelope estimation</h3><p>A traditional relational database, provisioned in the cloud can support ~1000 TPS.</p><p>In order to reach 1mil TPS, we&#39;d need 1000 database nodes. But if each transfer has two legs, then we actually need to support 2mil TPS.</p><p>One of our design goals would be to increase the TPS a single node can handle so that we can have less database nodes.</p><table><thead><tr><th>Per-node TPS</th><th>Node Number</th></tr></thead><tbody><tr><td>100</td><td>20,000</td></tr><tr><td>1,000</td><td>2,000</td></tr><tr><td>10,000</td><td>200</td></tr></tbody></table><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><h3 id="api-design" tabindex="-1"><a class="header-anchor" href="#api-design" aria-hidden="true">#</a> API Design</h3><p>We only need to support one endpoint for this interview:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>POST /v1/wallet/balance_transfer - transfers balance from one wallet to another
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Request parameters - from_account, to_account, amount (string to not lose precision), currency, transaction_id (idempotency key).</p><p>Sample response:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
    &quot;status&quot;: &quot;success&quot;
    &quot;transaction_id&quot;: &quot;01589980-2664-11ec-9621-0242ac130002&quot;
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="in-memory-sharding-solution" tabindex="-1"><a class="header-anchor" href="#in-memory-sharding-solution" aria-hidden="true">#</a> In-memory sharding solution</h3><p>Our wallet application maintains account balances for every user account.</p><p>One good data structure to represent this is a<code>map&lt;user_id, balance&gt;</code>, which can be implemented using an in-memory Redis store.</p><p>Since one redis node cannot withstand 1mil TPS, we need to partition our redis cluster into multiple nodes.</p><p>Example partitioning algorithm:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>String accountID = &quot;A&quot;;
Int partitionNumber = 7;
Int myPartition = accountID.hashCode() % partitionNumber;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Zookeeper can be used to store the number of partitions and addresses of redis nodes as it&#39;s a highly-available configuration storage.</p><p>Finally, a wallet service is a stateless service responsible for carrying out transfer operations. It can easily scale horizontally:</p><figure><img src="`+n+'" alt="wallet-service" tabindex="0" loading="lazy"><figcaption>wallet-service</figcaption></figure><p>Although this solution addresses scalability concerns, it doesn&#39;t allow us to execute balance transfers atomically.</p><h3 id="distributed-transactions" tabindex="-1"><a class="header-anchor" href="#distributed-transactions" aria-hidden="true">#</a> Distributed transactions</h3><p>One approach for handling transactions is to use the two-phase commit protocol on top of standard, sharded relational databases:</p><figure><img src="'+o+'" alt="distributed-transactions-relational-dbs" tabindex="0" loading="lazy"><figcaption>distributed-transactions-relational-dbs</figcaption></figure><p>Here&#39;s how the two-phase commit (2PC) protocol works:</p><figure><img src="'+r+'" alt="2pc-protocol" tabindex="0" loading="lazy"><figcaption>2pc-protocol</figcaption></figure><ul><li>Coordinator (wallet service) performs read and write operations on multiple databases as normal</li><li>When application is ready to commit the transaction, coordinator asks all databases to prepare it</li><li>If all databases replied with a &quot;yes&quot;, then the coordinator asks the databases to commit the transaction.</li><li>Otherwise, all databases are asked to abort the transaction</li></ul><p>Downsides to the 2PC approach:</p><ul><li>Not performant due to lock contention</li><li>The coordinator is a single point of failure</li></ul><h3 id="distributed-transaction-using-try-confirm-cancel-tc-c" tabindex="-1"><a class="header-anchor" href="#distributed-transaction-using-try-confirm-cancel-tc-c" aria-hidden="true">#</a> Distributed transaction using Try-Confirm/Cancel (TC/C)</h3><p>TC/C is a variation of the 2PC protocol, which works with compensating transactions:</p><ul><li>Coordinator asks all databases to reserve resources for the transaction</li><li>Coordinator collects replies from DBs - if yes, DBs are asked to try-confirm. If no, DBs are asked to try-cancel.</li></ul><p>One important difference between TC/C and 2PC is that 2PC performs a single transaction, whereas in TC/C, there are two independent transactions.</p><p>Here&#39;s how TC/C works in phases:</p><table><thead><tr><th>Phase</th><th>Operation</th><th>A</th><th>C</th></tr></thead><tbody><tr><td>1</td><td>Try</td><td>Balance change: -$1</td><td>Do nothing</td></tr><tr><td>2</td><td>Confirm</td><td>Do nothing</td><td>Balance change: +$1</td></tr><tr><td></td><td>Cancel</td><td>Balance change: +$1</td><td>Do Nothing</td></tr></tbody></table><p>Phase 1 - try:</p><figure><img src="'+l+'" alt="try-phase" tabindex="0" loading="lazy"><figcaption>try-phase</figcaption></figure><ul><li>coordinator starts local transaction in A&#39;s DB to reduce A&#39;s balance by 1$</li><li>C&#39;s DB is given a NOP instruction, which does nothing</li></ul><p>Phase 2a - confirm:</p><figure><img src="'+d+'" alt="confirm-phase" tabindex="0" loading="lazy"><figcaption>confirm-phase</figcaption></figure><ul><li>if both DBs replied with &quot;yes&quot;, confirm phase starts.</li><li>A&#39;s DB receives NOP, whereas C&#39;s DB is instructed to increase C&#39;s balance by 1$ (local transaction)</li></ul><p>Phase 2b - cancel:</p><figure><img src="'+c+'" alt="cancel-phase" tabindex="0" loading="lazy"><figcaption>cancel-phase</figcaption></figure><ul><li>If any of the operations in phase 1 fails, the cancel phase starts.</li><li>A&#39;s DB is instructed to increase A&#39;s balance by 1$, C&#39;s DB receives NOP</li></ul><p>Here&#39;s a comparison between 2PC and TC/C:</p><table><thead><tr><th></th><th>First Phase</th><th>Second Phase: success</th><th>Second Phase: fail</th></tr></thead><tbody><tr><td>2PC</td><td>transactions are not done yet</td><td>Commit/Cancel all transactions</td><td>Cancel all transactions</td></tr><tr><td>TC/C</td><td>All transactions are completed - committed or canceled</td><td>Execute new transactions if needed</td><td>Reverse the already committed transaction</td></tr></tbody></table><p>TC/C is also referred to as a distributed transaction by compensation. High-level operation is handled in the business logic.</p><p>Other properties of TC/C:</p><ul><li>database agnostic, as long as database supports transactions</li><li>Details and complexity of distributed transactions need to be handled in the business logic</li></ul><h3 id="tc-c-failure-modes" tabindex="-1"><a class="header-anchor" href="#tc-c-failure-modes" aria-hidden="true">#</a> TC/C Failure modes</h3><p>If the coordinator dies mid-flight, it needs to recover its intermediary state. That can be done by maintaining phase status tables, atomically updated within the database shards:</p><figure><img src="'+h+'" alt="phase-status-tables" tabindex="0" loading="lazy"><figcaption>phase-status-tables</figcaption></figure><p>What does that table contain:</p><ul><li>ID and content of distributed transaction</li><li>status of try phase - not sent, has been sent, response received</li><li>second phase name - confirm or cancel</li><li>status of second phase</li><li>out-of-order flag (explained later)</li></ul><p>One caveat when using TC/C is that there is a brief moment where the account states are inconsistent with each other while a distributed transaction is in-flight:</p><figure><img src="'+p+'" alt="unbalanced-state" tabindex="0" loading="lazy"><figcaption>unbalanced-state</figcaption></figure><p>This is fine as long as we always recover from this state and that users cannot use the intermediary state to eg spend it. This is guaranteed by always executing deductions prior to additions.</p><table><thead><tr><th>Try phase choices</th><th>Account A</th><th>Account C</th></tr></thead><tbody><tr><td>Choice 1</td><td>-$1</td><td>NOP</td></tr><tr><td>Choice 2 (invalid)</td><td>NOP</td><td>+$1</td></tr><tr><td>Choice 3 (invalid)</td><td>-$1</td><td>+$1</td></tr></tbody></table><p>Note that choice 3 from table above is invalid because we cannot guarantee atomic execution of transactions across different databases without relying on 2PC.</p><p>One edge-case to address is out of order execution:</p><figure><img src="'+u+'" alt="out-of-order-execution" tabindex="0" loading="lazy"><figcaption>out-of-order-execution</figcaption></figure><p>It is possible that a database receives a cancel operation, before receiving a try. This edge case can be handled by adding an out of order flag in our phase status table. When we receive a try operation, we first check if the out of order flag is set and if so, a failure is returned.</p><h3 id="distributed-transaction-using-saga" tabindex="-1"><a class="header-anchor" href="#distributed-transaction-using-saga" aria-hidden="true">#</a> Distributed transaction using Saga</h3><p>Another popular approach is using Sagas - a standard for implementing distributed transactions with microservice architectures.</p><p>Here&#39;s how it works:</p><ul><li>all operations are ordered in a sequence. All operations are independent in their own databases.</li><li>operations are executed from first to last</li><li>when an operation fails, the entire process starts to roll back until the beginning with compensating operations</li></ul><figure><img src="'+g+'" alt="saga" tabindex="0" loading="lazy"><figcaption>saga</figcaption></figure><p>How do we coordinate the workflow? There are two approaches we can take:</p><ul><li>Choreography - all services involved in a saga subscribe to the related events and do their part in the saga</li><li>Orchestration - a single coordinator instructs all services to do their jobs in the correct order</li></ul><p>The challenge of using choreography is that business logic is split across multiple service, which communicate asynchronously. The orchestration approach handles complexity well, so it is typically the preferred approach in a digital wallet system.</p><p>Here&#39;s a comparison between TC/C and Saga:</p><table><thead><tr><th></th><th>TC/C</th><th>Saga</th></tr></thead><tbody><tr><td>Compensating action</td><td>In Cancel phase</td><td>In rollback phase</td></tr><tr><td>Central coordination</td><td>Yes</td><td>Yes (orchestration mode)</td></tr><tr><td>Operation execution order</td><td>any</td><td>linear</td></tr><tr><td>Parallel execution possibility</td><td>Yes</td><td>No (linear execution)</td></tr><tr><td>Could see the partial inconsistent status</td><td>Yes</td><td>Yes</td></tr><tr><td>Application or database logic</td><td>Application</td><td>Application</td></tr></tbody></table><p>The main difference is that TC/C is parallelizable, so our decision is based on the latency requirement - if we need to achieve low latency, we should go for the TC/C approach.</p><p>Regardless of the approach we take, we still need to support auditing and replaying history to recover from failed states.</p><h3 id="event-sourcing" tabindex="-1"><a class="header-anchor" href="#event-sourcing" aria-hidden="true">#</a> Event sourcing</h3><p>In real-life, a digital wallet application might be audited and we have to answer certain questions:</p><ul><li>Do we know the account balance at any given time?</li><li>How do we know the historical and current balances are correct?</li><li>How do we prove the system logic is correct after a code change?</li></ul><p>Event sourcing is a technique which helps us answer these questions.</p><p>It consists of four concepts:</p><ul><li>command - intended action from the real world, eg transfer 1$ from account A to B. Need to have a global order, due to which they&#39;re put into a FIFO queue.- commands, unlike events, can fail and have some randomness due to eg IO or invalid state.</li><li>commands can produce zero or more events</li><li>event generation can contain randomness such as external IO. This will be revisited later</li><li>event - historical facts about events which occured in the system, eg &quot;transferred 1$ from A to B&quot;.- unlike commands, events are facts that have happened within our system</li><li>similar to commands, they need to be ordered, hence, they&#39;re enqueued in a FIFO queue</li><li>state - what has changed as a result of an event. Eg a key-value store between account and their balances.</li><li>state machine - drives the event sourcing process. It mainly validates commands and applies events to update the system state.- the state machine should be deterministic, hence, it shouldn&#39;t read external IO or rely on randomness.</li></ul><figure><img src="'+m+'" alt="event-sourcing" tabindex="0" loading="lazy"><figcaption>event-sourcing</figcaption></figure><p>Here&#39;s a dynamic view of event sourcing:</p><figure><img src="'+f+'" alt="dynamic-event-sourcing" tabindex="0" loading="lazy"><figcaption>dynamic-event-sourcing</figcaption></figure><p>For our wallet service, the commands are balance transfer requests. We can put them in a FIFO queue, such as Kafka:</p><figure><img src="'+b+'" alt="command-queue" tabindex="0" loading="lazy"><figcaption>command-queue</figcaption></figure><p>Here&#39;s the full picture:</p><figure><img src="'+y+'" alt="wallet-service-state-machine" tabindex="0" loading="lazy"><figcaption>wallet-service-state-machine</figcaption></figure><ul><li>state machine reads commands from the command queue</li><li>balance state is read from the database</li><li>command is validated. If valid, two events for each of the accounts is generated</li><li>next event is read and applied by updating the balance (state) in the database</li></ul><p>The main advantage of using event sourcing is its reproducibility. In this design, all state update operations are saved as immutable history of all balance changes.</p><p>Historical balances can always be reconstructed by replaying events from the beginning. Because the event list is immutable and the state machine is deterministic, we are guaranteed to succeed in replaying any of the intermediary states.</p><figure><img src="'+w+'" alt="historical-states" tabindex="0" loading="lazy"><figcaption>historical-states</figcaption></figure><p>All audit-related questions asked in the beginning of the section can be addressed by relying on event sourcing:</p><ul><li>Do we know the account balance at any given time? - events can be replayed from the start until the point which we are interested in</li><li>How do we know the historical and current balances are correct? - correctness can be verified by recalculating all events from the start</li><li>How do we prove the system logic is correct after a code change? - we can run different versions of the code against the events and verify their results are identical</li></ul><p>Answering client queries about their balance can be addressed using the CQRS architecture - there can be multiple read-only state machines which are responsible for querying the historical state, based on the immutable events list:</p><figure><img src="'+v+'" alt="cqrs-architecture" tabindex="0" loading="lazy"><figcaption>cqrs-architecture</figcaption></figure><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>In this section we&#39;ll explore some performance optimizations as we&#39;re still required to scale to 1mil TPS.</p><h3 id="high-performance-event-sourcing" tabindex="-1"><a class="header-anchor" href="#high-performance-event-sourcing" aria-hidden="true">#</a> High-performance event sourcing</h3><p>The first optimization we&#39;ll explore is to save commands and events into local disk store instead of an external store such as Kafka.</p><p>This avoids the network latency and also, since we&#39;re only doing appends, that operation is generally fast for HDDs.</p><p>The next optimization is to cache recent commands and events in-memory in order to save the time of loading them back from disk.</p><p>At a low-level, we can achieve the aforementioned optimizations by leveraging a command called mmap, which stores data in local disk as well as cache it in-memory:</p><figure><img src="'+x+'" alt="mmap-optimization" tabindex="0" loading="lazy"><figcaption>mmap-optimization</figcaption></figure><p>The next optimization we can do is also store state in the local file system using SQLite - a file-based local relational database. RocksDB is also another good option.</p><p>For our purposes, we&#39;ll choose RocksDB because it uses a log-structured merge-tree (LSM), which is optimized for write operations. Read performance is optimized via caching.</p><figure><img src="'+C+'" alt="rocks-db-approach" tabindex="0" loading="lazy"><figcaption>rocks-db-approach</figcaption></figure><p>To optimize the reproducibility, we can periodically save snapshots to disk so that we don&#39;t have to reproduce a given state from the very beginning every time. We could store snapshots as large binary files in distributed file storage, eg HDFS:</p><figure><img src="'+_+'" alt="snapshot-approach" tabindex="0" loading="lazy"><figcaption>snapshot-approach</figcaption></figure><h3 id="reliable-high-performance-event-sourcing" tabindex="-1"><a class="header-anchor" href="#reliable-high-performance-event-sourcing" aria-hidden="true">#</a> Reliable high-performance event sourcing</h3><p>All the optimizations done so far are great, but they make our service stateful. We need to introduce some form of replication for reliability purposes.</p><p>Before we do that, we should analyze what kind of data needs high reliability in our system:</p><ul><li>state and snapshot can always be regenerated by reproducing them from the events list. Hence, we only need to guarantee the event list reliability.</li><li>one might think we can always regenerate the events list from the command list, but that is not true, since commands are non-deterministic.</li><li>conclusion is that we need to ensure high reliability for the events list only</li></ul><p>In order to achieve high reliability for events, we need to replicate the list across multiple nodes. We need to guarantee:</p><ul><li>that there is no data loss</li><li>the relative order of data within a log file remains the same across replicas</li></ul><p>To achieve this, we can employ a consensus algorithm, such as Raft.</p><p>With Raft, there is a leader who is active and there are followers who are passive. If a leader dies, one of the followers picks up. As long as more than half of the nodes are up, the system continues running.</p><figure><img src="'+k+'" alt="raft-replication" tabindex="0" loading="lazy"><figcaption>raft-replication</figcaption></figure><p>With this approach, all nodes update the state, based on the events list. Raft ensures leader and followers have the same events list.</p><h3 id="distributed-event-sourcing" tabindex="-1"><a class="header-anchor" href="#distributed-event-sourcing" aria-hidden="true">#</a> Distributed event sourcing</h3><p>So far, we&#39;ve managed to design a system which has high single-node performance and is reliable.</p><p>Some limitations we have to tackle:</p><ul><li>The capacity of a single raft group is limited. At some point, we need to shard the data and implement distributed transactions</li><li>In the CQRS architecture, the request/response flow is slow. A client would need to periodically poll the system to learn when their wallet has been updated</li></ul><p>Polling is not real-time, hence, it can take a while for a user to learn about an update in their balance. Also, it can overload the query services if the polling frequency is too high:</p><figure><img src="'+T+'" alt="polling-approach" tabindex="0" loading="lazy"><figcaption>polling-approach</figcaption></figure><p>To mitigate the system load, we can introduce a reverse proxy, which sends commands on behalf of the user and polls for response on their behalf:</p><figure><img src="'+q+'" alt="reverse-proxy" tabindex="0" loading="lazy"><figcaption>reverse-proxy</figcaption></figure><p>This alleviates the system load as we could fetch data for multiple users using a single request, but it still doesn&#39;t solve the real-time receipt requirement.</p><p>One final change we could do is make the read-only state machines push responses back to the reverse proxy once it&#39;s available. This can give the user the sense that updates happen real-time:</p><figure><img src="'+z+'" alt="push-state-machines" tabindex="0" loading="lazy"><figcaption>push-state-machines</figcaption></figure><p>Finally, to scale the system even further, we can shard the system into multiple raft groups, where we implement distributed transactions on top of them using an orchestrator either via TC/C or Sagas:</p><figure><img src="'+S+'" alt="sharded-raft-groups" tabindex="0" loading="lazy"><figcaption>sharded-raft-groups</figcaption></figure><p>Here&#39;s an example lifecycle of a balance transfer request in our final system:</p><ul><li>User A sends a distributed transaction to the Saga coordinator with two operations -<code>A-1</code>and<code>C+1</code>.</li><li>Saga coordinator creates a record in the phase status table to trace the status of the transaction</li><li>Coordinator determines which partitions it needs to send commands to.</li><li>Partition 1&#39;s raft leader receives the<code>A-1</code>command, validates it, converts it to an event and replicates it across other nodes in the raft group</li><li>Event result is synchronized to the read state machine, which pushes a response back to the coordinator</li><li>Coordinator creates a record indicating that the operation was successful and proceeds with the next operation -<code>C+1</code></li><li>Next operation is executed similarly to the first one - partition is determined, command is sent, executed, read state machine pushes back a response</li><li>Coordinator creates a record indicating operation 2 was also successful and finally informs the client of the result</li></ul><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap Up</h2><p>Here&#39;s the evolution of our design:</p><ul><li>We started from a solution using an in-memory Redis. The problem with this approach is that it is not durable storage.</li><li>We moved on to using relational databases, on top of which we execute distributed transactions using 2PC, TC/C or distributed saga.</li><li>Next, we introduced event sourcing in order to make all the operations auditable</li><li>We started by storing the data into external storage using external database and queue, but that&#39;s not performant</li><li>We proceeded to store data in local file storage, leveraging the performance of append-only operations. We also used caching to optimize the read path</li><li>The previous approach, although performant, wasn&#39;t durable. Hence, we introduced Raft consensus with replication to avoid single points of failure</li><li>We also adopted CQRS with a reverse proxy to manage a transaction&#39;s lifecycle on behalf of our users</li><li>Finally, we partitioned our data across multiple raft groups, which are orchestrated using a distributed transaction mechanism - TC/C or distributed saga</li></ul>',145),I=[P];function A(B,O){return t(),a("div",null,I)}const W=e(D,[["render",A],["__file","47_digital_wallet.html.vue"]]);export{W as default};
