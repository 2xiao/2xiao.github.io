import{_ as o,r,o as n,c as l,b as e,d as a,e as i,a as t}from"./app-rLUZugrR.js";const c="/assets/system-design-228-o9k-V0bc.png",d="/assets/system-design-229-4l1i7hBC.png",p="/assets/system-design-230-vaIdD6SZ.png",h="/assets/system-design-231-COX4QfzK.png",u="/assets/system-design-232-DAVR0ac2.png",g="/assets/system-design-233-p0ylOQKN.png",m="/assets/system-design-234-V-ajcOCQ.png",f="/assets/system-design-235-rJXQkq48.png",b="/assets/system-design-236-Izk1X_KZ.png",y="/assets/system-design-237-7tlM6HCb.png",w="/assets/system-design-238-TECorZgI.png",v="/assets/system-design-239-LiqELjzD.png",k="/assets/system-design-240-JYo5n_yN.png",x="/assets/system-design-241-8KcKcMFP.png",_="/assets/system-design-242-GRxVUIoQ.png",I="/assets/system-design-243-_lPoGslQ.png",T="/assets/system-design-244-4c3OwlR_.png",q="/assets/system-design-245-QDCnSHaV.png",z="/assets/system-design-246-g0ar0dFa.png",C="/assets/system-design-247-JRU8Q2FF.png",A="/assets/system-design-248-pY35cDxk.png",W="/assets/system-design-249-nbtyugfg.png",D="/assets/system-design-250-dRgmjIh7.png",M="/assets/system-design-251-vbcVtRr0.png",O="/assets/system-design-252-CYXvO0ng.png",S="/assets/system-design-253-WsGXmGTe.png",P="/assets/system-design-254-qwoX1Ab4.png",R="/assets/system-design-255-T64nKljl.png",H="/assets/system-design-256-CpNRPYb0.png",K="/assets/system-design-257-Y2Ny8Yf_.png",B="/assets/system-design-258-azXRiwOW.png",F="/assets/system-design-259-InZ0GpcU.png",E="/assets/system-design-260-Iv46xY67.png",L="/assets/system-design-261-e5V1B1qs.png",Q={},j=t('<h1 id="distributed-message-queue" tabindex="-1"><a class="header-anchor" href="#distributed-message-queue" aria-hidden="true">#</a> Distributed Message Queue</h1><p>We&#39;ll be designing a distributed message queue in this chapter.</p><p>Benefits of message queues:</p><ul><li>Decoupling - Eliminates tight coupling between components. Let them update separately.</li><li>Improved scalability - Producers and consumers can be scaled independently based on traffic.</li><li>Increased availability - If one part of the system goes down, other parts continue interacting with the queue.</li><li>Better performance - Producers can produce messages without waiting for consumer confirmation.</li></ul><p>Some popular message queue implementations - Kafka, RabbitMQ, RocketMQ, Apache Pulsar, ActiveMQ, ZeroMQ.</p><p>Strictly speaking, Kafka and Pulsar are not message queues. They are event streaming platforms. There is however a convergence of features which blurs the distinction between message queues and event streaming platforms.</p><p>In this chapter, we&#39;ll be building a message queue with support for more advanced features such as long data retention, repeated message consumption, etc.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the problem and establish design scope</h2><p>Message queues ought to support few basic features - producers produce messages and consumers consume them. There are, however, different considerations with regards to performance, message delivery, data retention, etc.</p><p>Here&#39;s a set of potential questions between Candidate and Interviewer:</p><ul><li>C: What&#39;s the format and average message size? Is it text only?</li><li>I: Messages are text-only and usually a few KBs</li><li>C: Can messages be repeatedly consumed?</li><li>I: Yes, messages can be repeatedly consumed by different consumers. This is an added requirement, which traditional message queues don&#39;t support.</li><li>C: Are messages consumed in the same order they were produced?</li><li>I: Yes, order guarantee should be preserved. This is an added requirement, traditional message queues don&#39;t support this.</li><li>C: What are the data retention requirements?</li><li>I: Messages need to have a retention of two weeks. This is an added requirement.</li><li>C: How many producers and consumers do we want to support?</li><li>I: The more, the better.</li><li>C: What data delivery semantic do we want to support? At-most-once, at-least-once, exactly-once?</li><li>I: We definitely want to support at-least-once. Ideally, we can support all and make them configurable.</li><li>C: What&#39;s the target throughput for end-to-end latency?</li><li>I: It should support high throughput for use cases like log aggregation and low throughput for more traditional use cases.</li></ul><p>Functional requirements:</p><ul><li>Producers send messages to a message queue</li><li>Consumers consume messages from the queue</li><li>Messages can be consumed once or repeatedly</li><li>Historical data can be truncated</li><li>Message size is in the KB range</li><li>Order of messages needs to be preserved</li><li>Data delivery semantics is configurable - at-most-once/at-least-once/exactly-once.</li></ul><p>Non-functional requirements:</p><ul><li>High throughput or low latency. Configurable based on use-case</li><li>Scalable - system should be distributed and support a sudden surge in message volume</li><li>Persistent and durable - data should be persisted on disk and replicated among nodes</li></ul><p>Traditional message queues typically don&#39;t support data retention and don&#39;t provide ordering guarantees. This greatly simplifies the design and we&#39;ll discuss it.</p><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose high-level design and get buy-in</h2><p>Key components of a message queue:</p><figure><img src="'+c+'" alt="message-queue-components" tabindex="0" loading="lazy"><figcaption>message-queue-components</figcaption></figure><ul><li>Producer sends messages to a queue</li><li>Consumer subscribes to a queue and consumes the subscribed messages</li><li>Message queue is a service in the middle which decouples producers from consumers, letting them scale independently.</li><li>Producer and consumer are both clients, while the message queue is the server.</li></ul><h3 id="messaging-models" tabindex="-1"><a class="header-anchor" href="#messaging-models" aria-hidden="true">#</a> Messaging models</h3><p>The first type of messaging model is point-to-point and it&#39;s commonly found in traditional message queues:</p><figure><img src="'+d+'" alt="point-to-point-model" tabindex="0" loading="lazy"><figcaption>point-to-point-model</figcaption></figure><ul><li>A message is sent to a queue and it&#39;s consumed by exactly one consumer.</li><li>There can be multiple consumers, but a message is consumed only once.</li><li>Once message is acknowledged as consumed, it is removed from the queue.</li><li>There is no data retention in the point-to-point model, but there is such in our design.</li></ul><p>On the other hand, the publish-subscribe model is more common for event streaming platforms:</p><figure><img src="'+p+'" alt="publish-subscribe-model" tabindex="0" loading="lazy"><figcaption>publish-subscribe-model</figcaption></figure><ul><li>In this model, messages are associated to a topic.</li><li>Consumers are subscribed to a topic and they receive all messages sent to this topic.</li></ul><h3 id="topics-partitions-and-brokers" tabindex="-1"><a class="header-anchor" href="#topics-partitions-and-brokers" aria-hidden="true">#</a> Topics, partitions and brokers</h3><p>What if the data volume for a topic is too large? One way to scale is by splitting a topic into partitions (aka sharding):</p><figure><img src="'+h+'" alt="partitions" tabindex="0" loading="lazy"><figcaption>partitions</figcaption></figure><ul><li>Messages sent to a topic are evenly distributed across partitions</li><li>The servers that host partitions are called brokers</li><li>Each topic operates like a queue using FIFO for message processing. Message order is preserved within a partition.</li><li>The position of a message within the partition is called anoffset.</li><li>Each message produced is sent to a specific partition. A partition key specifies which partition a message should land in.- Eg a<code>user_id</code>can be used as a partition key to guarantee order of messages for the same user.</li><li>Each consumer subscribes to one or more partitions. When there are multiple consumers for the same messages, they form a consumer group.</li></ul><h3 id="consumer-groups" tabindex="-1"><a class="header-anchor" href="#consumer-groups" aria-hidden="true">#</a> Consumer groups</h3><p>Consumer groups are a set of consumers working together to consume messages from a topic:</p><figure><img src="'+u+'" alt="consumer-groups" tabindex="0" loading="lazy"><figcaption>consumer-groups</figcaption></figure><ul><li>Messages are replicated per consumer group (not per consumer).</li><li>Each consumer group maintains its own offset.</li><li>Reading messages in parallel by a consumer group improves throughput but hampers the ordering guarantee.</li><li>This can be mitigated by only allowing one consumer from a group to be subscribed to a partition.</li><li>This means that we can&#39;t have more consumers in a group than there are partitions.</li></ul><h3 id="high-level-architecture" tabindex="-1"><a class="header-anchor" href="#high-level-architecture" aria-hidden="true">#</a> High-level architecture</h3><figure><img src="'+g+'" alt="high-level-architecture" tabindex="0" loading="lazy"><figcaption>high-level-architecture</figcaption></figure><ul><li>Clients are producer and consumer. Producer pushes messages to a designated topic. Consumer group subscribes to messages from a topic.</li><li>Brokers hold multiple partitions. A partition holds a subset of messages for a topic.</li><li>Data storage stores messages in partitions.</li><li>State storage keeps the consumer states.</li><li>Metadata storage stores configuration and topic properties</li><li>The coordination service is responsible for service discovery (which brokers are alive) and leader election (which broker is leader, responsible for assigning partitions).</li></ul><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>In order to achieve high throughput and preserve the high data retention requirement, we made some important design choices:</p><ul><li>We chose an on-disk data structure which takes advantage of the properties of modern HDD and disk caching strategies of modern OS-es.</li><li>The message data structure is immutable to avoid extra copying, which we want to avoid in a high volume/high traffic system.</li><li>We designed our writes around batching as small I/O is an enemy of high throughput.</li></ul><h3 id="data-storage" tabindex="-1"><a class="header-anchor" href="#data-storage" aria-hidden="true">#</a> Data storage</h3><p>In order to find the best data store for messages, we must examine a message&#39;s properties:</p><ul><li>Write-heavy, read-heavy</li><li>No update/delete operations. In traditional message queues, there is a &quot;delete&quot; operation as messages are not retained.</li><li>Predominantly sequential read/write access pattern.</li></ul><p>What are our options:</p><ul><li>Database - not ideal as typical databases don&#39;t support well both write and read heavy systems.</li><li>Write-ahead log (WAL) - a plain text file which only supports appending to it and is very HDD-friendly.- We split partitions into segments to avoid maintaining a very large file.</li><li>Old segments are read-only. Writes are accepted by latest segment only.</li></ul><figure><img src="'+m+'" alt="wal-example" tabindex="0" loading="lazy"><figcaption>wal-example</figcaption></figure><p>WAL files are extremely efficient when used with traditional HDDs.</p><p>There is a misconception that HDD acces is slow, but that hugely depends on the access pattern. When the access pattern is sequential (as in our case), HDDs can achieve several MB/s write/read speed which is sufficient for our needs. We also piggyback on the fact that the OS caches disk data in memory aggressively.</p><h3 id="message-data-structure" tabindex="-1"><a class="header-anchor" href="#message-data-structure" aria-hidden="true">#</a> Message data structure</h3><p>It is important that the message schema is compliant between producer, queue and consumer to avoid extra copying. This allows much more efficient processing.</p><p>Example message structure:</p><figure><img src="'+f+'" alt="message-structure" tabindex="0" loading="lazy"><figcaption>message-structure</figcaption></figure><p>The key of the message specifies which partition a message belongs to. An example mapping is<code>hash(key) % numPartitions</code>. For more flexibility, the producer can override default keys in order to control which partitions messages are distributed to.</p><p>The message value is the payload of a message. It can be plaintext or a compressed binary block.</p><p>Note:Message keys, unlike traditional KV stores, need not be unique. It is acceptable to have duplicate keys and for it to even be missing.</p><p>Other message files:</p><ul><li>Topic - topic the message belongs to</li><li>Partition - The ID of the partition a message belongs to</li><li>Offset - The position of the message in a partition. A message can be located via<code>topic</code>,<code>partition</code>,<code>offset</code>.</li><li>Timestamp - When the message is stored</li><li>Size - the size of this message</li><li>CRC - checksum to ensure message integrity</li></ul><p>Additional features such as filtering can be supported by adding additional fields.</p><h3 id="batching" tabindex="-1"><a class="header-anchor" href="#batching" aria-hidden="true">#</a> Batching</h3><p>Batching is critical for the performance of our system. We apply it in the producer, consumer and message queue.</p><p>It is critical because:</p><ul><li>It allows the operating system to group messages together, amortizing the cost of expensive network round trips</li><li>Messages are written to the WAL in groups sequentially, which leads to a lot of sequential writes and disk caching.</li></ul><p>There is a trade-off between latency and throughput:</p><ul><li>High batching leads to high throughput and higher latency.</li><li>Less batching leads to lower throughput and lower latency.</li></ul><p>If we need to support lower latency since the system is deployed as a traditional message queue, the system could be tuned to use a smaller batch size.</p><p>If tuned for throughput, we might need more partitions per topic to compensate for the slower sequential disk write throughput.</p><h3 id="producer-flow" tabindex="-1"><a class="header-anchor" href="#producer-flow" aria-hidden="true">#</a> Producer flow</h3><p>If a producer wants to send a message to a partition, which broker should it connect to?</p><p>One option is to introduce a routing layer, which route messages to the correct broker. If replication is enabled, the correct broker is the leader replica:</p><figure><img src="'+b+'" alt="routing-layer" tabindex="0" loading="lazy"><figcaption>routing-layer</figcaption></figure><ul><li>Routing layer reads the replication plan from the metadata store and caches it locally.</li><li>Producer sends a message to the routing layer.</li><li>Message is forwarded to broker 1 who is the leader of the given partition</li><li>Follower replicas pull the new message from the leader. Once enough confirmations are received, the leader commits the data and responds to the producer.</li></ul><p>The reason for having replicas is to enable fault tolerance.</p><p>This approach works but has some drawbacks:</p><ul><li>Additional network hops due to the extra component</li><li>The design doesn&#39;t enable batching messages</li></ul><p>To mitigate these issues, we can embed the routing layer into the producer:</p><figure><img src="'+y+'" alt="routing-layer-producer" tabindex="0" loading="lazy"><figcaption>routing-layer-producer</figcaption></figure><ul><li>Fewer network hops lead to lower latency</li><li>Producers can control which partition a message is routed to</li><li>The buffer allows us to batch messages in-memory and send out larger batches in a single request, which increases throughput.</li></ul><p>The batch size choice is a classical trade-off between throughput and latency.</p><figure><img src="'+w+'" alt="batch-size-throughput-vs-latency" tabindex="0" loading="lazy"><figcaption>batch-size-throughput-vs-latency</figcaption></figure><ul><li>Larger batch size leads to longer wait time before batch is committed.</li><li>Smaller batch size leads to request being sent sooner and having lower latency but lower throughput.</li></ul><h3 id="consumer-flow" tabindex="-1"><a class="header-anchor" href="#consumer-flow" aria-hidden="true">#</a> Consumer flow</h3><p>The consumer specifies its offset in a partition and receives a chunk of messages, beginning from that offset:</p><figure><img src="'+v+'" alt="consumer-example" tabindex="0" loading="lazy"><figcaption>consumer-example</figcaption></figure><p>One important consideration when designing the consumer is whether to use a push or a pull model:</p><ul><li>Push model leads to lower latency as broker pushes messages to consumer as it receives them.- However, if rate of consumption falls behind the rate of production, the consumer can be overwhelmed.</li><li>It is challenging to deal with consumers with varying processing power as the broker controls the rate of consumption.</li><li>Pull model leads to the consumer controlling the consumption rate.- If rate of consumption is slow, consumer will not be overwhelmed and we can scale it to catch up.</li><li>The pull model is more suitable for batch processing, because with the push model, the broker can&#39;t know how many messages a consumer can handle.</li><li>With the pull model, on the other hand, consumers can aggressively fetch large message batches.</li><li>The down side is the higher latency and extra network calls when there are no new messages. Latter issue can be mitigated using long polling.</li></ul><p>Hence, most message queues (and us) choose the pull model.</p><figure><img src="'+k+'" alt="consumer-flow" tabindex="0" loading="lazy"><figcaption>consumer-flow</figcaption></figure><ul><li>A new consumer subscribes to topic A and joins group 1.</li><li>The correct broker node is found by hashing the group name. This way, all consumers in a group connect to the same broker.</li><li>Note that this consumer group coordinator is different from the coordination service (ZooKeeper).</li><li>Coordinator confirms that the consumer has joined the group and assigns partition 2 to that consumer.</li><li>There are different partition assignment strategies - round-robin, range, etc.</li><li>Consumer fetches latest messages from the last offset. The state storage keeps the consumer offsets.</li><li>Consumer processes messages and commits the offset to the broker. The order of those operations affects the message delivery semantics.</li></ul><h3 id="consumer-rebalancing" tabindex="-1"><a class="header-anchor" href="#consumer-rebalancing" aria-hidden="true">#</a> Consumer rebalancing</h3><p>Consumer rebalancing is responsible for deciding which consumers are responsible for which partition.</p><p>This process occurs when a consumer joins/leaves or a partition is added/removed.</p><p>The broker, acting as a coordinator plays a huge role in orchestrating the rebalancing workflow.</p><figure><img src="'+x+'" alt="consumer-rebalancing" tabindex="0" loading="lazy"><figcaption>consumer-rebalancing</figcaption></figure><ul><li>All consumers from the same group are connected to the same coordinator. The coordinator is found by hashing the group name.</li><li>When the consumer list changes, the coordinator chooses a new leader of the group.</li><li>The leader of the group calculates a new partition dispatch plan and reports it back to the coordinator, which broadcasts it to the other consumers.</li></ul><p>When the coordinator stops receiving heartbeats from the consumers in a group, a rebalancing is triggered:</p><figure><img src="'+_+'" alt="consumer-rebalance-example" tabindex="0" loading="lazy"><figcaption>consumer-rebalance-example</figcaption></figure><p>Let&#39;s explore what happens when a consumer joins a group:</p><figure><img src="'+I+'" alt="consumer-join-group-usecase" tabindex="0" loading="lazy"><figcaption>consumer-join-group-usecase</figcaption></figure><ul><li>Initially, only consumer A is in the group and it consumes all partitions.</li><li>Consumer B sends a request to join the group.</li><li>The coordinator notifies all group members that it&#39;s time to rebalance passively - as a response to the heartbeat.</li><li>Once all consumers rejoin the group, the coordinator chooses a leader and notifies the rest about the election result.</li><li>The leader generates the partition dispatch plan and sends it to the coordinator. Others wait for the dispatch plan.</li><li>Consumers start consuming from the newly assigned partitions.</li></ul><p>Here&#39;s what happens when a consumer leaves the group:</p><figure><img src="'+T+'" alt="consumer-leaves-group-usecase" tabindex="0" loading="lazy"><figcaption>consumer-leaves-group-usecase</figcaption></figure><ul><li>Consumer A and B are in the same group</li><li>Consumer B asks to leave the group</li><li>When coordinator receives A&#39;s heartbeat, it informs them that it&#39;s time to rebalance.</li><li>The rest of the steps are the same.</li></ul><p>The process is similar when a consumer doesn&#39;t send a heartbeat for a long time:</p><figure><img src="'+q+'" alt="consumer-no-heartbeat-usecase" tabindex="0" loading="lazy"><figcaption>consumer-no-heartbeat-usecase</figcaption></figure><h3 id="state-storage" tabindex="-1"><a class="header-anchor" href="#state-storage" aria-hidden="true">#</a> State storage</h3><p>The state storage stores mapping between partitions and consumers, as well as the last consumed offsets for a partition.</p><figure><img src="'+z+'" alt="state-storage" tabindex="0" loading="lazy"><figcaption>state-storage</figcaption></figure><p>Group 1&#39;s offset is at 6, meaning all previous messages are consumed. If a consumer crashes, the new consumer will continue from that message on wards.</p><p>Data access patterns for consumer states:</p><ul><li>Frequent read/write operations, but low volume</li><li>Data is updated frequently, but rarely deleted</li><li>Random read/write</li><li>Data consistency is important</li></ul><p>Given these requirements, a fast KV storage like Zookeeper is ideal.</p><h3 id="metadata-storage" tabindex="-1"><a class="header-anchor" href="#metadata-storage" aria-hidden="true">#</a> Metadata storage</h3><p>The metadata storage stores configuration and topic properties - partition number, retention period, replica distribution.</p><p>Metadata doesn&#39;t change often and volume is small, but there is a high consistency requirement. Zookeeper is a good choice for this storage.</p><h3 id="zookeeper" tabindex="-1"><a class="header-anchor" href="#zookeeper" aria-hidden="true">#</a> ZooKeeper</h3><p>Zookeeper is essential for building distributed message queues.</p><p>It is a hierarchical key-value store, commonly used for a distributed configuration, synchronization service and naming registry (ie service discovery).</p><figure><img src="'+C+'" alt="zookeeper" tabindex="0" loading="lazy"><figcaption>zookeeper</figcaption></figure><p>With this change, the broker only needs to maintain data for the messages. Metadata and state storage is in Zookeeper.</p><p>Zookeeper also helps with leader election of the broker replicas.</p><h3 id="replication" tabindex="-1"><a class="header-anchor" href="#replication" aria-hidden="true">#</a> Replication</h3><p>In distributed systems, hardware issues are inevitable. We can tackle this via replication to achieve high availability.</p><figure><img src="'+A+'" alt="replication-example" tabindex="0" loading="lazy"><figcaption>replication-example</figcaption></figure><ul><li>Each partition is replicated across multiple brokers, but there is only one leader replica.</li><li>Producers send messages to leader replicas</li><li>Followers pull the replicated messages from the leader</li><li>Once enough replicas are synchronized, the leader returns acknowledgment to the producer</li><li>Distribution of replicas for each partition is called the replica distribution plan.</li><li>The leader for a given partition creates the replica distribution plan and saves it in Zookeeper</li></ul><h3 id="in-sync-replicas" tabindex="-1"><a class="header-anchor" href="#in-sync-replicas" aria-hidden="true">#</a> In-sync replicas</h3><p>One problem we need to tackle is keeping messages in-sync between the leader and the followers for a given partition.</p><p>In-sync replicas (ISR) are replicas for a partition that stay in-sync with the leader.</p><p>The<code>replica.lag.max.messages</code>defines how many messages can a replica be lagging behind the leader to be considered in-sync.</p><figure><img src="'+W+'" alt="in-sync-replicas-example" tabindex="0" loading="lazy"><figcaption>in-sync-replicas-example</figcaption></figure><ul><li>Committed offset is 13</li><li>Two new messages are written to the leader, but not committed yet.</li><li>A message is committed once all replicas in the ISR have synchronized that message</li><li>Replica 2 and 3 have fully caught up with leader, hence, they are in ISR</li><li>Replica 4 has lagged behind, hence, is removed from ISR for now</li></ul><p>ISR reflects a trade-off between performance and durability.</p><ul><li>In order for producers not to lose messages, all replicas should be in sync before sending an acknowledgment</li><li>But a slow replica will cause the whole partition to become unavailable</li></ul><p>Acknowledgment handling is configurable.</p><p><code>ACK=all</code>means that all replicas in ISR have to sync a message. Message sending is slow, but message durability is highest.</p><figure><img src="'+D+'" alt="ack-all" tabindex="0" loading="lazy"><figcaption>ack-all</figcaption></figure><p><code>ACK=1</code>means that producer receives acknowledgment once leader receives the message. Message sending is fast, but message durability is low.</p><figure><img src="'+M+'" alt="ack-1" tabindex="0" loading="lazy"><figcaption>ack-1</figcaption></figure><p><code>ACK=0</code>means that producer sends messages without waiting for any acknowledgment from leader. Message sending is fastest, message durability is lowest.</p><figure><img src="'+O+'" alt="ack-0" tabindex="0" loading="lazy"><figcaption>ack-0</figcaption></figure><p>On the consumer side, we can connect all consumers to the leader for a partition and let them read messages from it:</p><ul><li>This makes for the simplest design and easiest operation</li><li>Messages in a partition are sent to only one consumer in a group, which limits the connections to the leader replica</li><li>The number of connections to leader replica is typically not high as long as the topic is not super hot</li><li>We can scale a hot topic by increasing the number of partitions and consumers</li><li>In certain scenarios, it might make sense to let a consumer lead from an ISR, eg if they&#39;re located in a separate DC</li></ul><p>The ISR list is maintained by the leader who tracks the lag between itself and each replica.</p><h3 id="scalability" tabindex="-1"><a class="header-anchor" href="#scalability" aria-hidden="true">#</a> Scalability</h3><p>Let&#39;s evaluate how we can scale different parts of the system.</p><h3 id="producer" tabindex="-1"><a class="header-anchor" href="#producer" aria-hidden="true">#</a> Producer</h3><p>The producer is much smaller than the consumer. Its scalability can easily be achieved by adding/removing new producer instances.</p><h3 id="consumer" tabindex="-1"><a class="header-anchor" href="#consumer" aria-hidden="true">#</a> Consumer</h3><p>Consumer groups are isolated from each other. It is easy to add/remove consumer groups at will.</p><p>Rebalancing help handle the case when consumers are added/removed from a group gracefully.</p><p>Consumer groups are rebalancing help us achieve scalability and fault tolerance.</p><h3 id="broker" tabindex="-1"><a class="header-anchor" href="#broker" aria-hidden="true">#</a> Broker</h3><p>How do brokers handle failure?</p><figure><img src="'+S+'" alt="broker-failure-recovery" tabindex="0" loading="lazy"><figcaption>broker-failure-recovery</figcaption></figure><ul><li>Once a broker fails, there are still enough replicas to avoid partition data loss</li><li>A new leader is elected and the broker coordinator redistributes partitions which were at the failed broker to existing replicas</li><li>Existing replicas pick up the new partitions and act as followers until they&#39;re caught up with the leader and become ISR</li></ul><p>Additional considerations to make the broker fault-tolerant:</p>',156),V=e("li",null,"The minimum number of ISRs balances latency and safety. You can fine-tune it to meet your needs.",-1),Z=e("li",null,"If all replicas of a partition are in the same node, then it's a waste of resources. Replicas should be across different brokers.",-1),N={href:"https://cwiki.apache.org/confluence/pages/viewpage.action?pageId=27846330",target:"_blank",rel:"noopener noreferrer"},Y=t('<p>How do we handle redistribution of replicas when a new broker is added?</p><figure><img src="'+P+'" alt="broker-replica-redistribution" tabindex="0" loading="lazy"><figcaption>broker-replica-redistribution</figcaption></figure><ul><li>We can temporarily allow more replicas than configured, until new broker catches up</li><li>Once it does, we can remove the partition replica which is no longer needed</li></ul><h3 id="partition" tabindex="-1"><a class="header-anchor" href="#partition" aria-hidden="true">#</a> Partition</h3><p>Whenever a new partition is added, the producer is notified and consumer rebalancing is triggered.</p><p>In terms of data storage, we can only store new messages to the new partition vs. trying to copy all old ones:</p><figure><img src="'+R+'" alt="partition-exmaple" tabindex="0" loading="lazy"><figcaption>partition-exmaple</figcaption></figure><p>Decreasing the number of partitions is more involved:</p><figure><img src="'+H+'" alt="partition-decrease" tabindex="0" loading="lazy"><figcaption>partition-decrease</figcaption></figure><ul><li>Once a partition is decommissioned, new messages are only received by remaining partitions</li><li>The decommissioned partition isn&#39;t removed immediately as messages can still be consumed from it</li><li>Once a pre-configured retention period passes, do we truncate the data and storage space is freed up</li><li>During the transitional period, producers only send messages to active partitions, but consumers read from all</li><li>Once retention period expires, consumers are rebalanced</li></ul><h3 id="data-delivery-semantics" tabindex="-1"><a class="header-anchor" href="#data-delivery-semantics" aria-hidden="true">#</a> Data delivery semantics</h3><p>Let&#39;s discuss different delivery semantics.</p><h3 id="at-most-once" tabindex="-1"><a class="header-anchor" href="#at-most-once" aria-hidden="true">#</a> At-most once</h3><p>With this guarantee, messages are delivered not more than once and could not be delivered at all.</p><figure><img src="'+K+'" alt="at-most-once" tabindex="0" loading="lazy"><figcaption>at-most-once</figcaption></figure><ul><li>Producer sends a message asynchronously to a topic. If message delivery fails, there is no retry.</li><li>Consumer fetches message and immediately commits offset. If consumer crashes before processing the message, the message will not be processed.</li></ul><h3 id="at-least-once" tabindex="-1"><a class="header-anchor" href="#at-least-once" aria-hidden="true">#</a> At-least once</h3><p>A message can be sent more than once and no message should be left unprocessed.</p><figure><img src="'+B+'" alt="at-least-once" tabindex="0" loading="lazy"><figcaption>at-least-once</figcaption></figure><ul><li>Producer sends message with<code>ack=1</code>or<code>ack=all</code>. If there is any issue, it will keep retrying.</li><li>Consumer fetches the message and consumes the offset only after it&#39;s done processing it.</li><li>It is possible for a message to be delivered more than once if eg consumer crashes before committing offset but after processing it.</li><li>This is why, this is good for use-cases where data duplication is acceptable or deduplication is possible.</li></ul><h3 id="exactly-once" tabindex="-1"><a class="header-anchor" href="#exactly-once" aria-hidden="true">#</a> Exactly once</h3><p>Extremely costly to implement for the system, albeit it&#39;s the friendliest guarantee to users:</p><figure><img src="'+F+'" alt="exactly-once" tabindex="0" loading="lazy"><figcaption>exactly-once</figcaption></figure><h3 id="advanced-features" tabindex="-1"><a class="header-anchor" href="#advanced-features" aria-hidden="true">#</a> Advanced features</h3><p>Let&#39;s discuss some advanced features, we might discuss in the interview.</p><h3 id="message-filtering" tabindex="-1"><a class="header-anchor" href="#message-filtering" aria-hidden="true">#</a> Message filtering</h3><p>Some consumers might want to only consume messages of a certain type within a partition.</p><p>This can be achieved by building separate topics for each subset of messages, but this can be costly if systems have too many differing use-cases.</p><ul><li>It is a waste of resources to store the same message on different topics</li><li>Producer is now tightly coupled to consumers as it changes with each new consumer requirement</li></ul><p>We can resolve this using message filtering.</p><ul><li>A naive approach would be to do the filtering on the consumer-side, but that introduces unnecessary consumer traffic</li><li>Alternatively, messages can have tags attached to them and consumers can specify which tags they&#39;re subscribed to</li><li>Filtering could also be done via the message payloads but that can be challenging and unsafe for encrypted/serialized messages</li><li>For more complex mathematical formulaes, the broker could implement a grammar parser or script executor, but that can be heavyweight for the message queue</li></ul><figure><img src="'+E+'" alt="message-filtering" tabindex="0" loading="lazy"><figcaption>message-filtering</figcaption></figure><h3 id="delayed-messages-scheduled-messages" tabindex="-1"><a class="header-anchor" href="#delayed-messages-scheduled-messages" aria-hidden="true">#</a> Delayed messages &amp; scheduled messages</h3><p>For some use-cases, we might want to delay or schedule message delivery. For example, we might submit a payment verification check for 30m from now, which triggers the consumer to see if a payment was successful.</p><p>This can be achieved by sending messages to temporary storage in the broker and moving the message to the partition at the right time:</p><figure><img src="'+L+'" alt="delayed-message-implementation" tabindex="0" loading="lazy"><figcaption>delayed-message-implementation</figcaption></figure>',36),G=e("li",null,"The temporary storage can be one or more special message topics",-1),X={href:"http://www.cs.columbia.edu/~nahum/w6998/papers/sosp87-timing-wheels.pdf",target:"_blank",rel:"noopener noreferrer"},U=e("h2",{id:"step-4-wrap-up",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#step-4-wrap-up","aria-hidden":"true"},"#"),a(" Step 4 - Wrap up")],-1),J=e("p",null,"Additional talking points:",-1),$=e("ul",null,[e("li",null,"Protocol of communication. Important considerations - support all use-cases and high data volume, as well as verify message integrity. Popular protocols - AMQP and Kafka protocol."),e("li",null,"Retry consumption - if we can't process a message immediately, we could send it to a dedicated retry topic to be attempted later."),e("li",null,"Historical data archive - old messages can be backed up in high-capacity storages such as HDFS or object storage (eg S3).")],-1);function ee(ae,se){const s=r("ExternalLinkIcon");return n(),l("div",null,[j,e("ul",null,[V,Z,e("li",null,[a("If all replicas of a partition crash, then the data is lost forever. Spreading replicas across data centers can help, but it adds up a lot of latency. One option is to use"),e("a",N,[a("data mirroring"),i(s)]),a("as a work around.")])]),Y,e("ul",null,[G,e("li",null,[a("The timing function can be achieved using dedicated delay queues or a"),e("a",X,[a("hierarchical time wheel"),i(s)])])]),U,J,$])}const te=o(Q,[["render",ee],["__file","39_distributed_message_queue.html.vue"]]);export{te as default};
