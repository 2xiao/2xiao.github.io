import{_ as e,o as i,c as a,a as s}from"./app-rLUZugrR.js";const t="/assets/system-design-163-f2QfN0-Q.png",l="/assets/system-design-164-5nqga7mn.png",o="/assets/system-design-165-GMTOkcJB.png",n="/assets/system-design-166-3BblmrCW.png",r="/assets/system-design-167-imRu9B5G.png",d="/assets/system-design-168-EBdA6XbP.png",c="/assets/system-design-169-hr5-WeFb.png",p="/assets/system-design-170-Q_57aVoS.png",u="/assets/system-design-171-3e3aA0kI.png",h="/assets/system-design-172-Hm0dp-Og.png",f="/assets/system-design-173-aqxUENeY.png",g="/assets/system-design-174-AmLGCSN5.png",m={},b=s('<h1 id="design-google-drive" tabindex="-1"><a class="header-anchor" href="#design-google-drive" aria-hidden="true">#</a> Design Google Drive</h1><p>Google Drive is a cloud file storage product, which helps you store documents, videos, etc from the cloud.</p><p>You can access them from any device and share them with friends and family.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the problem and establish design scope</h2><ul><li>C: What are most important features?</li><li>I: Upload/download files, file sync and notifications</li><li>C: Mobile or web?</li><li>I: Both</li><li>C: What are the supported file formats?</li><li>I: Any file type</li><li>C: Do files need to be encrypted?</li><li>I: Yes, files in storage need to be encrypted</li><li>C: Is the a file size limit?</li><li>I: Yes, files need to be 10 gb or smaller</li><li>C: How many users does the app have?</li><li>I: 10mil DAU</li></ul><p>Features we&#39;ll focus on:</p><ul><li>Adding files</li><li>Downloading files</li><li>Sync files across devices</li><li>See file revisions</li><li>Share file with friends</li><li>Send a notification when file is edited/deleted/shared</li></ul><p>Features not discussed:</p><ul><li>Collaborative editing</li></ul><p>Non-functional requirements:</p><ul><li>Reliability - data loss is unacceptable</li><li>Fast sync speed</li><li>Bandwidth usage - users will get unhappy if app consumes too much network traffic or battery</li><li>Scalability - we need to handle a lot of traffic</li><li>High availability - users should be able to use the system even when some services are down</li></ul><h3 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back of the envelope estimation</h3><ul><li>Assume 50mil sign ups and 10mil DAU</li><li>Users get 10 gb free space</li><li>Users upload 2 files per day, average size is 500kb</li><li>1:1 read-write ratio</li><li>Total space allocated - 50mil * 10gb = 500pb</li><li>QPS for upload API - 10mil * 2 uploads / 24h / 3600s = ~240</li><li>Peak QPS = 480</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - propose high-level design and get buy-in</h2><p>In this chapter, we&#39;ll use a different approach than other ones - we&#39;ll start building the design from a single server and scale out from there.</p><p>We&#39;ll start from:</p><ul><li>A web server to upload and download files</li><li>A database to keep track of metadata - user data, login info, files info, etc</li><li>Storage system to store the files</li></ul><p>Example storage we could use:</p><figure><img src="'+t+`" alt="storage-example" tabindex="0" loading="lazy"><figcaption>storage-example</figcaption></figure><h3 id="apis" tabindex="-1"><a class="header-anchor" href="#apis" aria-hidden="true">#</a> APIs</h3><p>Upload file:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>https://api.example.com/files/upload?uploadType=resumable
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This endpoint is used for uploading files with support for simple upload and resumable upload, which is used for large files. Resumable upload is achieved by retrieving an upload URL and uploading the file while monitoring upload state. If disturbed, resume the upload.</p><p>Download file:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>https://api.example.com/files/download
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The payload specifies which file to download:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
    &quot;path&quot;: &quot;/recipes/soup/best_soup.txt&quot;
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Get file revisions:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>https://api.example.com/files/list_revisions
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>params:</p><ul><li>path to file for which revision history is retrieved</li><li>maximum number of revisions to return</li></ul><p>All the APIs require authentication and use HTTPS.</p><h3 id="move-away-from-single-server" tabindex="-1"><a class="header-anchor" href="#move-away-from-single-server" aria-hidden="true">#</a> Move away from single server</h3><p>As more files are uploaded, at some point, you reach your storage&#39;s capacity.</p><p>One option to scale your storage server is by implementing sharing - each user&#39;s data is stored on separate servers:</p><figure><img src="`+l+'" alt="sharding-example" tabindex="0" loading="lazy"><figcaption>sharding-example</figcaption></figure><p>This solves your issue but you&#39;re still worried about potential data loss.</p><p>A good option to address that is to use an off-the-shelf solution like Amazon S3 which offers replication (same-region/cross-region) out of the box:</p><figure><img src="'+o+'" alt="amazon-s3" tabindex="0" loading="lazy"><figcaption>amazon-s3</figcaption></figure><p>Other areas you could improve:</p><ul><li>Load balancing - this ensures evenly distributed network traffic to your web server replicas.</li><li>More web servers - with the advent of a load balancer, you can easily scale your web server layer by adding more servers.</li><li>Metadata database - move the database away from the server to avoid single points of failure. You can also setup replication and sharding to meet scalability requirements.</li><li>File storage - Amazon S3 for storage. To ensure availability and durability, files are replicated in two separate geographical regions.</li></ul><p>Here&#39;s the updated design:</p><figure><img src="'+n+'" alt="updated-simple-design" tabindex="0" loading="lazy"><figcaption>updated-simple-design</figcaption></figure><h3 id="sync-conflicts" tabindex="-1"><a class="header-anchor" href="#sync-conflicts" aria-hidden="true">#</a> Sync conflicts</h3><p>Once the user base grows sufficiently, sync conflicts are unavoidable.</p><p>To address this, we can apply a strategy where the first who manages to modify a file first wins:</p><figure><img src="'+r+'" alt="sync-conflict" tabindex="0" loading="lazy"><figcaption>sync-conflict</figcaption></figure><p>What happens once you get a conflict? We generate a second version of the file which represents the alternative file version and it&#39;s up to the user to merge it:</p><figure><img src="'+d+'" alt="sync-conflict-example" tabindex="0" loading="lazy"><figcaption>sync-conflict-example</figcaption></figure><h3 id="high-level-design" tabindex="-1"><a class="header-anchor" href="#high-level-design" aria-hidden="true">#</a> High-level design</h3><figure><img src="'+c+'" alt="high-level-design" tabindex="0" loading="lazy"><figcaption>high-level-design</figcaption></figure><ul><li>User uses the application through a browser or a mobile app</li><li>Block servers upload files to cloud storage. Block storage is a technology which allows you to split a big file in blocks and store the blocks in a backing storage. Dropbox, for example, stores blocks of size 4mb.</li><li>Cloud storage - a file split into multiple blocks is stored in cloud storage</li><li>Cold storage - used for storing inactive files, infrequently accessed.</li><li>Load balancer - evenly distributes requests among API servers.</li><li>API servers - responsible for anything other than uploading files. Authentication, user profile management, updating file metadata, etc.</li><li>Metadata database - stores metadata about files uploaded to cloud storage.</li><li>Metadata cache - some of the metadata is cached for fast retrieval.</li><li>Notification service - Publisher/subscriber system which notifies users when a file is updated/edited/removed so that they can pull the latest changes.</li><li>Offline backup queue - used to queue file changes for users who are offline so that they can pull them once they come back online.</li></ul><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design deep dive</h2><p>Let&#39;s explore:</p><ul><li>block servers</li><li>metadata database</li><li>upload/download flow</li><li>notification service</li><li>saving storage space</li><li>failure handling</li></ul><h3 id="block-servers" tabindex="-1"><a class="header-anchor" href="#block-servers" aria-hidden="true">#</a> Block servers</h3><p>For large files, it&#39;s infeasible to send the whole file on each update as it consumes a lot of bandwidth.</p><p>Two optimizations we&#39;re going to explore:</p><ul><li>Delta sync - once a file is modified, only modified blocks are sent to the block servers instead of the whole file.</li><li>Compression - applying compression on blocks can significantly reduce data size. Different algorithms are suitable for different file types, eg for text files, we&#39;ll use gzip/bzip2.</li></ul><p>Apart from splitting files in blocks, the block servers also apply encryption prior to storing files in file storage:</p><figure><img src="'+p+'" alt="block-servers-deep-dive" tabindex="0" loading="lazy"><figcaption>block-servers-deep-dive</figcaption></figure><p>Example delta sync:</p><figure><img src="'+u+'" alt="delta-sync" tabindex="0" loading="lazy"><figcaption>delta-sync</figcaption></figure><h3 id="high-consistency-requirement" tabindex="-1"><a class="header-anchor" href="#high-consistency-requirement" aria-hidden="true">#</a> High consistency requirement</h3><p>Our system requires strong consistency as it&#39;s unacceptable to show different versions of a file to different people.</p><p>This is mainly problematic when we use caches, in particular the metadata cache in our example. To sustain strong consistency, we need to:</p><ul><li>keep cache master and replicas consistent</li><li>invalidate caches on database write</li></ul><p>For the database, strong consistency is guaranteed as long as we use a relational database, which supports ACID (all typically do).</p><h3 id="metadata-database" tabindex="-1"><a class="header-anchor" href="#metadata-database" aria-hidden="true">#</a> Metadata database</h3><p>Here&#39;s a simplified table schema for the metadata db (only interesting fields are shown):</p><figure><img src="'+h+'" alt="metadata-db-deep-dive" tabindex="0" loading="lazy"><figcaption>metadata-db-deep-dive</figcaption></figure><ul><li>User table contains basic information about the user such as username, email, profile photo, etc.</li><li>Device table stores device info. Push_id is used for sending push notifications. Users can have multiple devices.</li><li>Namespace - root directory of a user</li><li>File table stores everything related to a file</li><li>File_version stores the version history of a file. Existing fields are read-only to sustain file integrity.</li><li>Block - stores everything related to a file block. A file version can be reconstructed by joining all blocks in the correct version.</li></ul><h3 id="upload-flow" tabindex="-1"><a class="header-anchor" href="#upload-flow" aria-hidden="true">#</a> Upload flow</h3><figure><img src="'+f+'" alt="upload-flow" tabindex="0" loading="lazy"><figcaption>upload-flow</figcaption></figure><p>In the above flow, two requests are sent in parallel - updating file metadata and uploading the file to cloud storage.</p><p>Add file metadata:</p><ul><li>Client 1 sends request to update file metadata</li><li>New file metadata is stored and upload status is set to &quot;pending&quot;</li><li>Notify the notification service that a new file is being added.</li><li>Notification service notifies relevant clients about the file upload.</li></ul><p>Upload files to cloud storage:</p><ul><li>Client 1 uploads file contents to block servers</li><li>Block servers chunk the file in blocks, compresses, encrypts them and uploads to cloud storage</li><li>Once file is uploaded, upload completion callback is triggered. Request is sent to API servers.</li><li>File status is changed to &quot;uploaded&quot; in Metadata DB.</li><li>Notification service is notified of file uploaded event and client 2 is notified about the new file.</li></ul><h3 id="download-flow" tabindex="-1"><a class="header-anchor" href="#download-flow" aria-hidden="true">#</a> Download flow</h3><p>Download flow is triggered when file is added or edited elsewhere. Client is notified via:</p><ul><li>Notification if online</li><li>New changes are cached until user comes online if offline at the moment</li></ul><p>Once a client is notified of the changes, it requests the file metadata and then downloads the blocks to reconstruct the file:</p><figure><img src="'+g+'" alt="download-flow" tabindex="0" loading="lazy"><figcaption>download-flow</figcaption></figure><ul><li>Notification service informs client 2 of file changes</li><li>Client 2 fetches metadata from API servers</li><li>API servers fetch metadata from metadata DB</li><li>Client 2 gets the metadata</li><li>Once client receives the metadata, it sends requests to block servers to download blocks</li><li>Block servers download blocks from cloud storage and forwards them to the client</li></ul><h3 id="notification-service" tabindex="-1"><a class="header-anchor" href="#notification-service" aria-hidden="true">#</a> Notification service</h3><p>The notification service enables file changes to be communicated to clients as they happen.</p><p>Clients can communicate with the notification service via:</p><ul><li>long polling (eg Dropbox uses this approach)</li><li>Web sockets - communication is persistent and bidirectional</li></ul><p>Both options work well but we opt for long polling because:</p><ul><li>Communication for notification service is not bi-directional. Server sends information to clients, not vice versa.</li><li>WebSocket is meant for real-time bidirectional communication. For google drive, notifications are sent infrequently.</li></ul><p>With long polling, the client sends a request to the server which stays open until a change is received or timeout is reached. After that, a subsequent request is sent for next couple of changes.</p><h3 id="save-storage-space" tabindex="-1"><a class="header-anchor" href="#save-storage-space" aria-hidden="true">#</a> Save storage space</h3><p>To support file version history and ensure reliability, multiple versions of a file are stored across multiple data centers.</p><p>Storage space can be filled up quickly. Three techniques can be applied to save storage space:</p><ul><li>De-duplicate data blocks - if two blocks have the same hash, we can only store them once.</li><li>Adopt an intelligent backup strategy - set a limit on max version history and aggregate frequent edits into a single version.</li><li>Move infrequently accessed data to cold storage - eg Amazon S3 glacier is a good option for this, which is much cheaper than Amazon S3.</li></ul><h3 id="failure-handling" tabindex="-1"><a class="header-anchor" href="#failure-handling" aria-hidden="true">#</a> Failure handling</h3><p>Some typical failures and how you could resolve them:</p><ul><li>Load balancer failure - If a load balancer fails, a secondary becomes active and picks up the traffic.</li><li>Block server failure - If a block server fails, other replicas pick up the traffic and finish the job.</li><li>Cloud storage failure - S3 buckets are replicated across regions. If one region fails, traffic is redirected to the other one.</li><li>API server failure - Traffic is redirected to other service instances by the load balancer.</li><li>Metadata cache failure - Metadata cache servers are replicated multiple times. If one goes down, other nodes are still available.</li><li>Metadata DB failure - if master is down, promote one of the slaves to be master. If slave is down, use another one for read operations.</li><li>Notification service failure - If long polling connections are lost, clients reconnect to a different service replica, but reconnection of millions of clients will take some time.</li><li>Offline backup queue failure - Queues are replicated multiple times. If one queue fails, consumers need to resubscribe to the backup queue.</li></ul><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap up</h2><p>Properties of our Google Drive system design in a nutshell:</p><ul><li>Strongly consistent</li><li>Low network bandwidth</li><li>Fast sync</li></ul><p>Our design contains two flows - file upload and file sync.</p><p>If time permits, you could discuss alternative design approaches as there is no perfect design. For example, we can upload blocks directly to cloud storage instead of going through block servers.</p><p>This is faster than our approach but has drawbacks:</p><ul><li>Chunking, compression, encryption need to be implemented on different platforms (Android, iOS, Web).</li><li>Client can be hacked so implementing encryption client-side is not ideal.</li></ul><p>Another interesting discussion is moving online/offline logic to separate service so that other services can reuse it to implement interesting functionality.</p>',107),v=[b];function y(w,x){return i(),a("div",null,v)}const _=e(m,[["render",y],["__file","35_design_google_drive.html.vue"]]);export{_ as default};
