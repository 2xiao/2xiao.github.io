import{_ as n,r,o as l,c as d,b as t,d as e,e as s,w as o,a as i}from"./app-53EDpe6z.js";const c="/assets/system-design-313-CtkN4ntk.png",h="/assets/system-design-314-tj9sSMga.png",p="/assets/system-design-315-B4c2lG6E.png",u="/assets/system-design-316-KlUdfDRA.png",m="/assets/system-design-317-OB7WhoRW.png",g="/assets/system-design-318-tBOOsLqO.png",v="/assets/system-design-319-qXm_C_PI.png",b="/assets/system-design-320-QkkEybBe.png",y="/assets/system-design-321-urw-VfJE.png",f="/assets/system-design-322-PRVw1US1.png",w="/assets/system-design-323-xyc4Anzg.png",_="/assets/system-design-324-BTAEVd3v.png",k="/assets/system-design-325-AD4yjeSB.png",q="/assets/system-design-326-TF7JhHdB.png",x="/assets/system-design-327-MbMkjHqI.png",D="/assets/system-design-328-YwZktpq_.png",I="/assets/system-design-329-hU12XtQ7.png",T={},A=i('<h1 id="hotel-reservation-system" tabindex="-1"><a class="header-anchor" href="#hotel-reservation-system" aria-hidden="true">#</a> Hotel Reservation System</h1><p>In this chapter, we&#39;re designing a hotel reservation system, similar to Marriott International.</p><p>Applicable to other types of systems as well - Airbnb, flight reservation, movie ticket booking.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><p>Before diving into designing the system, we should ask the interviewer questions to clarify the scope:</p><ul><li>C: What is the scale of the system?</li><li>I: We&#39;re building a website for a hotel chain \\w 5000 hotels and 1mil rooms</li><li>C: Do customers pay when they make a reservation or when they arrive at the hotel?</li><li>I: They pay in full when making reservations.</li><li>C: Do customers book hotel rooms through the website only? Do we have to support other reservation options such as phone calls?</li><li>I: They make bookings through the website or app only.</li><li>C: Can customers cancel reservations?</li><li>I: Yes</li><li>C: Other things to consider?</li><li>I: Yes, we allow overbooking by 10%. Hotel will sell more rooms than there actually are. Hotels do this in anticipation that clients will cancel bookings.</li><li>C: Since not much time, we&#39;ll focus on - show hotel-related page, hotel-room details page, reserve a room, admin panel, support overbooking.</li><li>I: Sounds good.</li><li>I: One more thing - hotel prices change all the time. Assume a hotel room&#39;s price changes every day.</li><li>C: OK.</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>Support high concurrency - there might be a lot of customers trying to book the same hotel during peak season.</li><li>Moderate latency - it&#39;s ideal to have low latency when a user makes a reservation, but it&#39;s acceptable if the system takes a few seconds to process it.</li></ul><h3 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back-of-the-envelope estimation</h3><ul><li>5000 hotels and 1mil rooms in total</li><li>Assume 70% of rooms are occupied and average stay duration is 3 days</li><li>Estimated daily reservations - 1mil * 0.7 / 3 = ~240k reservations per day</li><li>Reservations per second - 240k / 10^5 seconds in a day = ~3. Average reservation TPS is low.</li></ul><p>Let&#39;s estimate the QPS. If we assume that there are three steps to reach the reservation page and there is a 10% conversion rate per page, we can estimate that if there are 3 reservations, then there must be 30 views of reservation page and 300 views of hotel room detail page.</p><figure><img src="'+c+`" alt="qps-estimation" tabindex="0" loading="lazy"><figcaption>qps-estimation</figcaption></figure><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><p>We&#39;ll explore - API Design, Data model, high-level design.</p><h3 id="api-design" tabindex="-1"><a class="header-anchor" href="#api-design" aria-hidden="true">#</a> API Design</h3><p>This API Design focuses on the core endpoints (using RESTful practices), we&#39;ll need in order to support a hotel reservation system.</p><p>A fully-fledged system would require a more extensive API with support for searching for rooms based on lots of criteria, but we won&#39;t be focusing on that in this section. Reason is that they aren&#39;t technically challenging, so they&#39;re out of scope.</p><p>Hotel-related API</p><ul><li><code>GET /v1/hotels/{id}</code>- get detailed info about a hotel</li><li><code>POST /v1/hotels</code>- add a new hotel. Only available to ops</li><li><code>PUT /v1/hotels/{id}</code>- update hotel info. Only available to ops</li><li><code>DELETE /v1/hotels/{id}</code>- delete a hotel. API is only available to ops</li></ul><p>Room-related API</p><ul><li><code>GET /v1/hotels/{id}/rooms/{id}</code>- get detailed information about a room</li><li><code>POST /v1/hotels/{id}/rooms</code>- Add a room. Only available to ops</li><li><code>PUT /v1/hotels/{id}/rooms/{id}</code>- Update room info. Only available to ops</li><li><code>DELETE /v1/hotels/{id}/rooms/{id}</code>- Delete a room. Only available to ops</li></ul><p>Reservation-related API</p><ul><li><code>GET /v1/reservations</code>- get reservation history of current user</li><li><code>GET /v1/reservations/{id}</code>- get detailed info about a reservation</li><li><code>POST /v1/reservations</code>- make a new reservation</li><li><code>DELETE /v1/reservations/{id}</code>- cancel a reservation</li></ul><p>Here&#39;s an example request to make a reservation:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
  &quot;startDate&quot;:&quot;2021-04-28&quot;,
  &quot;endDate&quot;:&quot;2021-04-30&quot;,
  &quot;hotelID&quot;:&quot;245&quot;,
  &quot;roomID&quot;:&quot;U12354673389&quot;,
  &quot;reservationID&quot;:&quot;13422445&quot;
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,25),P=t("code",null,"reservationID",-1),S=i('<h3 id="data-model" tabindex="-1"><a class="header-anchor" href="#data-model" aria-hidden="true">#</a> Data model</h3><p>Before we choose what database to use, let&#39;s consider our access patterns.</p><p>We need to support the following queries:</p><ul><li>View detailed info about a hotel</li><li>Find available types of rooms given a date range</li><li>Record a reservation</li><li>Look up a reservation or past history of reservations</li></ul><p>From our estimations, we know the scale of the system is not large, but we need to prepare for traffic surges.</p><p>Given this knowledge, we&#39;ll choose a relational database because:</p><ul><li>Relational DBs work well with read-heavy and less write-heavy systems.</li><li>NoSQL databases are normally optimized for writes, but we know we won&#39;t have many as only a fraction of users who visit the site make a reservation.</li><li>Relational DBs provide ACID guarantees. These are important for such a system as without them, we won&#39;t be able to prevent problems such as negative balance, double charge, etc.</li><li>Relational DBs can easily model the data as the structure is very clear.</li></ul><p>Here is our schema design:</p><figure><img src="'+h+'" alt="schema-design" tabindex="0" loading="lazy"><figcaption>schema-design</figcaption></figure><p>Most fields are self-explanatory. Only field worth mentioning is the<code>status</code>field which represents the state machine of a given room:</p><figure><img src="'+p+'" alt="status-state-machine" tabindex="0" loading="lazy"><figcaption>status-state-machine</figcaption></figure><p>This data model works well for a system like Airbnb, but not for hotels where users don&#39;t reserve a particular room but a room type. They reserve a type of room and a room number is chosen at the point of reservation.</p>',12),E=i('<h3 id="high-level-design" tabindex="-1"><a class="header-anchor" href="#high-level-design" aria-hidden="true">#</a> High-level Design</h3><p>We&#39;ve chosen a microservice architecture for this design. It has gained great popularity in recent years:</p><figure><img src="'+u+`" alt="high-level-design" tabindex="0" loading="lazy"><figcaption>high-level-design</figcaption></figure><ul><li>Users book a hotel room on their phone or computer</li><li>Admin perform administrative functions such as refunding/cancelling a payment, etc</li><li>CDN caches static resources such as JS bundles, images, videos, etc</li><li>Public API Gateway - fully-managed service which supports rate limiting, authentication, etc.</li><li>Internal APIs - only visible to authorized personnel. Usually protected by a VPN.</li><li>Hotel service - provides detailed information about hotels and rooms. Hotel and room data is static, so it can be cached aggressively.</li><li>Rate service - provides room rates for different future dates. An interesting note about this domain is that prices depend on how full a hotel is at a given day.</li><li>Reservation service - receives reservation requests and reserves hotel rooms. Also tracks room inventory as reservations are made/cancelled.</li><li>Payment service - processes payments and updates reservation statuses on success.</li><li>Hotel management service - available to authorized personnel only. Allows certain administrative functions for managing and viewing reservations, hotels, etc.</li></ul><p>Inter-service communication can be facilitated via a RPC framework, such as gRPC.</p><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>Let&#39;s dive deeper into:</p><ul><li>Improved data model</li><li>Concurrency issues</li><li>Scalability</li><li>Resolving data inconsistency in microservices</li></ul><h3 id="improved-data-model" tabindex="-1"><a class="header-anchor" href="#improved-data-model" aria-hidden="true">#</a> Improved data model</h3><p>As mentioned in a previous section, we need to amend our API and schema to enable reserving a type of room vs. a particular one.</p><p>For the reservation API, we no longer reserve a<code>roomID</code>, but we reserve a<code>roomTypeID</code>:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>POST /v1/reservations
{
  &quot;startDate&quot;:&quot;2021-04-28&quot;,
  &quot;endDate&quot;:&quot;2021-04-30&quot;,
  &quot;hotelID&quot;:&quot;245&quot;,
  &quot;roomTypeID&quot;:&quot;12354673389&quot;,
  &quot;roomCount&quot;:&quot;3&quot;,
  &quot;reservationID&quot;:&quot;13422445&quot;
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Here&#39;s the updated schema:</p><figure><img src="`+m+'" alt="updated-schema" tabindex="0" loading="lazy"><figcaption>updated-schema</figcaption></figure><ul><li>room - contains information about a room</li><li>room_type_rate - contains information about prices for a given room type</li><li>reservation - records guest reservation data</li><li>room_type_inventory - stores inventory data about hotel rooms.</li></ul><p>Let&#39;s take a look at the<code>room_type_inventory</code>columns as that table is more interesting:</p><ul><li>hotel_id - id of hotel</li><li>room_type_id - id of a room type</li><li>date - a single date</li><li>total_inventory - total number of rooms minus those that are temporarily taken off the inventory.</li><li>total_reserved - total number of rooms booked for given (hotel_id, room_type_id, date)</li></ul><p>There are alternative ways to design this table, but having one room per (hotel_id, room_type_id, date) enables easy reservation management and easier queries.</p><p>The rows in the table are pre-populated using a daily CRON job.</p><p>Sample data:</p><table><thead><tr><th>hotel_id</th><th>room_type_id</th><th>date</th><th>total_inventory</th><th>total_reserved</th></tr></thead><tbody><tr><td>211</td><td>1001</td><td>2021-06-01</td><td>100</td><td>80</td></tr><tr><td>211</td><td>1001</td><td>2021-06-02</td><td>100</td><td>82</td></tr><tr><td>211</td><td>1001</td><td>2021-06-03</td><td>100</td><td>86</td></tr><tr><td>211</td><td>1001</td><td>...</td><td>...</td><td></td></tr><tr><td>211</td><td>1001</td><td>2023-05-31</td><td>100</td><td>0</td></tr><tr><td>211</td><td>1002</td><td>2021-06-01</td><td>200</td><td>16</td></tr><tr><td>2210</td><td>101</td><td>2021-06-01</td><td>30</td><td>23</td></tr><tr><td>2210</td><td>101</td><td>2021-06-02</td><td>30</td><td>25</td></tr></tbody></table><p>Sample SQL query to check the availability of a type of room:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>SELECT date, total_inventory, total_reserved\nFROM room_type_inventory\nWHERE room_type_id = ${roomTypeId} AND hotel_id = ${hotelId}\nAND date between ${startDate} and ${endDate}\n</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>How to check availability for a specified number of rooms using that data (note that we support overbooking):</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>if (total_reserved + ${numberOfRoomsToReserve}) &lt;= 110% * total_inventory\n</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now let&#39;s do some estimation about the storage volume.</p><ul><li>We have 5000 hotels.</li><li>Each hotel has 20 types of rooms.</li><li>5000 _ 20 _ 2 (years) * 365 (days) = 73mil rows</li></ul><p>73 million rows is not a lot of data and a single database server can handle it. It makes sense, however, to setup read replication (potentially across different zones) to enable high availability.</p><p>Follow-up question - if reservation data is too large for a single database, what would you do?</p><ul><li>Store only current and future reservation data. Reservation history can be moved to cold storage.</li><li>Database sharding - we can shard our data by<code>hash(hotel_id) % servers_cnt</code>as we always select the<code>hotel_id</code>in our queries.</li></ul><h3 id="concurrency-issues" tabindex="-1"><a class="header-anchor" href="#concurrency-issues" aria-hidden="true">#</a> Concurrency issues</h3><p>Another important problem to address is double booking.</p><p>There are two issues to address:</p><ul><li>Same user clicks on &quot;book&quot; twice</li><li>Multiple users try to book a room at the same time</li></ul><p>Here&#39;s a visualization of the first problem:</p><figure><img src="'+g+'" alt="double-booking-single-user" tabindex="0" loading="lazy"><figcaption>double-booking-single-user</figcaption></figure><p>There are two approaches to solving this problem:</p><ul><li>Client-side handling - front-end can disable the book button once clicked. If a user disabled javascript, however, they won&#39;t see the button becoming grayed out.</li><li>Idemptent API - Add an idempotency key to the API, which enables a user to execute an action once, regardless of how many times the endpoint is invoked:</li></ul><figure><img src="'+v+'" alt="idempotency" tabindex="0" loading="lazy"><figcaption>idempotency</figcaption></figure><p>Here&#39;s how this flow works:</p><ul><li>A reservation order is generated once you&#39;re in the process of filling in your details and making a booking. The reservation order is generated using a globally unique identifier.</li><li>Submit reservation 1 using the<code>reservation_id</code>generated in the previous step.</li><li>If &quot;complete booking&quot; is clicked a second time, the same<code>reservation_id</code>is sent and the backend detects that this is a duplicate reservation.</li><li>The duplication is avoided by making the<code>reservation_id</code>column have a unique constraint, preventing multiple records with that id being stored in the DB.</li></ul><figure><img src="'+b+'" alt="unique-constraint-violation" tabindex="0" loading="lazy"><figcaption>unique-constraint-violation</figcaption></figure><p>What if there are multiple users making the same reservation?</p><figure><img src="'+y+`" alt="double-booking-multiple-users" tabindex="0" loading="lazy"><figcaption>double-booking-multiple-users</figcaption></figure><ul><li>Let&#39;s assume the transaction isolation level is not serializable</li><li>User 1 and 2 attempt to book the same room at the same time.</li><li>Transaction 1 checks if there are enough rooms - there are</li><li>Transaction 2 check if there are enough rooms - there are</li><li>Transaction 2 reserves the room and updates the inventory</li><li>Transaction 1 also reserves the room as it still sees there are 99<code>total_reserved</code>rooms out of 100.</li><li>Both transactions successfully commit the changes</li></ul><p>This problem can be solved using some form of locking mechanism:</p><ul><li>Pessimistic locking</li><li>Optimistic locking</li><li>Database constraints</li></ul><p>Here&#39;s the SQL we use to reserve a room:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>## step 1: check room inventory {#step-1-check-room-inventory}
SELECT date, total_inventory, total_reserved
FROM room_type_inventory
WHERE room_type_id = \${roomTypeId} AND hotel_id = \${hotelId}
AND date between \${startDate} and \${endDate}

## For every entry returned from step 1 {#for-every-entry-returned-from-step-1}
if((total_reserved + \${numberOfRoomsToReserve}) &gt; 110% * total_inventory) {
  Rollback
}

## step 2: reserve rooms {#step-2-reserve-rooms}
UPDATE room_type_inventory
SET total_reserved = total_reserved + \${numberOfRoomsToReserve}
WHERE room_type_id = \${roomTypeId}
AND date between \${startDate} and \${endDate}

Commit
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="option-1-pessimistic-locking" tabindex="-1"><a class="header-anchor" href="#option-1-pessimistic-locking" aria-hidden="true">#</a> Option 1: Pessimistic locking</h3><p>Pessimistic locking prevents simultaneous updates by putting a lock on a record while it&#39;s being updated.</p><p>This can be done in MySQL by using the<code>SELECT... FOR UPDATE</code>query, which locks the rows selected by the query until the transaction is committed.</p><figure><img src="`+f+'" alt="pessimistic-locking" tabindex="0" loading="lazy"><figcaption>pessimistic-locking</figcaption></figure><p>Pros:</p><ul><li>Prevents applications from updating data that is being changed</li><li>Easy to implement and avoids conflict by serializing updates. Useful when there is heavy data contention.</li></ul><p>Cons:</p><ul><li>Deadlocks may occur when multiple resources are locked.</li><li>This approach is not scalable - if transaction is locked for too long, this has impact on all other transactions trying to access the resource.</li><li>The impact is severe when the query selects a lot of resources and the transaction is long-lived.</li></ul><p>The author doesn&#39;t recommend this approach due to its scalability issues.</p><h3 id="option-2-optimistic-locking" tabindex="-1"><a class="header-anchor" href="#option-2-optimistic-locking" aria-hidden="true">#</a> Option 2: Optimistic locking</h3><p>Optimistic locking allows multiple users to attempt to update a record at the same time.</p><p>There are two common ways to implement it - version numbers and timestamps. Version numbers are recommended as server clocks can be inaccurate.</p><figure><img src="'+w+'" alt="optimistic-locking" tabindex="0" loading="lazy"><figcaption>optimistic-locking</figcaption></figure><ul><li>A new<code>version</code>column is added to the database table</li><li>Before a user modifies a database row, the version number is read</li><li>When the user updates the row, the version number is increased by 1 and written back to the database</li><li>Database validation prevents the insert if the new version number doesn&#39;t exceed the previous one</li></ul><p>Optimistic locking is usually faster than pessimistic locking as we&#39;re not locking the database. Its performance tends to degrade when concurrency is high, however, as that leads to a lot of rollbacks.</p><p>Pros:</p><ul><li>It prevents applications from editing stale data</li><li>We don&#39;t need to acquire a lock in the database</li><li>Preferred option when data contention is low, ie rarely are there update conflicts</li></ul><p>Cons:</p><ul><li>Performance is poor when data contention is high</li></ul><p>Optimistic locking is a good option for our system as reservation QPS is not extremely high.</p><h3 id="option-3-database-constraints" tabindex="-1"><a class="header-anchor" href="#option-3-database-constraints" aria-hidden="true">#</a> Option 3: Database constraints</h3><p>This approach is very similar to optimistic locking, but the guardrails are implemented using a database constraint:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>CONSTRAINT `check_room_count` CHECK((`total_inventory - total_reserved` &gt;= 0))\n</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><figure><img src="'+_+'" alt="database-constraint" tabindex="0" loading="lazy"><figcaption>database-constraint</figcaption></figure><p>Pros:</p><ul><li>Easy to implement</li><li>Works well when data contention is small</li></ul><p>Cons:</p><ul><li>Similar to optimistic locking, performs poorly when data contention is high</li><li>Database constraints cannot be easily version-controlled like application code</li><li>Not all databases support constraints</li></ul><p>This is another good option for a hotel reservation system due to its ease of implementation.</p><h3 id="scalability" tabindex="-1"><a class="header-anchor" href="#scalability" aria-hidden="true">#</a> Scalability</h3><p>Usually, the load of a hotel reservation system is not high.</p><p>However, the interviewer might ask you how you&#39;d handle a situation where the system gets adopted for a larger, popular travel site such as booking.com In that case, QPS can be 1000 times larger.</p><p>When there is such a situation, it is important to understand where our bottlenecks are. All the services are stateless, so they can be easily scaled via replication.</p><p>The database, however, is stateful and it&#39;s not as obvious how it can get scaled.</p><p>One way to scale it is by implementing database sharding - we can split the data across multiple databases, where each of them contain a portion of the data.</p><p>We can shard based on<code>hotel_id</code>as all queries filter based on it. Assuming, QPS is 30,000, after sharding the database in 16 shards, each shard handles 1875 QPS, which is within a single MySQL cluster&#39;s load capacity.</p><figure><img src="'+k+'" alt="database-sharding" tabindex="0" loading="lazy"><figcaption>database-sharding</figcaption></figure><p>We can also utilize caching for room inventory and reservations via Redis. We can set TTL so that old data can expire for days which are past.</p><figure><img src="'+q+`" alt="inventory-cache" tabindex="0" loading="lazy"><figcaption>inventory-cache</figcaption></figure><p>The way we store an inventory is based on the<code>hotel_id</code>,<code>room_type_id</code>and<code>date</code>:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>key: hotelID_roomTypeID_{date}
value: the number of available rooms for the given hotel ID, room type ID and date.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Data consistency happens async and is managed by using a CDC streaming mechanism - database changes are read and applied to a separate system. Debezium is a popular option for synchronizing database changes with Redis.</p><p>Using such a mechanism, there is a possibility that the cache and database are inconsistent for some time. This is fine in our case because the database will prevent us from making an invalid reservation.</p><p>This will cause some issue on the UI as a user would have to refresh the page to see that &quot;there are no more rooms left&quot;, but that is something which can happen regardless of this issue if eg a person hesitates a lot before making a reservation.</p><p>Caching pros:</p><ul><li>Reduced database load</li><li>High performance, as Redis manages data in-memory</li></ul><p>Caching cons:</p><ul><li>Maintaining data consistency between cache and DB is hard. We need to consider how the inconsistency impacts user experience.</li></ul><h3 id="data-consistency-among-services" tabindex="-1"><a class="header-anchor" href="#data-consistency-among-services" aria-hidden="true">#</a> Data consistency among services</h3><p>A monolithic application enables us to use a shared relational database for ensuring data consistency.</p><p>In our microservice design, we chose a hybrid approach where some services are separate, but the reservation and inventory APIs are handled by the same servicefor the reservation and inventory APIs.</p><p>This is done because we want to leverage the relational database&#39;s ACID guarantees to ensure consistency.</p><p>However, the interviewer might challenge this approach as it&#39;s not a pure microservice architecture, where each service has a dedicated database:</p><figure><img src="`+x+'" alt="microservices-vs-monolith" tabindex="0" loading="lazy"><figcaption>microservices-vs-monolith</figcaption></figure><p>This can lead to consistency issues. In a monolithic server, we can leverage a relational DBs transaction capabilities to implement atomic operations:</p><figure><img src="'+D+'" alt="atomicity-monolith" tabindex="0" loading="lazy"><figcaption>atomicity-monolith</figcaption></figure><p>It&#39;s more challenging, however, to guarantee this atomicity when the operation spans across multiple services:</p><figure><img src="'+I+'" alt="microservice-non-atomic-operation" tabindex="0" loading="lazy"><figcaption>microservice-non-atomic-operation</figcaption></figure><p>There are some well-known techniques to handle these data inconsistencies:</p><ul><li>Two-phase commit - a database protocol which guarantees atomic transaction commit across multiple nodes. It&#39;s not performant, though, since a single node lag leads to all nodes blocking the operation.</li><li>Saga - a sequence of local transactions, where compensating transactions are triggered if any of the steps in a workflow fail. This is an eventually consistent approach.</li></ul><p>It&#39;s worth noting that addressing data inconsistencies across microservices is a challenging problem, which raise the system complexity. It is good to consider whether the cost is worth it, given our more pragmatic approach of encapsulating dependent operations within the same relational database.</p><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap Up</h2><p>We presented a design for a hotel reservation system.</p><p>These are the steps we went through:</p><ul><li>Gathering requirements and doing back-of-the-envelope calculations to understand the system&#39;s scale</li><li>We presented the API Design, Data Model and system architecture in the high-level design</li><li>In the deep dive, we explored alternative database schema designs as requirements changed</li><li>We discussed race conditions and proposed solutions - pessimistic/optimistic locking, database constraints</li><li>Ways to scale the system via database sharding and caching</li><li>Finally we addressed how to handle data consistency issues across multiple microservices</li></ul>',114);function R(C,O){const a=r("RouterLink");return l(),d("div",null,[A,t("p",null,[e("Note that the"),P,e("is an idempotency key to avoid double booking. Details explained in"),s(a,{to:"/system-design/42_hotel_reservation_system.html"},{default:o(()=>[e("concurrency section")]),_:1})]),S,t("p",null,[e("This shortcoming will be addressed in the"),s(a,{to:"/system-design/42_hotel_reservation_system.html"},{default:o(()=>[e("Improved Data Model")]),_:1}),e("section.")]),E])}const H=n(T,[["render",R],["__file","42_hotel_reservation_system.html.vue"]]);export{H as default};
