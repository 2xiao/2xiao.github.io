import{_ as e,o as a,c as i,a as s}from"./app-53EDpe6z.js";const t="/assets/system-design-367-P-sRbWKS.png",r="/assets/system-design-368-q07s7iT-.png",n="/assets/system-design-369-CnKOkNRg.png",o="/assets/system-design-370-XVp86eUv.png",l="/assets/system-design-371-KbqgppQ9.png",d="/assets/system-design-372-cySvwj2r.png",c="/assets/system-design-373-2LtE2ciZ.png",u="/assets/system-design-374-SZbxU-4U.png",p="/assets/system-design-375-PmKYzRKq.png",h="/assets/system-design-376-XLUdTAUD.png",g="/assets/system-design-377-HzO4B0y_.png",m="/assets/system-design-378-5kpHQg6V.png",b="/assets/system-design-379-T12Q1i0v.png",v="/assets/system-design-380-MV_ap5g0.png",f="/assets/system-design-381-UwyVuV9h.png",y="/assets/system-design-382-DaB6493L.png",w="/assets/system-design-383-DBl0vW41.png",x="/assets/system-design-384-7rorKuqz.png",_="/assets/system-design-385-EcwILvkL.png",q="/assets/system-design-386-satC3HTT.png",k="/assets/system-design-387-juI5espH.png",S="/assets/system-design-388-Ua2ItbAJ.png",A="/assets/system-design-389-89RAW2VS.png",I={},R=s('<h1 id="real-time-gaming-leaderboard" tabindex="-1"><a class="header-anchor" href="#real-time-gaming-leaderboard" aria-hidden="true">#</a> Real-time Gaming Leaderboard</h1><p>We are going to design a leaderboard for an online mobile game:</p><figure><img src="'+t+`" alt="leaderboard" tabindex="0" loading="lazy"><figcaption>leaderboard</figcaption></figure><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><ul><li>C: How is the score calculated for the leaderboard?</li><li>I: User gets a point whenever they win a match.</li><li>C: Are all players included in the leaderboard?</li><li>I: Yes</li><li>C: Is there a time segment, associated with the leaderboard?</li><li>I: Each month, a new tournament starts which starts a new leaderboard.</li><li>C: Can we assume we only care about top 10 users?</li><li>I: We want to display top 10 users, along with position of specific user. If time permits, we can discuss showing users around particular user in the leaderboard.</li><li>C: How many users are in a tournament?</li><li>I: 5mil DAU and 25mil MAU</li><li>C: How many matches are played on average during a tournament?</li><li>I: Each player plays 10 matches per day on average</li><li>C: How do we determine the rank if two players have the same score?</li><li>I: Their rank is the same in that case. If time permits, we can discuss breaking ties.</li><li>C: Does the leaderboard need to be real-time?</li><li>I: Yes, we want to present real-time results or as close as possible to real-time. It is not okay to present batched result history.</li></ul><h3 id="functional-requirements" tabindex="-1"><a class="header-anchor" href="#functional-requirements" aria-hidden="true">#</a> Functional requirements</h3><ul><li>Display top 10 players on leaderboard</li><li>Show a user&#39;s specific rank</li><li>Display users which are four places above and below given user (bonus)</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>Real-time updates on scores</li><li>Score update is reflected on the leaderboard in real-time</li><li>General scalability, availability, reliability</li></ul><h3 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back-of-the-envelope estimation</h3><p>With 50mil DAU, if the game has an even distribution of players during a 24h period, we&#39;d have an average of 50 users per second. However, since distribution is typically uneven, we can estimate that the peak online users would be 250 users per second.</p><p>QPS for users scoring a point - given 10 games per day on average, 50 users/s * 10 = 500 QPS. Peak QPS = 2500.</p><p>QPS for fetching the top 10 leaderboard - assuming users open that once a day on average, QPS is 50.</p><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><h3 id="api-design" tabindex="-1"><a class="header-anchor" href="#api-design" aria-hidden="true">#</a> API Design</h3><p>The first API we need is one to update a user&#39;s score:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>POST /v1/scores
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This API takes two params -<code>user_id</code>and<code>points</code>scored for winning a game.</p><p>This API should only be accessible to game servers, not end clients.</p><p>Next one is for getting the top 10 players of the leaderboard:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>GET /v1/scores
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Example response:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
  &quot;data&quot;: [
    {
      &quot;user_id&quot;: &quot;user_id1&quot;,
      &quot;user_name&quot;: &quot;alice&quot;,
      &quot;rank&quot;: 1,
      &quot;score&quot;: 12543
    },
    {
      &quot;user_id&quot;: &quot;user_id2&quot;,
      &quot;user_name&quot;: &quot;bob&quot;,
      &quot;rank&quot;: 2,
      &quot;score&quot;: 11500
    }
  ],
  ...
  &quot;total&quot;: 10
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>You can also get the score of a particular user:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>GET /v1/scores/{:user_id}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Example response:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
    &quot;user_info&quot;: {
        &quot;user_id&quot;: &quot;user5&quot;,
        &quot;score&quot;: 1000,
        &quot;rank&quot;: 6,
    }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="high-level-architecture" tabindex="-1"><a class="header-anchor" href="#high-level-architecture" aria-hidden="true">#</a> High-level architecture</h3><figure><img src="`+r+'" alt="high-level-architecture" tabindex="0" loading="lazy"><figcaption>high-level-architecture</figcaption></figure><ul><li>When a player wins a game, client sends a request to the game service</li><li>Game service validates if win is valid and calls the leaderboard service to update the player&#39;s score</li><li>Leaderboard service updates the user&#39;s score in the leaderboard store</li><li>Player makes a call to leaderboard service to fetch leaderboard data, eg top 10 players and given player&#39;s rank</li></ul><p>An alternative design which was considered is the client updating their score directly within the leaderboard service:</p><figure><img src="'+n+'" alt="alternative-design" tabindex="0" loading="lazy"><figcaption>alternative-design</figcaption></figure><p>This option is not secure as it&#39;s susceptible to man-in-the-middle attacks. Players can put a proxy and change their score as they please.</p><p>One additional caveat is that for games, where the game logic is managed by the server, cliets don&#39;t need to call the server explicitly to record their win. Servers do it automatically for them based on the game logic.</p><p>One additional consideration is whether we should put a message queue between the game server and the leaderboard service. This would be useful if other services are interested in game results, but that is not an explicit requirement in the interview so far, hence it&#39;s not included in the design:</p><figure><img src="'+o+'" alt="message-queue-based-comm" tabindex="0" loading="lazy"><figcaption>message-queue-based-comm</figcaption></figure><h3 id="data-models" tabindex="-1"><a class="header-anchor" href="#data-models" aria-hidden="true">#</a> Data models</h3><p>Let&#39;s discuss the options we have for storing leaderboard data - relational DBs, Redis, NoSQL.</p><p>The NoSQL solution is discussed in the deep dive section.</p><h3 id="relational-database-solution" tabindex="-1"><a class="header-anchor" href="#relational-database-solution" aria-hidden="true">#</a> Relational database solution</h3><p>If the scale doesn&#39;t matter and we don&#39;t have that many users, a relational DB serves our quite well.</p><p>We can start from a simple leaderboard table, one for each month (personal note - this doesn&#39;t make sense. You can just add a<code>month</code>column and avoid the headache of maintaining new tables each month):</p><figure><img src="'+l+'" alt="leaderboard-table" tabindex="0" loading="lazy"><figcaption>leaderboard-table</figcaption></figure><p>There is additional data to include in there, but that is irrelevant to the queries we&#39;d run, so it&#39;s omitted.</p><p>What happens when a user wins a point?</p><figure><img src="'+d+`" alt="user-wins-point" tabindex="0" loading="lazy"><figcaption>user-wins-point</figcaption></figure><p>If a user doesn&#39;t exist in the table yet, we need to insert them first:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>INSERT INTO leaderboard (user_id, score) VALUES (&#39;mary1934&#39;, 1);
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>On subsequent calls, we&#39;d just update their score:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>UPDATE leaderboard set score=score + 1 where user_id=&#39;mary1934&#39;;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>How do we find the top players of a leaderboard?</p><figure><img src="`+c+`" alt="find-leaderboard-position" tabindex="0" loading="lazy"><figcaption>find-leaderboard-position</figcaption></figure><p>We can run the following query:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>SELECT (@rownum := @rownum + 1) AS rank, user_id, score
FROM leaderboard
ORDER BY score DESC;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This is not performant though as it makes a table scan to order all records in the database table.</p><p>We can optimize it by adding an index on<code>score</code>and using the<code>LIMIT</code>operation to avoid scanning everything:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>SELECT (@rownum := @rownum + 1) AS rank, user_id, score
FROM leaderboard
ORDER BY score DESC
LIMIT 10;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This approach, however, doesn&#39;t scale well if the user is not at the top of the leaderboard and you&#39;d want to locate their rank.</p><h3 id="redis-solution" tabindex="-1"><a class="header-anchor" href="#redis-solution" aria-hidden="true">#</a> Redis solution</h3><p>We want to find a solution, which works well even for millions of players without having to fallback on complex database queries.</p><p>Redis is an in-memory data store, which is fast as it works in-memory and has a suitable data structure to serve our needs - sorted set.</p><p>A sorted set is a data structure similar to sets in programming languages, which allows you to keep a data structure sorted by a given criteria. Internally, it is implemented using a hash-map to maintain mapping between key (user_id) and value (score) and a skip list which maps scores to users in sorted order:</p><figure><img src="`+u+'" alt="sorted-set" tabindex="0" loading="lazy"><figcaption>sorted-set</figcaption></figure><p>How does a skip list work?</p><ul><li>It is a linked list which allows for fast search</li><li>It consists of a sorted linked list and multi-level indexes</li></ul><figure><img src="'+p+'" alt="skip-list" tabindex="0" loading="lazy"><figcaption>skip-list</figcaption></figure><p>This structure enables us to quickly search for specific values when the data set is large enough. In the example below (64 nodes), it requires traversing 62 nodes in a base linked list to find the given value and 11 nodes in the skip-list case:</p><figure><img src="'+h+`" alt="skip-list-performance" tabindex="0" loading="lazy"><figcaption>skip-list-performance</figcaption></figure><p>Sorted sets are more performant than relational databases as the data is kept sorted at all times at the price of O(logN) add and find operation.</p><p>In contract, here&#39;s an example nested query we need to run to find the rank of a given user in a relational DB:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>SELECT *,(SELECT COUNT(*) FROM leaderboard lb2
WHERE lb2.score &gt;= lb1.score) RANK
FROM leaderboard lb1
WHERE lb1.user_id = {:user_id};
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>What operations do we need to operate our leaderboard in Redis?</p><ul><li><code>ZADD</code>- insert the user into the set if they don&#39;t exist. Otherwise, update the score. O(logN) time complexity.</li><li><code>ZINCRBY</code>- increment the score of a user by given amount. If user doesn&#39;t exist, score starts at zero. O(logN) time complexity.</li><li><code>ZRANGE/ZREVRANGE</code>- fetch a range of users, sorted by their score. We can specify order (ASC/DESC), offset and result size. O(logN+M) time complexity where M is result size.</li><li><code>ZRANK/ZREVRANK</code>- Fetch the position (rank) of given user in ASC/DESC order. O(logN) time complexity.</li></ul><p>What happens when a user scores a point?</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>ZINCRBY leaderboard_feb_2021 1 &#39;mary1934&#39;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>There&#39;s a new leaderboard created every month while old ones are moved to historical storage.</p><p>What happens when a user fetches top 10 players?</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>ZREVRANGE leaderboard_feb_2021 0 9 WITHSCORES
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Example result:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>[(user2,score2),(user1,score1),(user5,score5)...]
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>What about user fetching their leaderboard position?</p><figure><img src="`+g+`" alt="leaderboard-position-of-user" tabindex="0" loading="lazy"><figcaption>leaderboard-position-of-user</figcaption></figure><p>This can be easily achieved by the following query, given that we know a user&#39;s leaderboard position:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>ZREVRANGE leaderboard_feb_2021 357 365
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>A user&#39;s position can be fetched using<code>ZREVRANK &lt;user-id&gt;</code>.</p><p>Let&#39;s explore what our storage requirements are:</p><ul><li>Assuming worst-case scenario of all 25mil MAU participating in the game for a given month</li><li>ID is 24-character string and score is 16-bit integer, we need 26 bytes * 25mil = ~650MB of storage</li><li>Even if we double the storage cost due to the overhead of the skip list, this would still easily fit in a modern redis cluster</li></ul><p>Another non-functional requirement to consider is supporting 2500 updates per second. This is well within a single Redis server&#39;s capabilities.</p><p>Additional caveats:</p><ul><li>We can spin up a Redis replica to avoid losing data when a redis server crashes</li><li>We can still leverage Redis persistence to not lose data in the event of a crash</li><li>We&#39;ll need two supporting tables in MySQL to fetch user details such as username, display name, etc as well as store when eg a user won a game</li><li>The second table in MySQL can be used to reconstruct leaderboard when there is an infrastructure failure</li><li>As a small performance optimization, we could cache the user details of top 10 players as they&#39;d be frequently accessed</li></ul><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><h3 id="to-use-a-cloud-provider-or-not" tabindex="-1"><a class="header-anchor" href="#to-use-a-cloud-provider-or-not" aria-hidden="true">#</a> To use a cloud provider or not</h3><p>We can either choose to deploy and manage our own services or use a cloud provider to manage them for us.</p><p>If we choose to manage the services our selves, we&#39;ll use redis for leaderboard data, mysql for user profile and potentially a cache for user profile if we want to scale the database:</p><figure><img src="`+m+'" alt="manage-services-ourselves" tabindex="0" loading="lazy"><figcaption>manage-services-ourselves</figcaption></figure><p>Alternatively, we could use cloud offerings to manage a lot of the services for us. For example, we can use AWS API Gateway to route API calls to AWS Lambda functions:</p><figure><img src="'+b+'" alt="api-gateway-mapping" tabindex="0" loading="lazy"><figcaption>api-gateway-mapping</figcaption></figure><p>AWS Lambda enables us to run code without managing or provisioning servers ourselves. It runs only when needed and scales automatically.</p><p>Exmaple user scoring a point:</p><figure><img src="'+v+'" alt="user-scoring-point-lambda" tabindex="0" loading="lazy"><figcaption>user-scoring-point-lambda</figcaption></figure><p>Example user retrieving leaderboard:</p><figure><img src="'+f+'" alt="user-retrieve-leaderboard" tabindex="0" loading="lazy"><figcaption>user-retrieve-leaderboard</figcaption></figure><p>Lambdas are an implementation of a serverless architecture. We don&#39;t need to manage scaling and environment setup.</p><p>Author recommends going with this approach if we build the game from the ground up.</p><h3 id="scaling-redis" tabindex="-1"><a class="header-anchor" href="#scaling-redis" aria-hidden="true">#</a> Scaling Redis</h3><p>With 5mil DAU, we can get away with a single Redis instance from both a storage and QPS perspective.</p><p>However, if we imagine userbase grows 10x to 500mil DAU, then we&#39;d need 65gb for storage and QPS goes to 250k.</p><p>Such scale would require sharding.</p><p>One way to achieve it is by range-partitioning the data:</p><figure><img src="'+y+'" alt="range-partition" tabindex="0" loading="lazy"><figcaption>range-partition</figcaption></figure><p>In this example, we&#39;ll shard based on user&#39;s score. We&#39;ll maintain the mapping between user_id and shard in application code. We can do that either via MySQL or another cache for the mapping itself.</p><p>To fetch the top 10 players, we&#39;d query the shard with the highest scores (<code>[900-1000]</code>).</p><p>To fetch a user&#39;s rank, we&#39;ll need to calculate the rank within the user&#39;s shard and add up all users with higher scores in other shards. The latter is a O(1) operation as total records per shard can quickly be accessed via the info keyspace command.</p><p>Alternatively, we can use hash partitioning via Redis Cluster. It is a proxy which distributes data across redis nodes based on partitioning similar to consistent hashing, but not exactly the same:</p><figure><img src="'+w+'" alt="hash-partition" tabindex="0" loading="lazy"><figcaption>hash-partition</figcaption></figure><p>Calculating the top 10 players is challenging with this setup. We&#39;ll need to get the top 10 players of each shard and merge the results in the application:</p><figure><img src="'+x+'" alt="top-10-players-calculation" tabindex="0" loading="lazy"><figcaption>top-10-players-calculation</figcaption></figure><p>There are some limitations with the hash partitioning:</p><ul><li>If we need to fetch top K users, where K is high, latency can increase as we&#39;ll need to fetch a lot of data from all the shards</li><li>Latency increases as the number of partitions grows</li><li>There is no straightforward approach to determine a user&#39;s rank</li></ul><p>Due to all this, the author leans towards using fixed partitions for this problem.</p><p>Other caveats:</p><ul><li>A best practice is to allocate twice as much memory as required for write-heavy redis nodes to accommodate snapshots if required</li><li>We can use a tool called Redis-benchmark to track the performance of a redis setup and make data-driven decisions</li></ul><h3 id="alternative-solution-nosql" tabindex="-1"><a class="header-anchor" href="#alternative-solution-nosql" aria-hidden="true">#</a> Alternative solution: NoSQL</h3><p>An alternative solution to consider is using an appropriate NoSQL database optimized for:</p><ul><li>heavy writes</li><li>effectively sorting items within the same partition by score</li></ul><p>DynamoDB, Cassandra or MongoDB are all good fits.</p><p>In this chapter, the author has decided to use DynamoDB. It is a fully-managed NoSQL database, which offers reliable performance and great scalability. It also enables usage of global secondary indexes when we need to query fields not part of the primary key.</p><figure><img src="'+_+'" alt="dynamo-db" tabindex="0" loading="lazy"><figcaption>dynamo-db</figcaption></figure><p>Let&#39;s start from a table for storing a leaderboard for a chess game:</p><figure><img src="'+q+'" alt="chess-game-leaderboard-table-1" tabindex="0" loading="lazy"><figcaption>chess-game-leaderboard-table-1</figcaption></figure><p>This works well, but doesn&#39;t scale well if we need to query anything by score. Hence, we can put the score as a sort key:</p><figure><img src="'+k+'" alt="chess-game-leaderboard-table-2" tabindex="0" loading="lazy"><figcaption>chess-game-leaderboard-table-2</figcaption></figure><p>Another problem with this design is that we&#39;re partitioning by month. This leads to a hotspot partition as the latest month will be unevenly accessed compared to the others.</p><p>We could use a technique called write sharding, where we append a partition number for each key, calculated via<code>user_id % num_partitions</code>:</p><figure><img src="'+S+'" alt="chess-game-leaderboard-table-3" tabindex="0" loading="lazy"><figcaption>chess-game-leaderboard-table-3</figcaption></figure><p>An important trade-off to consider is how many partitions we should use:</p><ul><li>The more partitions there are, the higher the write scalability</li><li>However, read scalability suffers as we need to query more partitions to collect aggregate results</li></ul><p>Using this approach requires that we use the &quot;scatter-gather&quot; technique we saw earlier, which grows in time complexity as we add more partitions:</p><figure><img src="'+A+`" alt="scatter-gather-2" tabindex="0" loading="lazy"><figcaption>scatter-gather-2</figcaption></figure><p>To make a good evaluation on the number of partitions, we&#39;d need to do some benchmarking.</p><p>This NoSQL approach still has one major downside - it is hard to calculate the specific rank of a user.</p><p>If we have sufficient scale to require us to shard, we could then perhaps tell users what &quot;percentile&quot; of scores they&#39;re in.</p><p>A cron job can periodically run to analyze score distributions, based on which a user&#39;s percentile is determined, eg:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>10th percentile = score &lt; 100
20th percentile = score &lt; 500
...
90th percentile = score &lt; 6500
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap Up</h2><p>Other things to discuss if time permits:</p><ul><li>Faster retrieval - We can cache the user object via a Redis hash with mapping<code>user_id -&gt; user object</code>. This enables faster retrieval vs. querying the database.</li><li>Breaking ties - When two players have the same score, we can break the tie by sorting them based on last played game.</li><li>System failure recovery - In the event of a large-scale Redis outage, we can recreate the leaderboard by going through the MySQL WAL entries and recreate it via an ad-hoc script</li></ul>`,147),T=[R];function E(W,D){return a(),i("div",null,T)}const L=e(I,[["render",E],["__file","45_real_time_gaming_leaderboard.html.vue"]]);export{L as default};
