import{_ as e,o as i,c as s,a}from"./app-rLUZugrR.js";const t="/assets/system-design-175-QQHpFxss.png",n="/assets/system-design-176-dzvKzKg1.png",r="/assets/system-design-177-5bltUbSI.png",o="/assets/system-design-178-wu-V8c5G.png",d="/assets/system-design-179-g-UceKzx.png",l="/assets/system-design-180-_0caJERw.png",h="/assets/system-design-181-BE38A4-y.png",c="/assets/system-design-182-P3wduhgZ.png",u="/assets/system-design-183--_xOZ_I3.png",p="/assets/system-design-184-rGDPqMC7.png",g="/assets/system-design-185-clYjb8NV.png",b="/assets/system-design-186-Ns-aCE91.png",m="/assets/system-design-187-r3CyRETY.png",f="/assets/system-design-188-aJjP2Qnk.png",v="/assets/system-design-189-EXkilivZ.png",y="/assets/system-design-190-iz_gMMYN.png",w="/assets/system-design-191-ujIT5ZdC.png",x="/assets/system-design-192-p5tHcQsZ.png",_="/assets/system-design-193-Km11ePrD.png",q="/assets/system-design-194-0W0L4MO8.png",S="/assets/system-design-195-kXBe5AKZ.png",k={},I=a(`<h1 id="proximity-service" tabindex="-1"><a class="header-anchor" href="#proximity-service" aria-hidden="true">#</a> Proximity Service</h1><p>A proximity service enables you to discover nearby places such as restaurants, hotels, theatres, etc.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the problem and establish design scope</h2><p>Sample questions to understand the problem better:</p><ul><li>C: Can a user specify a search radius? What if there are not enough businesses within the search area?</li><li>I: We only care about businesses within a certain area. If time permits, we can discuss enhancing the functionality.</li><li>C: What&#39;s the max radius allowed? Can I assume it&#39;s 20km?</li><li>I: Yes, that is a reasonable assumption</li><li>C: Can a user change the search radius via the UI?</li><li>I: Yes, let&#39;s say we have the options - 0.5km, 1km, 2km, 5km, 20km</li><li>C: How is business information modified? Do we need to reflect changes in real-time?</li><li>I: Business owners can add/delete/update a business. Assume changes are going to be propagated on the next day.</li><li>C: How do we handle search results while the user is moving?</li><li>I: Let&#39;s assume we don&#39;t need to constantly update the page since users are moving slowly.</li></ul><h3 id="functional-requirements" tabindex="-1"><a class="header-anchor" href="#functional-requirements" aria-hidden="true">#</a> Functional requirements</h3><ul><li>Return all businesses based on user&#39;s location</li><li>Business owners can add/delete/update a business. Information is not reflected in real-time.</li><li>Customers can view detailed information about a business</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>Low latency - users should be able to see nearby businesses quickly</li><li>Data privacy - Location info is sensitive data and we should take this into consideration in order to comply with regulations</li><li>High availability and scalability requirements - We should ensure system can handle spike in traffic during peak hours in densely populated areas</li></ul><h3 id="back-of-the-envelope-calculation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-calculation" aria-hidden="true">#</a> Back-of-the-envelope calculation</h3><ul><li>Assuming 100mil daily active users and 200mil businesses</li><li>Search QPS == 100mil * 5 (average searches per day) / 10^5 (seconds in day) == 5000</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and get Buy-In</h2><h3 id="api-design" tabindex="-1"><a class="header-anchor" href="#api-design" aria-hidden="true">#</a> API Design</h3><p>We&#39;ll use a RESTful API convention to design a simplified version of the APIs.</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>GET /v1/search/nearby
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This endpoint returns businesses based on search criteria, paginated.</p><p>Request parameters - latitude, longitude, radius</p><p>Example response:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
  &quot;total&quot;: 10,
  &quot;businesses&quot;:[{business object}]
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The endpoint returns everything required to render a search results page, but a user might require additional details about a particular business, fetched via other endpoints.</p><p>Here&#39;s some other business APIs we&#39;ll need:</p><ul><li><code>GET /v1/businesses/{:id}</code>- return business detailed info</li><li><code>POST /v1/businesses</code>- create a new business</li><li><code>PUT /v1/businesses/{:id}</code>- update business details</li><li><code>DELETE /v1/businesses/{:id}</code>- delete a business</li></ul><h3 id="data-model" tabindex="-1"><a class="header-anchor" href="#data-model" aria-hidden="true">#</a> Data model</h3><p>In this problem, the read volume is high because these features are commonly used:</p><ul><li>Search for nearby businesses</li><li>View the detailed information of a business</li></ul><p>On the other hand, write volume is low because we rarely change business information. Hence for a read-heavy workflow, a relational database such as MySQL is ideal.</p><p>In terms of schema, we&#39;ll need one main<code>business</code>table which holds information about a business:</p><figure><img src="`+t+'" alt="business-table" tabindex="0" loading="lazy"><figcaption>business-table</figcaption></figure><p>We&#39;ll also need a geo-index table so that we efficiently process spatial operations. This table will be discussed later when we introduce the concept of geohashes.</p><h3 id="high-level-design" tabindex="-1"><a class="header-anchor" href="#high-level-design" aria-hidden="true">#</a> High-level design</h3><p>Here&#39;s a high-level overview of the system:</p><figure><img src="'+n+'" alt="high-level-design" tabindex="0" loading="lazy"><figcaption>high-level-design</figcaption></figure><ul><li>The load balancer automatically distributes incoming traffic across multiple services. A company typically provides a single DNS entry point and internally routes API calls to appropriate services based on URL paths.</li><li>Location-based service (LBS) - read-heavy, stateless service, responsible for serving read requests for nearby businesses</li><li>Business service - supports CRUD operations on businesses.</li><li>Database cluster - stores business information and replicates it in order to scale reads. This leads to some inconsistency for LBS to read business information, which is not an issue for our use-case</li><li>Scalability of business service and LBS - since both services are stateless, we can easily scale them horizontally</li></ul><h3 id="algorithms-to-fetch-nearby-businesses" tabindex="-1"><a class="header-anchor" href="#algorithms-to-fetch-nearby-businesses" aria-hidden="true">#</a> Algorithms to fetch nearby businesses</h3><p>In real life, one might use a geospatial database, such as Geohash in Redis or Postgres with PostGIS extension.</p><p>Let&#39;s explore how these databases work and what other alternative algorithms there are for this type of problem.</p><h3 id="two-dimensional-search" tabindex="-1"><a class="header-anchor" href="#two-dimensional-search" aria-hidden="true">#</a> Two-dimensional search</h3><p>The most intuitive and naive approach to solving this problem is to draw a circle around the person and fetch all businesses within the circle&#39;s radius:</p><figure><img src="'+r+`" alt="2d-search" tabindex="0" loading="lazy"><figcaption>2d-search</figcaption></figure><p>This can easily be translated to a SQL query:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>SELECT business_id, latitude, longitude,
FROM business
WHERE (latitude BETWEEN {:my_lat} - radius AND {:my_lat} + radius) AND
      (longitude BETWEEN {:my_long} - radius AND {:my_long} + radius)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This query is not efficient because we need to query the whole table. An alternative is to build an index on the longitude and latitude columns but that won&#39;t improve performance by much.</p><p>This is because we still need to subsequently filter a lot of data regardless of whether we index by long or lat:</p><figure><img src="`+o+'" alt="2d-query-problem" tabindex="0" loading="lazy"><figcaption>2d-query-problem</figcaption></figure><p>We can, however, build 2D indexes and there are different approaches to that:</p><figure><img src="'+d+'" alt="2d-index-options" tabindex="0" loading="lazy"><figcaption>2d-index-options</figcaption></figure><p>We&#39;ll discuss the ones highlighted in purple - geohash, quadtree and google S2 are the most popular approaches.</p><h3 id="evenly-divided-grid" tabindex="-1"><a class="header-anchor" href="#evenly-divided-grid" aria-hidden="true">#</a> Evenly divided grid</h3><p>Another option is to divide the world in small grids:</p><figure><img src="'+l+'" alt="evenly-divided-grid" tabindex="0" loading="lazy"><figcaption>evenly-divided-grid</figcaption></figure><p>The major flaw with this approach is that business distribution is uneven as there are a lot of businesses concentrated in new york and close to zero in the sahara desert.</p><h3 id="geohash" tabindex="-1"><a class="header-anchor" href="#geohash" aria-hidden="true">#</a> Geohash</h3><p>Geohash works similarly to the previous approach, but it recursively divides the world into smaller and smaller grids, where each two bits correspond to a single quadrant:</p><figure><img src="'+h+`" alt="geohash-example" tabindex="0" loading="lazy"><figcaption>geohash-example</figcaption></figure><p>Geohashes are typically represented in base32. Here&#39;s the example geohash of google headquarters:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>1001 10110 01001 10000 11011 11010 (base32 in binary) → 9q9hvu (base32)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>It supports 12 levels of precision, but we only need up to 6 levels for our use-case:</p><figure><img src="`+c+'" alt="geohash-precision" tabindex="0" loading="lazy"><figcaption>geohash-precision</figcaption></figure><p>Geohashes enable us to quickly locate neighboring regions based on a substring of the geohash:</p><figure><img src="'+u+'" alt="geohash-substring" tabindex="0" loading="lazy"><figcaption>geohash-substring</figcaption></figure><p>However, one issue \\w geohashes is that there can be places which are very close to each other which don&#39;t share any prefix, because they&#39;re on different sides of the equator or meridian:</p><figure><img src="'+p+'" alt="boundary-issue-geohash" tabindex="0" loading="lazy"><figcaption>boundary-issue-geohash</figcaption></figure><p>Another issue is that two businesses can be very close but not share a common prefix because they&#39;re in different quadrants:</p><figure><img src="'+g+'" alt="geohash-boundary-issue-2" tabindex="0" loading="lazy"><figcaption>geohash-boundary-issue-2</figcaption></figure><p>This can be mitigated by fetching neighboring geohashes, not just the geohash of the user.</p><p>A benefit of using geohashes is that we can use them to easily implement the bonus problem of increasing search radius in case insufficient businesses are fetched via query:</p><figure><img src="'+b+'" alt="geohash-expansion" tabindex="0" loading="lazy"><figcaption>geohash-expansion</figcaption></figure><p>This can be done by removing the last letter of the target geohash to increase radius.</p><h3 id="quadtree" tabindex="-1"><a class="header-anchor" href="#quadtree" aria-hidden="true">#</a> Quadtree</h3><p>A quadtree is a data structure, which recursively subdivides quadrants as deep as it needs to, based on business needs:</p><figure><img src="'+m+'" alt="quadtree-example" tabindex="0" loading="lazy"><figcaption>quadtree-example</figcaption></figure><p>This is an in-memory solution which can&#39;t easily be implemented in a database.</p><p>Here&#39;s how it might look conceptually:</p><figure><img src="'+f+`" alt="quadtree-concept" tabindex="0" loading="lazy"><figcaption>quadtree-concept</figcaption></figure><p>Example pseudocode to build a quadtree:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>public void buildQuadtree(TreeNode node) {
    if (countNumberOfBusinessesInCurrentGrid(node) &gt; 100) {
        node.subdivide();
        for (TreeNode child : node.getChildren()) {
            buildQuadtree(child);
        }
    }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In a leaf node, we store:</p><ul><li>Top-left, bottom-right coordinates to identify the quadrant dimensions</li><li>List of business IDs in the grid</li></ul><p>In an internalt node we store:</p><ul><li>Top-left, bottom-right coordinates of quadrant dimensions</li><li>4 pointers to children</li></ul><p>The total memory to represent the quadtree is calculated as ~1.7GB in the book if we assume that we operate with 200mil businesses.</p><p>Hence, a quadtree can be stored in a single server, in-memory, although we can of course replicate it for redundancy and load balancing purposes.</p><p>One consideration to take into consideration if this approach is adopted - startup time of server can be a couple of minutes while the quadtree is being built.</p><p>Hence, this should be taken into account during the deployment process. Eg a healthcheck endpoint can be exposed and queried to signal when the quadtree build is finished.</p><p>Another consideration is how to update the quadtree. Given our requirements, a good option would be to update it every night using a nightly job due to our commitment of reflecting changes at start of next day.</p><p>It is nevertheless possible to update the quadtree on the fly, but that would complicate the implementation significantly.</p><p>Example quadtree of Denver:</p><figure><img src="`+v+'" alt="denver-quadtree" tabindex="0" loading="lazy"><figcaption>denver-quadtree</figcaption></figure><h3 id="google-s2" tabindex="-1"><a class="header-anchor" href="#google-s2" aria-hidden="true">#</a> Google S2</h3><p>Google S2 is a geometry library, which supports mapping 2D points on a 1D plane using hilbert curves. Objects close to each other on the 2D plane are close on the hilbert curve as well:</p><figure><img src="'+y+'" alt="hilbert-curve" tabindex="0" loading="lazy"><figcaption>hilbert-curve</figcaption></figure><p>This library is great for geofencing, which supports covering arbitrary areas vs. confining yourself to specific quadrants.</p><figure><img src="'+w+'" alt="geofence-example" tabindex="0" loading="lazy"><figcaption>geofence-example</figcaption></figure><p>This functionality can be used to support more advanced use-cases than nearby businesses.</p><p>Another benefit of Google S2 is its region cover algorithm, which enables us to define more granular precision levels, than those provided by geohashes.</p><h3 id="recommendation" tabindex="-1"><a class="header-anchor" href="#recommendation" aria-hidden="true">#</a> Recommendation</h3><p>There is no perfect solution, different companies adopt different solutions:</p><figure><img src="'+x+'" alt="company-adoptions" tabindex="0" loading="lazy"><figcaption>company-adoptions</figcaption></figure><p>Author suggest choosing geohashes or quadtree in an interview as those are easier to explain than Google S2.</p><p>Here&#39;s a quick summary of geohashes:</p><ul><li>Easy to use and implement, no need to build a tree</li><li>supports returning businesses within a specified radius</li><li>Geohash precision is fixed. More complex logic is required if a more granular precision is needed</li><li>Updating the index is easy</li></ul><p>Here&#39;s a quick summary of quadtrees:</p><ul><li>Slightly harder to implement as it requires us to build a tree</li><li>Supports fetching k-nearest neighbors instead of businesses within radius, which can be a good use-case for certain features</li><li>Grid size can be dynamically adjusted based on population density</li><li>Updating the index is more complicated than updating the geohash variant. All problems with updating and balancing trees are present when working with quad trees.</li></ul><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>Let&#39;s dive deeper into some areas of the design.</p><h3 id="scale-the-database" tabindex="-1"><a class="header-anchor" href="#scale-the-database" aria-hidden="true">#</a> Scale the database</h3><p>The business table can be scaled by sharding it in case it doesn&#39;t fit in a single server instance.</p><p>The geohash table can be represented by two columns:</p><figure><img src="'+_+`" alt="geohash-table-example" tabindex="0" loading="lazy"><figcaption>geohash-table-example</figcaption></figure><p>We don&#39;t need to shard the geohash table as we don&#39;t have that much data. We calculated that it takes ~1.7gb to build a quad tree and geohash space usage is similar.</p><p>We can, however, replicate the table to scale the read load.</p><h3 id="caching" tabindex="-1"><a class="header-anchor" href="#caching" aria-hidden="true">#</a> Caching</h3><p>Before using caching, we should ask ourselves if it is really necessary. In our case, the workflow is read-heavy and data can fit into a single server, so this kind of data is ripe for caching.</p><p>We should be careful when choosing the cache key. Location coordinates are not a good cache key as they often change and can be inaccurate.</p><p>Using the geohash is a more suitable key candidate.</p><p>Here&#39;s how we could query all businesses in a geohash:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>SELECT business_id FROM geohash_index WHERE geohash LIKE \`{:geohash}%\`
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Here&#39;s example code to cache the data in redis:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>public List&lt;String&gt; getNearbyBusinessIds(String geohash) {
    String cacheKey = hash(geohash);
    List&lt;string&gt; listOfBusinessIds = Redis.get(cacheKey);
    if (listOfBusinessIDs  == null) {
        listOfBusinessIds = Run the select SQL query above;
        Cache.set(cacheKey, listOfBusinessIds, &quot;1d&quot;);
    }
    return listOfBusinessIds;
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>We can cache the data on all precisions we support, which are not a lot, ie<code>geohash_4, geohash_5, geohash_6</code>.</p><p>As we already discussed, the storage requirements are not high and can fit into a single redis server, but we could replicate it for redundancy purposes as well as to scale reads.</p><p>We can even deploy multiple redis replicas across different data centers.</p><p>We could also cache<code>business_id -&gt; business_data</code>as users could often query the details of the same popular restaurant.</p><h3 id="region-and-availability-zones" tabindex="-1"><a class="header-anchor" href="#region-and-availability-zones" aria-hidden="true">#</a> Region and availability zones</h3><p>We can deploy multiple LBS service instances across the globe so that users query the instance closest to them. This leads to reduced latency;</p><figure><img src="`+q+'" alt="cross-dc-deployment" tabindex="0" loading="lazy"><figcaption>cross-dc-deployment</figcaption></figure><p>It also enables us to spread traffic evenly across the globe. This could also be required in order to comply with certain data privacy laws.</p><h3 id="follow-up-question-filter-businesses-by-type-or-time" tabindex="-1"><a class="header-anchor" href="#follow-up-question-filter-businesses-by-type-or-time" aria-hidden="true">#</a> Follow-up question - filter businesses by type or time</h3><p>Once businesses are filtered, the result set is going to be small, hence, it is acceptable to filter the data in-memory.</p><h3 id="final-design-diagram" tabindex="-1"><a class="header-anchor" href="#final-design-diagram" aria-hidden="true">#</a> Final design diagram</h3><figure><img src="'+S+'" alt="final-design" tabindex="0" loading="lazy"><figcaption>final-design</figcaption></figure><ul><li>Client tries to locate restaurants within 500meters of their location</li><li>Load balancer forwards the request to the LBS</li><li>LBS maps the radius to geohash with length 6</li><li>LBS calculates neighboring geohashes and adds them to the list</li><li>For each geohash, LBS calls the redis server to fetch corresponding business IDs. This can be done in parallel.</li><li>Finally, LBS hydrates the business ids, filters the result and returns it to the user</li><li>Business-related APIs are separated from the LBS into the business service, which checks the cache first for any read requests before consulting the database</li><li>Business updates are handled via a nightly job, which updates the geohash store</li></ul><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap Up</h2><p>Summary of some of the more interesting topics we covered:</p><ul><li>Discussed several indexing options - 2d search, evenly divided grid, geohash, quadtree, google S2</li><li>Discussed caching, replication, sharding, cross-DC deployments in the deep dive section</li></ul>',135),T=[I];function E(B,z){return i(),s("div",null,T)}const D=e(k,[["render",E],["__file","36_proximity_service.html.vue"]]);export{D as default};
