import{_ as e,o as t,c as a,a as i}from"./app-53EDpe6z.js";const s="/assets/system-design-123-YUImGTgk.png",r="/assets/system-design-124-u3v0-jPM.png",o="/assets/system-design-125-mYbwYFTQ.png",n="/assets/system-design-126-eKyp35Tu.png",l="/assets/system-design-127-ht_OeKwX.png",c="/assets/system-design-128-RuVVZJZU.png",h="/assets/system-design-129-xdVI_NtO.png",d="/assets/system-design-130-uyhmxAtV.png",g="/assets/system-design-131-THKoA4x5.png",p="/assets/system-design-132-W4qCn8nx.png",u="/assets/system-design-133-D04MtBKe.png",m="/assets/system-design-134-zGIpy8pJ.png",f="/assets/system-design-135-xKHg0ajr.png",y="/assets/system-design-136-58xV9dwx.png",b="/assets/system-design-137-ftZdz5ej.png",v="/assets/system-design-138-ZgE7TqBf.png",w={},x=i('<h1 id="design-a-search-autocomplete-system" tabindex="-1"><a class="header-anchor" href="#design-a-search-autocomplete-system" aria-hidden="true">#</a> Design A Search Autocomplete System</h1><p>Search autocomplete is the feature provided by many platforms such as Amazon, Google and others when you put your cursor in your search bar and start typing something you&#39;re looking for:</p><figure><img src="'+s+'" alt="google-search" tabindex="0" loading="lazy"><figcaption>google-search</figcaption></figure><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the problem and establish design scope</h2><ul><li>C: Is the matching only supported at the beginning of a search term or eg at the middle?</li><li>I: Only at the beginning</li><li>C: How many autocompletion suggestions should the system return?</li><li>I: 5</li><li>C: Which suggestions should the system choose?</li><li>I: Determined by popularity based on historical query frequency</li><li>C: Does system support spell check?</li><li>I: Spell check or auto-correct is not supported.</li><li>C: Are search queries in English?</li><li>I: Yes, if time allows, we can discuss multi-language support</li><li>C: Is capitalization and special characters supported?</li><li>I: We assume all queries use lowercase characters</li><li>C: How many users use the product?</li><li>I: 10mil DAU</li></ul><p>Summary:</p><ul><li>Fast response time. An article about facebook autocomplete reviews that suggestions should be returned with 100ms delay at most to avoid stuttering</li><li>Relevant - autocomplete suggestions should be relevant to search term</li><li>Sorted - suggestions should be sorted by popularity</li><li>Scalable - system can handle high traffic volumes</li><li>Highly available - system should be up even if parts of the system are unresponsive</li></ul><h2 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back of the envelope estimation</h2><ul><li>Assume we have 10mil DAU</li><li>On average, person performs 10 searches per day</li><li>10mil * 10 = 100mil searches per day = 100 000 000 / 86400 = 1200 searches.</li><li>given 4 works of 5 chars search on average -&gt; 1200 * 20 = 24000 QPS. Peak QPS = 48000 QPS.</li><li>20% of daily queries are new -&gt; 100mil _ 0.2 = 20mil new searches _ 20 bytes = 400mb new data per day.</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose high-level design and get buy-in</h2><p>At a high-level, the system has two components:</p><ul><li>Data gathering service - gathers user input queries and aggregates them in real-time.</li><li>Query service - given search query, return topmost 5 suggestions.</li></ul><h3 id="data-gathering-service" tabindex="-1"><a class="header-anchor" href="#data-gathering-service" aria-hidden="true">#</a> Data gathering service</h3><p>This service is responsible for maintaining a frequency table:</p><figure><img src="'+r+'" alt="frequency-table" tabindex="0" loading="lazy"><figcaption>frequency-table</figcaption></figure><h3 id="query-service" tabindex="-1"><a class="header-anchor" href="#query-service" aria-hidden="true">#</a> Query service</h3><p>Given a frequency table like the one above, this service is responsible for returning the top 5 suggestions based on the frequency column:</p><figure><img src="'+o+'" alt="query-service-example" tabindex="0" loading="lazy"><figcaption>query-service-example</figcaption></figure><p>Querying the data set is a matter of running the following SQL query:</p><figure><img src="'+n+'" alt="query-service-sql-query" tabindex="0" loading="lazy"><figcaption>query-service-sql-query</figcaption></figure><p>This is acceptable for small data sets but becomes impractical for large ones.</p><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design deep dive</h2><p>In this section, we&#39;ll deep dive into several components which will improve the initial high-level design.</p><h3 id="trie-data-structure" tabindex="-1"><a class="header-anchor" href="#trie-data-structure" aria-hidden="true">#</a> Trie data structure</h3><p>We use relational databases in the high-level design, but to achieve a more optimal solution, we&#39;ll need to leverage a suitable data structure.</p><p>We can use tries for fast string prefix retrieval.</p><ul><li>It is a tree-like data structure</li><li>The root represents the empty string</li><li>Each node has 26 children, representing each of the next possible characters. To save space, we don&#39;t store empty links.</li><li>Each node represents a single word or prefix</li><li>For this problem, apart from storing the strings, we&#39;ll need to store the frequency against each leaf</li></ul><figure><img src="'+l+'" alt="trie-example-with-frequency" tabindex="0" loading="lazy"><figcaption>trie-example-with-frequency</figcaption></figure><p>To implement the algorithm, we need to:</p><ul><li>first find the node representing the prefix (time complexity O(p), where p = length of prefix)</li><li>traverse subtree to find all leafs (time complexity O(c), where c = total children)</li><li>sort retrieved children by their frequencies (time complexity O(clogc), where c = total children)</li></ul><figure><img src="'+c+'" alt="trie-algorithm" tabindex="0" loading="lazy"><figcaption>trie-algorithm</figcaption></figure><p>This algorithm works, but there are ways to optimize it as we&#39;ll have to traverse the entire trie in the worst-case scenario.</p><h3 id="limit-the-max-length-of-prefix" tabindex="-1"><a class="header-anchor" href="#limit-the-max-length-of-prefix" aria-hidden="true">#</a> Limit the max length of prefix</h3><p>We can leverage the fact that users rarely use a very long search term to limit max prefix to 50 chars.</p><p>This reduces the time complexity from<code>O(p) + O(c) + O(clogc)</code>-&gt;<code>O(1) + O(c) + O(clogc)</code>.</p><h3 id="cache-top-search-queries-at-each-node" tabindex="-1"><a class="header-anchor" href="#cache-top-search-queries-at-each-node" aria-hidden="true">#</a> Cache top search queries at each node</h3><p>To avoid traversing the whole trie, we can cache the top k most frequently accessed works in each node:</p><figure><img src="'+h+'" alt="caching-top-search-results" tabindex="0" loading="lazy"><figcaption>caching-top-search-results</figcaption></figure><p>This reduces the time complexity to<code>O(1)</code>as top K search terms are already cached. The trade-off is that it takes much more space than a traditional trie.</p><h3 id="data-gathering-service-1" tabindex="-1"><a class="header-anchor" href="#data-gathering-service-1" aria-hidden="true">#</a> Data gathering service</h3><p>In previous design, when user types in search term, data is updated in real-time. This is not practical on a bigger scale due to:</p><ul><li>billions of queries per day</li><li>Top suggestions may not change much once trie is built</li></ul><p>Hence, we&#39;ll instead update the trie asynchronously based on analytics data:</p><figure><img src="'+d+'" alt="data-gathering-service" tabindex="0" loading="lazy"><figcaption>data-gathering-service</figcaption></figure><p>The analytics logs contain raw rows of data related to search terms \\w timestamps:</p><figure><img src="'+g+'" alt="analytics-log" tabindex="0" loading="lazy"><figcaption>analytics-log</figcaption></figure><p>The aggregators&#39; responsibility is to map the analytics data into a suitable format and also aggregate it to lesser records.</p><p>The cadence at which we aggregate depends on the use-case for our auto-complete functionality. If we need the data to be relatively fresh &amp; updated real-time (eg twitter search), we can aggregate once every eg 30m. If, on the other hand, we don&#39;t need the data to be updated real-time (eg google search), we can aggregate once per week.</p><p>Example weekly aggregated data:</p><figure><img src="'+p+'" alt="weekly-aggredated-data" tabindex="0" loading="lazy"><figcaption>weekly-aggredated-data</figcaption></figure><p>The workers are responsible for building the trie data structure, based on aggregated data, and storing it in DB.</p><p>The trie cache keeps the trie loaded in-memory for fast read. It takes a weekly snapshot of the DB.</p><p>The trie DB is the persistent storage. There are two options for this problem:</p><ul><li>Document store (eg MongoDB) - we can periodically build the trie, serialize it and store it in the DB.</li><li>Key-value store (eg DynamoDB) - we can also store the trie in hashmap format.</li></ul><figure><img src="'+u+'" alt="trie-as-hashmap" tabindex="0" loading="lazy"><figcaption>trie-as-hashmap</figcaption></figure><h3 id="query-service-1" tabindex="-1"><a class="header-anchor" href="#query-service-1" aria-hidden="true">#</a> Query service</h3><p>The query service fetches top suggestions from Trie Cache or fallbacks to Trie DB on cache miss:</p><figure><img src="'+m+'" alt="query-service-improved" tabindex="0" loading="lazy"><figcaption>query-service-improved</figcaption></figure><p>Some additional optimizations for the Query service:</p><ul><li>Using AJAX requests on client-side - these prevent the browser from refreshing the page.</li><li>Data sampling - instead of logging all requests, we can log a sample of them to avoid too many logs.</li><li>Browser caching - since auto-complete suggestions don&#39;t change often, we can leverage the browser cache to avoid extra calls to backend.</li></ul><p>Example with Google search caching search results on the browser for 1h:</p><figure><img src="'+f+'" alt="google-browser-caching" tabindex="0" loading="lazy"><figcaption>google-browser-caching</figcaption></figure><h3 id="trie-operations" tabindex="-1"><a class="header-anchor" href="#trie-operations" aria-hidden="true">#</a> Trie operations</h3><p>Let&#39;s briefly describe common trie operations.</p><h3 id="create" tabindex="-1"><a class="header-anchor" href="#create" aria-hidden="true">#</a> Create</h3><p>The trie is created by workers using aggregated data, collected via analytics logs.</p><h3 id="update" tabindex="-1"><a class="header-anchor" href="#update" aria-hidden="true">#</a> Update</h3><p>There are two options to handling updates:</p><ul><li>Not updating the trie, but reconstructing it instead. This is acceptable if we don&#39;t need real-time suggestions.</li><li>Updating individual nodes directly - we prefer to avoid it as it&#39;s slow. Updating a single node required updating all parent nodes as well due to the cached suggestions:</li></ul><figure><img src="'+y+'" alt="update-trie" tabindex="0" loading="lazy"><figcaption>update-trie</figcaption></figure><h3 id="delete" tabindex="-1"><a class="header-anchor" href="#delete" aria-hidden="true">#</a> Delete</h3><p>To avoid showing suggestions including hateful content or any other content we don&#39;t want to show, we can add a filter between the trie cache and the API servers:</p><figure><img src="'+b+'" alt="filter-layer" tabindex="0" loading="lazy"><figcaption>filter-layer</figcaption></figure><p>The database is asynchronously updated to remove hateful content.</p><h3 id="scale-the-storage" tabindex="-1"><a class="header-anchor" href="#scale-the-storage" aria-hidden="true">#</a> Scale the storage</h3><p>At some point, our trie won&#39;t be able to fit on a single server. We need to devise a sharding mechanism.</p><p>One option to achieve this is to shard based on the letters of the alphabet - eg<code>a-m</code>goes on one shard,<code>n-z</code>on the other.</p><p>This doesn&#39;t work well as data is unevenly distributed due to eg the letter<code>a</code>being much more frequent than<code>x</code>.</p><p>To mitigate this, we can have a dedicated shard mapper, which is responsible for devising a smart sharding algorithm, which factors in the uneven distribution of search terms:</p><figure><img src="'+v+'" alt="sharding" tabindex="0" loading="lazy"><figcaption>sharding</figcaption></figure><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap up</h2><p>Other talking points:</p><ul><li>How to support multi-language - we store unicode characters in trie nodes, instead of ASCII.</li><li>What if top search queries differ across countries - we can build different tries per country and leverage CDNs to improve response time.</li><li>How can we support trending (real-time) search queries? - current design doesn&#39;t support this and improving it to support it is beyond the scope of the book. Some options:- Reduce working data set via sharding</li><li>Change ranking model to assign more weight to recent search queries</li><li>Data may come as streams which you filter upon and use map-reduce technologies to process it - Hadoop, Apache Spark, Apache Storm, Apache Kafka, etc.</li></ul>',83),_=[x];function q(k,T){return t(),a("div",null,_)}const D=e(w,[["render",q],["__file","33_design_a_search_autocomplete_system.html.vue"]]);export{D as default};
