import{_ as e,o as t,c as a,a as i}from"./app-53EDpe6z.js";const s="/assets/system-design-284-ShVGWr-Q.png",n="/assets/system-design-285-6Ajuw4_G.png",r="/assets/system-design-286-IQo5stg4.png",o="/assets/system-design-287-S3lFa21P.png",d="/assets/system-design-288-8vIIUIl-.png",l="/assets/system-design-289-blgj-sf8.png",c="/assets/system-design-290-s_LmpWzw.png",g="/assets/system-design-291-WqYzVDl3.png",h="/assets/system-design-292-xBefu062.png",p="/assets/system-design-293-w0Y636H5.png",u="/assets/system-design-294-MamWtXcl.png",m="/assets/system-design-295-BcfWUInJ.png",f="/assets/system-design-296-9KdZYiOc.png",b="/assets/system-design-297-lF1ZE5GT.png",y="/assets/system-design-298-DgLoyWwe.png",w="/assets/system-design-299-FrLm9afP.png",v="/assets/system-design-300-V-GfP26i.png",k="/assets/system-design-301-C8xjEtdG.png",_="/assets/system-design-302-u_BQ7pYk.png",x="/assets/system-design-303-v5b3OldO.png",A="/assets/system-design-304-sr2hfnAQ.png",q="/assets/system-design-305-drRMFLnN.png",T="/assets/system-design-306-kdurs7EG.png",I="/assets/system-design-307-9pZzbN7v.png",S="/assets/system-design-308-yHFU_Hzu.png",z="/assets/system-design-309-UMSGtaOZ.png",D="/assets/system-design-310-Pc-jKCwb.png",R="/assets/system-design-311-_qdOkaYL.png",P="/assets/system-design-312-q_elCEbb.png",H={},W=i('<h1 id="ad-click-event-aggregation" tabindex="-1"><a class="header-anchor" href="#ad-click-event-aggregation" aria-hidden="true">#</a> Ad Click Event Aggregation</h1><p>Digital advertising is a big industry with the rise of Facebook, YouTube, TikTok, etc.</p><p>Hence, tracking ad click events is important. In this chapter, we explore how to design an ad click event aggregation system at Facebook/Google scale.</p><p>Digital advertising has a process called real-time bidding (RTB), where digital advertising inventory is bought and sold:</p><figure><img src="'+s+`" alt="digital-advertising-example" tabindex="0" loading="lazy"><figcaption>digital-advertising-example</figcaption></figure><p>Speed of RTB is important as it usually occurs within a second. Data accuracy is also very important as it impacts how much money advertisers pay.</p><p>Based on ad click event aggregations, advertisers can make decisions such as adjust target audience and keywords.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><ul><li>C: What is the format of the input data?</li><li>I: 1bil ad clicks per day and 2mil ads in total. Number of ad-click events grows 30% year-over-year.</li><li>C: What are some of the most important queries our system needs to support?</li><li>I: Top queries to take into consideration:- Return number of click events for ad X in last Y minutes</li><li>Return top 100 most clicked ads in the past 1min. Both parameters should be configurable. Aggregation occurs every minute.</li><li>Support data filtering by<code>ip</code>,<code>user_id</code>,<code>country</code>for the above queries</li><li>C: Do we need to worry about edge cases? Some of the ones I can think of:- There might be events that arrive later than expected</li><li>There might be duplicate events</li><li>Different parts of the system might be down, so we need to consider system recovery</li><li>I: That&#39;s a good list, take those into consideration</li><li>C: What is the latency requirement?</li><li>I: A few minutes of e2e latency for ad click aggregation. For RTB, it is less than a second. It is ok to have that latency for ad click aggregation as those are usually used for billing and reporting.</li></ul><h3 id="functional-requirements" tabindex="-1"><a class="header-anchor" href="#functional-requirements" aria-hidden="true">#</a> Functional requirements</h3><ul><li>Aggregate the number of clicks of<code>ad_id</code>in the last Y minutes</li><li>Return top 100 most clicked<code>ad_id</code>every minute</li><li>Support aggregation filtering by different attributes</li><li>Dataset volume is at Facebook or Google scale</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>Correctness of the aggregation result is important as it&#39;s used for RTB and ads billing</li><li>Properly handle delayed or duplicate events</li><li>Robustness - system should be resilient to partial failures</li><li>Latency - a few minutes of e2e latency at most</li></ul><h3 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back-of-the-envelope estimation</h3><ul><li>1bil DAU</li><li>Assuming user clicks 1 ad per day -&gt; 1bil ad clicks per day</li><li>Ad click QPS = 10,000</li><li>Peak QPS is 5 times the number = 50,000</li><li>A single ad click occupies 0.1KB storage. Daily storage requirement is 100gb</li><li>Monthly storage = 3tb</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><p>In this section, we discuss query API design, data model and high-level design.</p><h3 id="query-api-design" tabindex="-1"><a class="header-anchor" href="#query-api-design" aria-hidden="true">#</a> Query API Design</h3><p>The API is a contract between the client and the server. In our case, the client is the dashboard user - data scientist/analyst, advertiser, etc.</p><p>Here&#39;s our functional requirements:</p><ul><li>Aggregate the number of clicks of<code>ad_id</code>in the last Y minutes</li><li>Return top N most clicked<code>ad_id</code>in the last M minutes</li><li>Support aggregation filtering by different attributes</li></ul><p>We need two endpoints to achieve those requirements. Filtering can be done via query parameters on one of them.</p><p>Aggregate number of clicks of ad_id in the last M minutes:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>GET /v1/ads/{:ad_id}/aggregated_count
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Query parameters:</p><ul><li>from - start minute. Default is now - 1 min</li><li>to - end minute. Default is now</li><li>filter - identifier for different filtering strategies. Eg 001 means &quot;non-US clicks&quot;.</li></ul><p>Response:</p><ul><li>ad_id - ad identifier</li><li>count - aggregated count between start and end minutes</li></ul><p>Return top N most clicked ad_ids in the last M minutes</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>GET /v1/ads/popular_ads
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Query parameters:</p><ul><li>count - top N most clicked ads</li><li>window - aggregation window size in minutes</li><li>filter - identifier for different filtering strategies</li></ul><p>Response:</p><ul><li>list of ad_ids</li></ul><h3 id="data-model" tabindex="-1"><a class="header-anchor" href="#data-model" aria-hidden="true">#</a> Data model</h3><p>In our system, we have raw and aggregated data.</p><p>Raw data looks like this:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>[AdClickEvent] ad001, 2021-01-01 00:00:01, user 1, 207.148.22.22, USA
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Here&#39;s an example in a structured format:</p><table><thead><tr><th>ad_id</th><th>click_timestamp</th><th>user</th><th>ip</th><th>country</th></tr></thead><tbody><tr><td>ad001</td><td>2021-01-01 00:00:01</td><td>user1</td><td>207.148.22.22</td><td>USA</td></tr><tr><td>ad001</td><td>2021-01-01 00:00:02</td><td>user1</td><td>207.148.22.22</td><td>USA</td></tr><tr><td>ad002</td><td>2021-01-01 00:00:02</td><td>user2</td><td>209.153.56.11</td><td>USA</td></tr></tbody></table><p>Here&#39;s the aggregated version:</p><table><thead><tr><th>ad_id</th><th>click_minute</th><th>filter_id</th><th>count</th></tr></thead><tbody><tr><td>ad001</td><td>202101010000</td><td>0012</td><td>2</td></tr><tr><td>ad001</td><td>202101010000</td><td>0023</td><td>3</td></tr><tr><td>ad001</td><td>202101010001</td><td>0012</td><td>1</td></tr><tr><td>ad001</td><td>202101010001</td><td>0023</td><td>6</td></tr></tbody></table><p>The<code>filter_id</code>helps us achieve our filtering requirements.</p><table><thead><tr><th>filter*id</th><th>region</th><th>IP</th><th>user_id</th></tr></thead><tbody><tr><td>0012</td><td>US</td><td>*</td><td>_</td></tr><tr><td>0013</td><td>_</td><td>123.1.2.3</td><td>_</td></tr></tbody></table><p>To support quickly returning top N most clicked ads in the last M minutes, we&#39;ll also maintain this structure:</p><table><thead><tr><th>most_clicked_ads</th><th></th><th></th></tr></thead><tbody><tr><td>window_size</td><td>integer</td><td>The aggregation window size (M) in minutes</td></tr><tr><td>update_time_minute</td><td>timestamp</td><td>Last updated timestamp (in 1-minute granularity)</td></tr><tr><td>most_clicked_ads</td><td>array</td><td>List of ad IDs in JSON format.</td></tr></tbody></table><p>What are some pros and cons between storing raw data and storing aggregated data?</p><ul><li>Raw data enables using the full data set and supports data filtering and recalculation</li><li>On the other hand, aggregated data allows us to have a smaller data set and a faster query</li><li>Raw data means having a larger data store and a slower query</li><li>Aggregated data, however, is derived data, hence there is some data loss.</li></ul><p>In our design, we&#39;ll use a combination of both approaches:</p><ul><li>It&#39;s a good idea to keep the raw data around for debugging. If there is some bug in aggregation, we can discover the bug and backfill.</li><li>Aggregated data should be stored as well for faster query performance.</li><li>Raw data can be stored in cold storage to avoid extra storage costs.</li></ul><p>When it comes to the database, there are several factors to take into consideration:</p><ul><li>What does the data look like? Is it relational, document or blob?</li><li>Is the workload read-heavy, write-heavy or both?</li><li>Are transactions needed?</li><li>Do the queries rely on OLAP functions like SUM and COUNT?</li></ul><p>For the raw data, we can see that the average QPS is 10k and peak QPS is 50k, so the system is write-heavy. On the other hand, read traffic is low as raw data is mostly used as backup if anything goes wrong.</p><p>Relational databases can do the job, but it can be challenging to scale the writes. Alternatively, we can use Cassandra or InfluxDB which have better native support for heavy write loads.</p><p>Another option is to use Amazon S3 with a columnar data format like ORC, Parquet or AVRO. Since this setup is unfamiliar, we&#39;ll stick to Cassandra.</p><p>For aggregated data, the workload is both read and write heavy as aggregated data is constantly queried for dashboards and alerts. It is also write-heavy as data is aggregated and written every minute by the aggregation service. Hence, we&#39;ll use the same data store (Cassandra) here as well.</p><h3 id="high-level-design" tabindex="-1"><a class="header-anchor" href="#high-level-design" aria-hidden="true">#</a> High-level design</h3><p>Here&#39;s how our system looks like:</p><figure><img src="`+n+'" alt="high-level-design-1" tabindex="0" loading="lazy"><figcaption>high-level-design-1</figcaption></figure><p>Data flows as an unbounded data stream on both inputs and outputs.</p><p>In order to avoid having a synchronous sink, where a consumer crashing can cause the whole system to stall, we&#39;ll leverage asynchronous processing using message queues (Kafka) to decouple consumers and producers.</p><figure><img src="'+r+'" alt="high-level-design-2" tabindex="0" loading="lazy"><figcaption>high-level-design-2</figcaption></figure><p>The first message queue stores ad click event data:</p><table><thead><tr><th>ad_id</th><th>click_timestamp</th><th>user_id</th><th>ip</th><th>country</th></tr></thead></table><p>The second message queue contains ad click counts, aggregated per-minute:</p><table><thead><tr><th>ad_id</th><th>click_minute</th><th>count</th></tr></thead></table><p>As well as top N clicked ads aggregated per minute:</p><table><thead><tr><th>update_time_minute</th><th>most_clicked_ads</th></tr></thead></table><p>The second message queue is there in order to achieve end to end exactly-once atomic commit semantics:</p><figure><img src="'+o+'" alt="atomic-commit" tabindex="0" loading="lazy"><figcaption>atomic-commit</figcaption></figure><p>For the aggregation service, using the MapReduce framework is a good option:</p><figure><img src="'+d+'" alt="ad-count-map-reduce" tabindex="0" loading="lazy"><figcaption>ad-count-map-reduce</figcaption></figure><figure><img src="'+l+'" alt="top-100-map-reduce" tabindex="0" loading="lazy"><figcaption>top-100-map-reduce</figcaption></figure><p>Each node is responsible for one single task and it sends the processing result to the downstream node.</p><p>The map node is responsible for reading from the data source, then filtering and transforming the data.</p><p>For example, the map node can allocate data across different aggregation nodes based on the<code>ad_id</code>:</p><figure><img src="'+c+'" alt="map-node" tabindex="0" loading="lazy"><figcaption>map-node</figcaption></figure><p>Alternatively, we can distribute ads across Kafka partitions and let the aggregation nodes subscribe directly within a consumer group. However, the mapping node enables us to sanitize or transform the data before subsequent processing.</p><p>Another reason might be that we don&#39;t have control over how data is produced, so events related to the same<code>ad_id</code>might go on different partitions.</p><p>The aggregate node counts ad click events by<code>ad_id</code>in-memory every minute.</p><p>The reduce node collects aggregated results from aggregate node and produces the final result:</p><figure><img src="'+g+'" alt="reduce-node" tabindex="0" loading="lazy"><figcaption>reduce-node</figcaption></figure><p>This DAG model uses the MapReduce paradigm. It takes big data and leverages parallel distributed computing to turn it into regular-sized data.</p><p>In the DAG model, intermediate data is stored in-memory and different nodes communicate with each other using TCP or shared memory.</p><p>Let&#39;s explore how this model can now help us to achieve our various use-cases.</p><p>Use-case 1 - aggregate the number of clicks:</p><figure><img src="'+h+'" alt="use-case-1" tabindex="0" loading="lazy"><figcaption>use-case-1</figcaption></figure><ul><li>Ads are partitioned using<code>ad_id % 3</code></li></ul><p>Use-case 2 - return top N most clicked ads:</p><figure><img src="'+p+'" alt="use-case-2" tabindex="0" loading="lazy"><figcaption>use-case-2</figcaption></figure><ul><li>In this case, we&#39;re aggregating the top 3 ads, but this can be extended to top N ads easily</li><li>Each node maintains a heap data structure for fast retrieval of top N ads</li></ul><p>Use-case 3 - data filtering: To support fast data filtering, we can predefine filtering criterias and pre-aggregate based on it:</p><table><thead><tr><th>ad_id</th><th>click_minute</th><th>country</th><th>count</th></tr></thead><tbody><tr><td>ad001</td><td>202101010001</td><td>USA</td><td>100</td></tr><tr><td>ad001</td><td>202101010001</td><td>GPB</td><td>200</td></tr><tr><td>ad001</td><td>202101010001</td><td>others</td><td>3000</td></tr><tr><td>ad002</td><td>202101010001</td><td>USA</td><td>10</td></tr><tr><td>ad002</td><td>202101010001</td><td>GPB</td><td>25</td></tr><tr><td>ad002</td><td>202101010001</td><td>others</td><td>12</td></tr></tbody></table><p>This technique is called thestar schemaand is widely used in data warehouses. The filtering fields are calleddimensions.</p><p>This approach has the following benefits:</p><ul><li>Simple to undertand and build</li><li>Current aggregation service can be reused to create more dimensions in the star schema.</li><li>Accessing data based on filtering criteria is fast as results are pre-calculated</li></ul><p>A limitation of this approach is that it creates many more buckets and records, especially when we have lots of filtering criterias.</p><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>Let&#39;s dive deeper into some of the more interesting topics.</p><h3 id="streaming-vs-batching" tabindex="-1"><a class="header-anchor" href="#streaming-vs-batching" aria-hidden="true">#</a> Streaming vs. Batching</h3><p>The high-level architecture we proposed is a type of stream processing system. Here&#39;s a comparison between three types of systems:</p><table><thead><tr><th></th><th>Services (Online system)</th><th>Batch system (offline system)</th><th>Streaming system (near real-time system)</th></tr></thead><tbody><tr><td>Responsiveness</td><td>Respond to the client quickly</td><td>No response to the client needed</td><td>No response to the client needed</td></tr><tr><td>Input</td><td>User requests</td><td>Bounded input with finite size. A large amount of data</td><td>Input has no boundary (infinite streams)</td></tr><tr><td>Output</td><td>Responses to clients</td><td>Materialized views, aggregated metrics, etc.</td><td>Materialized views, aggregated metrics, etc.</td></tr><tr><td>Performance measurement</td><td>Availability, latency</td><td>Throughput</td><td>Throughput, latency</td></tr><tr><td>Example</td><td>Online shopping</td><td>MapReduce</td><td>Flink [13]</td></tr></tbody></table><p>In our design, we used a mixture of batching and streaming.</p><p>We used streaming for processing data as it arrives and generates aggregated results in near real-time. We used batching, on the other hand, for historical data backup.</p><p>A system which contains two processing paths - batch and streaming, simultaneously, this architecture is called lambda. A disadvantage is that you have two processing paths with two different codebases to maintain.</p><p>Kappa is an alternative architecture, which combines batch and stream processing in one processing path. The key idea is to use a single stream processing engine.</p><p>Lambda architecture:</p><figure><img src="'+u+'" alt="lambda-architecture" tabindex="0" loading="lazy"><figcaption>lambda-architecture</figcaption></figure><p>Kappa architecture:</p><figure><img src="'+m+'" alt="kappa-architecture" tabindex="0" loading="lazy"><figcaption>kappa-architecture</figcaption></figure><p>Our high-level design uses Kappa architecture as reprocessing of historical data also goes through the aggregation service.</p><p>Whenever we have to recalculate aggregated data due to eg a major bug in aggregation logic, we can recalculate the aggregation from the raw data we store.</p><ul><li>Recalculation service retrieves data from raw storage. This is a batch job.</li><li>Retrieved data is sent to a dedicated aggregation service, so that the real-time processing aggregation service is not impacted.</li><li>Aggregated results are sent to the second message queue, after which we update the results in the aggregation database.</li></ul><figure><img src="'+f+'" alt="recalculation-example" tabindex="0" loading="lazy"><figcaption>recalculation-example</figcaption></figure><h3 id="time" tabindex="-1"><a class="header-anchor" href="#time" aria-hidden="true">#</a> Time</h3><p>We need a timestamp to perform aggregation. It can be generated in two places:</p><ul><li>event time - when ad click occurs</li><li>Processing time - system time when the server processes the event</li></ul><p>Due to the usage of async processing (message queues) and network delays, there can be significant difference between event time and processing time.</p><ul><li>If we use processing time, aggregation results can be inaccurate</li><li>If we use event time, we have to deal with delayed events</li></ul><p>There is no perfect solution, we need to consider trade-offs:</p><table><thead><tr><th></th><th>Pros</th><th>Cons</th></tr></thead><tbody><tr><td>Event time</td><td>Aggregation results are more accurate</td><td>Clients might have the wrong time or timestamp might be generated by malicious users</td></tr><tr><td>Processing time</td><td>Server timestamp is more reliable</td><td>The timestamp is not accurate if event is late</td></tr></tbody></table><p>Since data accuracy is important, we&#39;ll use the event time for aggregation.</p><p>To mitigate the issue of delayed events, a technique called &quot;watermark&quot; can be leveraged.</p><p>In the example below, event 2 misses the window where it needs to be aggregated:</p><figure><img src="'+b+'" alt="watermark-technique" tabindex="0" loading="lazy"><figcaption>watermark-technique</figcaption></figure><p>However, if we purposefully extend the aggregation window, we can reduce the likelihood of missed events. The extended part of a window is called a &quot;watermark&quot;:</p><figure><img src="'+y+'" alt="watermark-2" tabindex="0" loading="lazy"><figcaption>watermark-2</figcaption></figure><ul><li>Short watermark increases likelihood of missed events, but reduces latency</li><li>Longer watermark reduces likelihood of missed events, but increases latency</li></ul><p>There is always likelihood of missed events, regardless of the watermark&#39;s size. But there is no use in optimizing for such low-probability events.</p><p>We can instead resolve such inconsistencies by doing end-of-day reconciliation.</p><h3 id="aggregation-window" tabindex="-1"><a class="header-anchor" href="#aggregation-window" aria-hidden="true">#</a> Aggregation window</h3><p>There are four types of window functions:</p><ul><li>Tumbling (fixed) window</li><li>Hopping window</li><li>Sliding window</li><li>Session window</li></ul><p>In our design, we leverage a tumbling window for ad click aggregations:</p><figure><img src="'+w+'" alt="tumbling-window" tabindex="0" loading="lazy"><figcaption>tumbling-window</figcaption></figure><p>As well as a sliding window for the top N clicked ads in M minutes aggregation:</p><figure><img src="'+v+'" alt="sliding-window" tabindex="0" loading="lazy"><figcaption>sliding-window</figcaption></figure><h3 id="delivery-guarantees" tabindex="-1"><a class="header-anchor" href="#delivery-guarantees" aria-hidden="true">#</a> Delivery guarantees</h3><p>Since the data we&#39;re aggregating is going to be used for billing, data accuracy is a priority.</p><p>Hence, we need to discuss:</p><ul><li>How to avoid processing duplicate events</li><li>How to ensure all events are processed</li></ul><p>There are three delivery guarantees we can use - at-most-once, at-least-once and exactly once.</p><p>In most circumstances, at-least-once is sufficient when a small amount of duplicates is acceptable. This is not the case for our system, though, as a difference in small percent can result in millions of dollars of discrepancy. Hence, we&#39;ll need to use exactly-once delivery semantics.</p><h3 id="data-deduplication" tabindex="-1"><a class="header-anchor" href="#data-deduplication" aria-hidden="true">#</a> Data deduplication</h3><p>One of the most common data quality issues is duplicated data.</p><p>It can come from a wide range of sources:</p><ul><li>Client-side - a client might resend the same event multiple times. Duplicated events sent with malicious intent are best handled by a risk engine.</li><li>Server outage - An aggregation service node goes down in the middle of aggregation and the upstream service hasn&#39;t received an acknowledgment so event is resent.</li></ul><p>Here&#39;s an example of data duplication occurring due to failure to acknowledge an event on the last hop:</p><figure><img src="'+k+'" alt="data-duplication-example" tabindex="0" loading="lazy"><figcaption>data-duplication-example</figcaption></figure><p>In this example, offset 100 will be processed and sent downstream multiple times.</p><p>One option to try and mitigate this is to store the last seen offset in HDFS/S3, but this risks the result never reaching downstream:</p><figure><img src="'+_+'" alt="data-duplication-example-2" tabindex="0" loading="lazy"><figcaption>data-duplication-example-2</figcaption></figure><p>Finally, we can store the offset while interacting with downstream atomically. To achieve this, we need to implement a distributed transaction:</p><figure><img src="'+x+'" alt="data-duplication-example-3" tabindex="0" loading="lazy"><figcaption>data-duplication-example-3</figcaption></figure><p>Personal side-note: Alternatively, if the downstream system handles the aggregation result idempotently, there is no need for a distributed transaction.</p><h3 id="scale-the-system" tabindex="-1"><a class="header-anchor" href="#scale-the-system" aria-hidden="true">#</a> Scale the system</h3><p>Let&#39;s discuss how we scale the system as it grows.</p><p>We have three independent components - message queue, aggregation service and database. Since they are decoupled, we can scale them independently.</p><p>How do we scale the message queue:</p><ul><li>We don&#39;t put a limit on producers, so they can be scaled easily</li><li>Consumers can be scaled by assigning them to consumer groups and increasing the number of consumers.</li><li>For this to work, we also need to ensure there are enough partitions created preemptively</li><li>Also, consumer rebalancing can take a while when there are thousands of consumers so it is recommended to do it off peak hours</li><li>We could also consider partitioning the topic by geography, eg<code>topic_na</code>,<code>topic_eu</code>, etc.</li></ul><figure><img src="'+A+'" alt="scale-consumers" tabindex="0" loading="lazy"><figcaption>scale-consumers</figcaption></figure><p>How do we scale the aggregation service:</p><figure><img src="'+q+'" alt="aggregation-service-scaling" tabindex="0" loading="lazy"><figcaption>aggregation-service-scaling</figcaption></figure><ul><li>The map-reduce nodes can easily be scaled by adding more nodes</li><li>The throughput of the aggregation service can be scaled by by utilising multi-threading</li><li>Alternatively, we can leverage resource providers such as Apache YARN to utilize multi-processing</li><li>Option 1 is easier, but option 2 is more widely used in practice as it&#39;s more scalable</li><li>Here&#39;s the multi-threading example:</li></ul><figure><img src="'+T+'" alt="multi-threading-example" tabindex="0" loading="lazy"><figcaption>multi-threading-example</figcaption></figure><p>How do we scale the database:</p><ul><li>If we use Cassandra, it natively supports horizontal scaling utilizing consistent hashing</li><li>If a new node is added to the cluster, data automatically gets rebalanced across all (virtual) nodes</li><li>With this approach, no manual (re)sharding is required</li></ul><figure><img src="'+I+'" alt="cassandra-scalability" tabindex="0" loading="lazy"><figcaption>cassandra-scalability</figcaption></figure><p>Another scalability issue to consider is the hotspot issue - what if an ad is more popular and gets more attention than others?</p><figure><img src="'+S+'" alt="hotspot-issue" tabindex="0" loading="lazy"><figcaption>hotspot-issue</figcaption></figure><ul><li>In the above example, aggregation service nodes can apply for extra resources via the resource manager</li><li>The resource manager allocates more resources, so the original node isn&#39;t overloaded</li><li>The original node splits the events into 3 groups and each of the aggregation nodes handles 100 events</li><li>Result is written back to the original aggregation node</li></ul><p>Alternative, more sophisticated ways to handle the hotspot problem:</p><ul><li>Global-Local Aggregation</li><li>Split Distinct Aggregation</li></ul><h3 id="fault-tolerance" tabindex="-1"><a class="header-anchor" href="#fault-tolerance" aria-hidden="true">#</a> Fault Tolerance</h3><p>Within the aggregation nodes, we are processing data in-memory. If a node goes down, the processed data is lost.</p><p>We can leverage consumer offsets in kafka to continue from where we left off once another node picks up the slack. However, there is additional intermediary state we need to maintain, as we&#39;re aggregating the top N ads in M minutes.</p><p>We can make snapshots at a particular minute for the on-going aggregation:</p><figure><img src="'+z+'" alt="fault-tolerance-example" tabindex="0" loading="lazy"><figcaption>fault-tolerance-example</figcaption></figure><p>If a node goes down, the new node can read the latest committed consumer offset, as well as the latest snapshot to continue the job:</p><figure><img src="'+D+'" alt="fault-tolerance-recovery-example" tabindex="0" loading="lazy"><figcaption>fault-tolerance-recovery-example</figcaption></figure><h3 id="data-monitoring-and-correctness" tabindex="-1"><a class="header-anchor" href="#data-monitoring-and-correctness" aria-hidden="true">#</a> Data monitoring and correctness</h3><p>As the data we&#39;re aggregating is critical as it&#39;s used for billing, it is very important to have rigorous monitoring in place in order to ensure correctness.</p><p>Some metrics we might want to monitor:</p><ul><li>Latency - Timestamps of different events can be tracked in order to understand the e2e latency of the system</li><li>Message queue size - If there is a sudden increase in queue size, we need to add more aggregation nodes. As Kafka is implemented via a distributed commit log, we need to keep track of records-lag metrics instead.</li><li>System resources on aggregation nodes - CPU, disk, JVM, etc.</li></ul><p>We also need to implement a reconciliation flow which is a batch job, running at the end of the day. It calculates the aggregated results from the raw data and compares them against the actual data stored in the aggregation database:</p><figure><img src="'+R+'" alt="reconciliation-flow" tabindex="0" loading="lazy"><figcaption>reconciliation-flow</figcaption></figure><h3 id="alternative-design" tabindex="-1"><a class="header-anchor" href="#alternative-design" aria-hidden="true">#</a> Alternative design</h3><p>In a generalist system design interview, you are not expected to know the internals of specialized software used in big data processing.</p><p>Explaining the thought process and discussing trade-offs is more important than knowing specific tools, which is why the chapter covers a generic solution.</p><p>An alternative design, which leverages off-the-shelf tooling, is to store ad click data in Hive with an ElasticSearch layer on top built for faster queries.</p><p>Aggregation is typically done in OLAP databases such as ClickHouse or Druid.</p><figure><img src="'+P+'" alt="alternative-design" tabindex="0" loading="lazy"><figcaption>alternative-design</figcaption></figure><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap up</h2><p>Things we covered:</p><ul><li>Data model and API Design</li><li>Using MapReduce to aggregate ad click events</li><li>Scaling the message queue, aggregation service and database</li><li>Mitigating the hotspot issue</li><li>Monitoring the system continuously</li><li>Using reconciliation to ensure correctness</li><li>Fault tolerance</li></ul><p>The ad click event aggregation is a typical big data processing system.</p><p>It would be easier to understand and design it if you have prior knowledge of related technologies:</p><ul><li>Apache Kafka</li><li>Apache Spark</li><li>Apache Flink</li></ul>',198),C=[W];function U(F,M){return t(),a("div",null,C)}const N=e(H,[["render",U],["__file","41_ad_click_event_aggregation.html.vue"]]);export{N as default};
