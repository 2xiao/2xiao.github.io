import{_ as e,o as i,c as a,a as t}from"./app-rLUZugrR.js";const s="/assets/system-design-330-LB8vr1fS.png",l="/assets/system-design-331-xPGAz0ZT.png",r="/assets/system-design-332-AcZALgEZ.png",n="/assets/system-design-333-D_3SbYTi.png",o="/assets/system-design-334-7j4ToevY.png",d="/assets/system-design-335-v5nNQCw6.png",c="/assets/system-design-336-XhlmvvGt.png",u="/assets/system-design-337-z-TuGcyH.png",m="/assets/system-design-338-vbTZa_2_.png",p="/assets/system-design-339-eUZ3Zx_z.png",h="/assets/system-design-340-Fy-99PBL.png",g="/assets/system-design-341-W2ksYkHj.png",f="/assets/system-design-342-kQvUlXt9.png",b="/assets/system-design-343-pCsv0BOw.png",v={},y=t('<h1 id="distributed-email-service" tabindex="-1"><a class="header-anchor" href="#distributed-email-service" aria-hidden="true">#</a> Distributed Email Service</h1><p>We&#39;ll design a distributed email service, similar to gmail in this chapter.</p><p>In 2020, gmail had 1.8bil active users, while Outlook had 400mil users worldwide.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><ul><li>C: How many users use the system?</li><li>I: 1bil users</li><li>C: I think following features are important - auth, send/receive email, fetch email, filter emails, search email, anti-spam protection.</li><li>I: Good list. Don&#39;t worry about auth for now.</li><li>C: How do users connect \\w email servers?</li><li>I: Typically, email clients connect via SMTP, POP, IMAP, but we&#39;ll use HTTP for this problem.</li><li>C: Can emails have attachments?</li><li>I: Yes</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>Reliability - we shouldn&#39;t lose data</li><li>Availability - We should use replication to prevent single points of failure. We should also tolerate partial system failures.</li><li>Scalability - As userbase grows, our system should be able to handle them.</li><li>Flexibility and extensibility - system should be flexible and easy to extend with new features. One of the reasons we chose HTTP over SMTP/other mail protocols.</li></ul><h3 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back-of-the-envelope estimation</h3><ul><li>1bil users</li><li>Assuming one person sends 10 emails per day -&gt; 100k emails per second.</li><li>Assuming one person receives 40 emails per day and each email on average has 50kb metadata -&gt; 730pb storage per year.</li><li>Assuming 20% of emails have storage attachments and average size is 500kb -&gt; 1,460pb per year.</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><h3 id="email-knowledge-101" tabindex="-1"><a class="header-anchor" href="#email-knowledge-101" aria-hidden="true">#</a> Email knowledge 101</h3><p>There are various protocols used for sending and receiving emails:</p><ul><li>SMTP - standard protocol for sending emails from one server to another.</li><li>POP - standard protocol for receiving and downloading emails from a remote mail server to a local client. Once retrieved, emails are deleted from remote server.</li><li>IMAP - similar to POP, it is used for receiving and downloading emails from a remote server, but it keeps the emails on the server-side.</li><li>HTTPS - not technically an email protocol, but it can be used for web-based email clients.</li></ul><p>Apart from the mailing protocol, there are some DNS records we need to configure for our email server - the MX records:</p><figure><img src="'+s+'" alt="dns-lookup" tabindex="0" loading="lazy"><figcaption>dns-lookup</figcaption></figure><p>Email attachments are sent base64-encoded and there is usually a size limit of 25mb on most mail services. This is configurable and varies from individual to corporate accounts.</p><h3 id="traditional-mail-servers" tabindex="-1"><a class="header-anchor" href="#traditional-mail-servers" aria-hidden="true">#</a> Traditional mail servers</h3><p>Traditional mail servers work well when there are a limited number of users, connected to a single server.</p><figure><img src="'+l+'" alt="traditional-mail-server" tabindex="0" loading="lazy"><figcaption>traditional-mail-server</figcaption></figure><ul><li>Alice logs into her Outlook email and presses &quot;send&quot;. Email is sent to Outlook mail server. Communication is via SMTP.</li><li>Outlook server queries DNS to find MX record for gmail.com and transfers the email to their servers. Communication is via SMTP.</li><li>Bob fetches emails from his gmail server via IMAP/POP.</li></ul><p>In traditional mail servers, emails were stored on the local file system. Every email was a separate file.</p><figure><img src="'+r+`" alt="local-dir-storage" tabindex="0" loading="lazy"><figcaption>local-dir-storage</figcaption></figure><p>As the scale grew, disk I/O became a bottleneck. Also, it doesn&#39;t satisfy our high availability and reliability requirements. Disks can be damaged and server can go down.</p><h3 id="distributed-mail-servers" tabindex="-1"><a class="header-anchor" href="#distributed-mail-servers" aria-hidden="true">#</a> Distributed mail servers</h3><p>Distributed mail servers are designed to support modern use-cases and solve modern scalability issues.</p><p>These servers can still support IMAP/POP for native email clients and SMTP for mail exchange across servers.</p><p>But for rich web-based mail clients, a RESTful API over HTTP is typically used.</p><p>Example APIs:</p><ul><li><code>POST /v1/messages</code>- sends a message to recipients in To, Cc, Bcc headers.</li><li><code>GET /v1/folders</code>- returns all folders of an email account Example response:</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>[{id: string        Unique folder identifier.
  name: string      Name of the folder.
                    According to RFC6154 [9], the default folders can be one of
                    the following: All, Archive, Drafts, Flagged, Junk, Sent,
                    and Trash.
  user_id: string   Reference to the account owner
}]
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>GET /v1/folders/{:folder_id}/messages</code>- returns all messages under a folder \\w pagination</li><li><code>GET /v1/messages/{:message_id}</code>- get all information about a particular message Example response:</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
  user_id: string                      // Reference to the account owner.
  from: {name: string, email: string}  // &lt;name, email&gt; pair of the sender.
  to: [{name: string, email: string}]  // A list of &lt;name, email&gt; paris
  subject: string                      // Subject of an email
  body: string                         //  Message body
  is_read: boolean                     //  Indicate if a message is read or not.
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Here&#39;s the high-level design of the distributed mail server:</p><figure><img src="`+n+'" alt="high-level-architecture" tabindex="0" loading="lazy"><figcaption>high-level-architecture</figcaption></figure><ul><li>Webmail - users use web browsers to send/receive emails</li><li>Web servers - public-facing request/response services used to manage login, signup, user profile, etc.</li><li>Real-time servers - Used for pushing new email updates to clients in real-time. We use websockets for real-time communication but fallback to long-polling for older browsers that don&#39;t support them.</li><li>Metadata db - stores email metadata such as subject, body, from, to, etc.</li><li>Attachment store - Object store (eg Amazon S3), suitable for storing large files.</li><li>Distributed cache - We can cache recent emails in Redis to improve UX.</li><li>Search store - distributed document store, used for supporting full-text searches.</li></ul><p>Here&#39;s what the email sending flow looks like:</p><figure><img src="'+o+'" alt="email-sending-flow" tabindex="0" loading="lazy"><figcaption>email-sending-flow</figcaption></figure><ul><li>User writes an email and presses &quot;send&quot;. Email is sent to load balancer.</li><li>Load balancer rate limits excessive mail sends and routes to one of the web servers.</li><li>Web servers do basic email validation (eg email size) and short-circuits outbound flow if domain is same as sender. But does spam check first.</li><li>If basic validation passes, email is sent to message queue (attachment is referenced from object store)</li><li>If basic validation fails, email is sent to error queue</li><li>SMTP outgoing workers pull messages from outgoing queue, do spam/virus checks and route to destination mail server.</li><li>Email is stored in the &quot;Sent Emails&quot; folder</li></ul><p>We need to also monitor size of outgoing message queue. Growing too large might indicate a problem:</p><ul><li>Recipient&#39;s mail server is unavailable. We can retry sending the email at a later time using exponential backoff.</li><li>Not enough consumers to handle the load, we might have to scale the consumers.</li></ul><p>Here&#39;s the email receiving flow:</p><figure><img src="'+d+'" alt="email-receiving-flow" tabindex="0" loading="lazy"><figcaption>email-receiving-flow</figcaption></figure><ul><li>Incoming emails arrive at the SMTP load balancer. Mails are distributed to SMTP servers, where mail acceptance policy is done (eg invalid emails are directly discarded).</li><li>If attachment of email is too large, we can put it in object store (s3).</li><li>Mail processing workers do preliminary checks, after which mails are forwarded to storage, cache, object store and real-time servers.</li><li>Offline users get their new emails once they come back online via HTTP API.</li></ul><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>Let&#39;s now go deeper into some of the components.</p><h3 id="metadata-database" tabindex="-1"><a class="header-anchor" href="#metadata-database" aria-hidden="true">#</a> Metadata database</h3><p>Here are some of the characteristics of email metadata:</p><ul><li>headers are usually small and frequently accessed</li><li>Body size ranges from small to big, but is typically read once</li><li>Most mail operations are isolated to a single user - eg fetching email, marking as read, searching.</li><li>Data recency impacts data usage. Users typically read only recent emails</li><li>Data has high-reliability requirements. Data loss is unacceptable.</li></ul><p>At gmail/outlook scale, the database is typically custom made to reduce input/output operations per second (IOPS).</p><p>Let&#39;s consider what database options we have:</p><ul><li>Relational database - we can build indexes for headers and body, but these DBs are typically optimized for small chunks of data.</li><li>Distributed object store - this can be a good option for backup storage, but can&#39;t efficiently support searching/marking as read/etc.</li><li>NoSQL - Google BigTable is used by gmail, but it&#39;s not open-sourced.</li></ul><p>Based on above analysis, very few existing solutions seems to fit our needs perfectly. In an interview setting, it&#39;s infeasible to design a new distributed database solution, but important to mention characteristics:</p><ul><li>Single column can be a single-digit MB</li><li>Strong data consistency</li><li>Designed to reduce disk I/O</li><li>Highly available and fault tolerant</li><li>Should be easy to create incremental backups</li></ul><p>In order to partition the data, we can use the<code>user_id</code>as a partition key, so that one user&#39;s data is stored on a single shard. This prohibits us from sharing an email with multiple users, but this is not a requirement for this interview.</p><p>Let&#39;s define the tables:</p><ul><li>Primary key consists of partition key (data distribution) and clustering key (sorting data)</li><li>Queries we need to support - get all folders for a user, display all emails for a folder, create/get/delete an email, fetch read/unread email, get conversation threads (bonus)</li></ul><p>Legend for tables to follow:</p><figure><img src="'+c+'" alt="legend" tabindex="0" loading="lazy"><figcaption>legend</figcaption></figure><p>Here is the folders table:</p><figure><img src="'+u+'" alt="folders-table" tabindex="0" loading="lazy"><figcaption>folders-table</figcaption></figure><p>emails table:</p><figure><img src="'+m+'" alt="emails-table" tabindex="0" loading="lazy"><figcaption>emails-table</figcaption></figure><ul><li>email_id is timeuuid which allows sorting based on timestamp when email was created</li></ul><p>Attachments are stored in a separate table, identified by filename:</p><figure><img src="'+p+'" alt="attachments" tabindex="0" loading="lazy"><figcaption>attachments</figcaption></figure><p>Supporting fetchin read/unread emails is easy in a traditional relational database, but not in Cassandra, since filtering on non-partition/clustering key is prohibited. One workaround to fetch all emails in a folder and filter in-memory, but that doesn&#39;t work well for a big-enough application.</p><p>What we can do is denormalize the emails table into read/unread emails tables:</p><figure><img src="'+h+`" alt="read-unread-emails" tabindex="0" loading="lazy"><figcaption>read-unread-emails</figcaption></figure><p>In order to support conversation threads, we can include some headers, which mail clients interpret and use to reconstruct a conversation thread:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
  &quot;headers&quot; {
     &quot;Message-Id&quot;: &quot;&lt;7BA04B2A-430C-4D12-8B57-862103C34501@gmail.com&gt;&quot;,
     &quot;In-Reply-To&quot;: &quot;&lt;CAEWTXuPfN=LzECjDJtgY9Vu03kgFvJnJUSHTt6TW@gmail.com&gt;&quot;,
     &quot;References&quot;: [&quot;&lt;7BA04B2A-430C-4D12-8B57-862103C34501@gmail.com&gt;&quot;]
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Finally, we&#39;ll trade availability for consistency for our distributed database, since it is a hard requirement for this problem.</p><p>Hence, in the event of a failover or network parititon, sync/update actions will be briefly unavailable to impacted users.</p><h3 id="email-deliverability" tabindex="-1"><a class="header-anchor" href="#email-deliverability" aria-hidden="true">#</a> Email deliverability</h3><p>It is easy to setup a server to send emails, but getting the email to a receiver&#39;s inbox is hard, due to spam-protection algorithms.</p><p>If we just setup a new mail server and start sending mails through it, our emails will probably end up in the spam folder.</p><p>Here&#39;s what we can do to prevent that:</p><ul><li>Dedicated IPs - use dedicated IPs for sending emails, otherwise, recipient servers will not trust you.</li><li>Classify emails - avoid sending marketing emails from the same servers to prevent more important email to be classified as spam</li><li>Warm up your IP address slowly to build a good reputation with big email providers. It takes 2 to 6 weeks to warm up a new IP</li><li>Ban spammers quickly to not deteriorate your reputation</li><li>Feedback processing - setup a feedback loop with ISPs to keep track of complaint rate and ban spam accounts quickly.</li><li>Email authentication - use common techniques to combat phishing such as Sender Policy Framework, DomainKeys Identified Mail, etc.</li></ul><p>You don&#39;t need to remember all of this. Just know that building a good mail server requires a lot of domain knowledge.</p><h3 id="search" tabindex="-1"><a class="header-anchor" href="#search" aria-hidden="true">#</a> Search</h3><p>Searching includes doing a full-text search based on email contents or more advanced queries based on from, to, subject, unread, etc filters.</p><p>One characteristic of email search is that it is local to the user and it has more writes than reads, because we need to re-index it on each operation, but users rarely use the search tab.</p><p>Let&#39;s compare google search with email search:</p><table><thead><tr><th></th><th>Scope</th><th>Sorting</th><th>Accuracy</th></tr></thead><tbody><tr><td>Google search</td><td>The whole internet</td><td>Sort by relevance</td><td>Indexing takes some time, so not instant results.</td></tr><tr><td>Email search</td><td>User’s own email box</td><td>Sort by attributes eg time, date, etc</td><td>Indexing should be quick and results accurate.</td></tr></tbody></table><p>To achieve this search functionality, one option is to use an Elasticsearch cluster. We can use<code>user_id</code>as the partition key to group data under the same node:</p><figure><img src="`+g+'" alt="elasticsearch" tabindex="0" loading="lazy"><figcaption>elasticsearch</figcaption></figure><p>Mutating operations are async via Kafka in order to decouple services from the reindexing flow. Actually searching for data happens synchronously.</p><p>Elasticsearch is one of the most popular search-engine databases and supports full-text search for emails very well.</p><p>Alternatively, we can attempt to develop our own custom search solution to meet our specific requirements.</p><p>Designing such a system is out of scope. One of the core challenges when building it is to optimize it for write-heavy workloads.</p><p>To achieve that, we can use Log-Structured Merge-Trees (LSM) to structure the index data on disk. Write path is optimized for sequential writes only. This technique is used in Cassandra, BigTable and RocksDB.</p><p>Its core idea is to store data in-memory until a predefined threshold is reached, after which it is merged in the next layer (disk):</p><figure><img src="'+f+'" alt="lsm-tree" tabindex="0" loading="lazy"><figcaption>lsm-tree</figcaption></figure><p>Main trade-offs between the two approaches:</p><ul><li>Elasticsearch scales to some extent, whereas a custom search engine can be fine-tuned for the email use-case, allowing it to scale further.</li><li>Elasticsearch is a separate service we need to maintain, alongside the metadata store. A custom solution can be the datastore itself.</li><li>Elasticsearch is an off-the-shelf solution, whereas the custom search engine would require significant engineering effort to build.</li></ul><h3 id="scalability-and-availability" tabindex="-1"><a class="header-anchor" href="#scalability-and-availability" aria-hidden="true">#</a> Scalability and availability</h3><p>Since individual user operations don&#39;t collide with other users, most components can be independently scaled.</p><p>To ensure high availability, we can also use a multi-DC setup with leader-folower failover in case of failures:</p><figure><img src="'+b+'" alt="multi-dc-example" tabindex="0" loading="lazy"><figcaption>multi-dc-example</figcaption></figure><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap up</h2><p>Additional talking points:</p><ul><li>Fault tolerance - Many parts of the system could fail. It is worthwhile how we&#39;d handle node failures.</li><li>Compliance - PII needs to be stored in a reasonable way, given Europe&#39;s GDPR laws.</li><li>Security - email encryption, phishing protection, safe browsing, etc.</li><li>Optimizations - eg preventing duplication of the same attachments, sent multiple times by different users.</li></ul>',101),w=[y];function k(x,_){return i(),a("div",null,w)}const S=e(v,[["render",k],["__file","43_distributed_email_service.html.vue"]]);export{S as default};
