import{_ as s,r as o,o as n,c as r,b as e,d as t,e as l,a}from"./app-rLUZugrR.js";const d="/assets/system-design-344-p-Lh0f0k.png",c="/assets/system-design-345-4JBrjnvK.png",h="/assets/system-design-346-MMLW0ag7.png",p="/assets/system-design-347-s5OmlUcD.png",u="/assets/system-design-348-qabLA2dp.png",g="/assets/system-design-349-ZMZxg-dw.png",b="/assets/system-design-350-yYBPh2My.png",m="/assets/system-design-351-ehleF6t4.png",f="/assets/system-design-352-CP8CfDzd.png",y="/assets/system-design-353-hjCxRNLR.png",v="/assets/system-design-354-oI-zjS7c.png",w="/assets/system-design-355-7hu1L_Yw.png",j="/assets/system-design-356-qvjadLAL.png",k="/assets/system-design-357-KjYkvqn6.png",_="/assets/system-design-358-7CU1_mHk.png",x="/assets/system-design-359-Azl9c4yK.png",I="/assets/system-design-360-fYdLvvGJ.png",T="/assets/system-design-361-JQwG3agn.png",z="/assets/system-design-362-HRjNCKiI.png",S="/assets/system-design-363-Cw1vog2H.png",D="/assets/system-design-364-woec355P.png",A="/assets/system-design-365-2AUNz7pg.png",q="/assets/system-design-366-SRvwVibk.png",P={},C=a('<h1 id="s3-like-object-storage" tabindex="-1"><a class="header-anchor" href="#s3-like-object-storage" aria-hidden="true">#</a> S3-like Object Storage</h1><p>In this chapter, we&#39;ll be designing an object storage service, similar to Amazon S3.</p><p>Storage systems fall into three broad categories:</p><ul><li>Block storage</li><li>File storage</li><li>Object storage</li></ul><p>Block storage are devices, which came out in 1960s. HDDs and SSDs are such examples. These devices are typically physically attached to a server, although they can also be network-attached via high-speed network protocols. Servers can format the raw blocks and use them as a file system or it can hand control of them to servers directly.</p><p>File storage is built on top of block storage. It provides a higher level of abstraction, making it easier to manage folders and files.</p><p>Object storage sacrifices performance for high durability, vast scale and low cost. It targets &quot;cold&quot; data and is mainly used for archival and backup. There is no hierarchical directory structure, all data is stored as objects in a flat structure. It is relatively slow compared to other storage types. Most cloud providers have an object storage offering - Amazon S3, Google GCS, etc.</p><figure><img src="'+d+'" alt="storage-comparison" tabindex="0" loading="lazy"><figcaption>storage-comparison</figcaption></figure><table><thead><tr><th></th><th>Block Storage</th><th>File Storage</th><th>Object Storage</th></tr></thead><tbody><tr><td>Mutable Content</td><td>Y</td><td>Y</td><td>N (has object versioning）</td></tr><tr><td>Cost</td><td>High</td><td>Medium to high</td><td>Low</td></tr><tr><td>Performance</td><td>Medium to high, very high</td><td>Medium to high</td><td>Low to medium</td></tr><tr><td>Consistency</td><td>Strong consistency</td><td>Strong consistency</td><td>Strong consistency [5]</td></tr><tr><td>Data access</td><td>SAS/iSCSI/FC</td><td>Standard file access, CIFS/SMB, and NFS</td><td>RESTful API</td></tr><tr><td>Scalability</td><td>Medium scalability</td><td>High scalability</td><td>Vast scalability</td></tr><tr><td>Good for</td><td>Virtual machines (VM), databases</td><td>General-purpose file system access</td><td>Binary data, unstructured data</td></tr></tbody></table><p>Some terminology, related to object storage:</p><ul><li>Bucket - logical container for objects. Name is globally unique.</li><li>Object - An individual piece of data, stored in a bucket. Contains object data and metadata.</li><li>Versioning - A feature keeping multiple variants of an object in the same bucket.</li><li>Uniform Resource Identifier (URI) - each resource is uniquely identified by a URI.</li><li>Service-level Agreement (SLA) - contract between service provider and client.</li></ul><p>Amazon S3 Standard-Infrequent Access storage class SLAs:</p><ul><li>Durability of 99.999999999% across multiple Availability Zones</li><li>Data is resilient in the event of entire Availability Zone being destroyed</li><li>Designed for 99.9% availability</li></ul><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><ul><li>C: Which features should be included?</li><li>I: Bucket creation, Object upload/download, versioning, Listing objects in a bucket</li><li>C: What is the typical data size?</li><li>I: We need to store both massive objects and small objects efficiently</li><li>C: How much data do we store in a year?</li><li>I: 100 petabytes</li><li>C: Can we assume 6 nines of data durbility (99.9999%) and service availability of 4 nines (99.99%)?</li><li>I: Yes, sounds reasonable</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>100 PB of data</li><li>6 nines of data durability</li><li>4 nines of service availability</li><li>Storage efficiency. Reduce storage cost while maintaining high reliability and performance</li></ul><h3 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back-of-the-envelope estimation</h3><p>Object storage is likely to have bottlenecks in disk capacity or IO per second (IOPS).</p><p>Assumptions:</p><ul><li>we have 20% small (less than 1mb), 60% mid-size (1-64mb) and 20% large objects (greater than 64mb),</li><li>One hard disk (SATA, 7200rpm) is capable of doing 100-150 random seeks per second (100-150 IOPS)</li></ul><p>Given the assumptions, we can estimate the total number of objects the system can persist.</p><ul><li>Let&#39;s use median size per object type to simplify calculation - 0.5mb for small, 32mb for medium, 200mb for large.</li><li>Given 100PB of storage (10^11 MB) and 40% of storage usage results in 0.68bil objects</li><li>If we assume metadata is 1kb, then we need 0.68tb space to store metadata info</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><p>Let&#39;s explore some interesting properties of object storage before diving into the design:</p><ul><li>Object immutability - objects in object storage are immutable (not the case in other storage systems). We may delete them or replace them, but no update.</li><li>Key-value store - an object URI is its key and we can get its contents by making an HTTP call</li><li>Write once, read many times - data access pattern is writing once and reading many times. According to some Linkedin research, 95% of operations are reads</li><li>Support both small and large objects</li></ul><p>Design philosophy of object storage is similar to UNIX - when we save a file, it creates the filename in a data structure, called inode and file data is stored in different disk locations. The inode contains a list of file block pointers, which point to different locations on disk.</p><p>When accessing a file, we first fetch its metadata from the inode, prior to fetching the file contents.</p><p>Object storage works similarly - metadata store is used for file information, but contents are stored on disk:</p><figure><img src="'+c+'" alt="object-store-vs-unix" tabindex="0" loading="lazy"><figcaption>object-store-vs-unix</figcaption></figure><p>By separating metadata from file contents, we can scale the different stores independently:</p><figure><img src="'+h+'" alt="bucket-and-object" tabindex="0" loading="lazy"><figcaption>bucket-and-object</figcaption></figure><h3 id="high-level-design" tabindex="-1"><a class="header-anchor" href="#high-level-design" aria-hidden="true">#</a> High-level design</h3><figure><img src="'+p+'" alt="high-level-design" tabindex="0" loading="lazy"><figcaption>high-level-design</figcaption></figure><ul><li>Load balancer - distributes API requests across service replicas</li><li>API service - Stateless server, orchestrating calls to metadata and object store, as well as IAM service.</li><li>Identity and access management (IAM) - central place for auth, authz, access control.</li><li>Data store - stores and retrieves actual data. Operations are based on object ID (UUID).</li><li>Metadata store - stores object metadata</li></ul><h3 id="uploading-an-object" tabindex="-1"><a class="header-anchor" href="#uploading-an-object" aria-hidden="true">#</a> Uploading an object</h3><figure><img src="'+u+`" alt="uploading-object" tabindex="0" loading="lazy"><figcaption>uploading-object</figcaption></figure><ul><li>Create a bucket named &quot;bucket-to-share&quot; via HTTP PUT request</li><li>API service calls IAM to ensure user is authorized and has write permissions</li><li>API service calls metadata store to create a bucket entry. Once created, success response is returned.</li><li>After bucket is created, HTTP PUT is sent to create an object named &quot;script.txt&quot;</li><li>API service verifies user identity and ensures user has write permissions</li><li>Once validation passes, object payload is sent via HTTP PUT to the data store. Data store persists it and returns a UUID.</li><li>API service calls metadata store to create a new entry with object_id, bucket_id and bucket_name, among other metadata.</li></ul><p>Example object upload request:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>PUT /bucket-to-share/script.txt HTTP/1.1
Host: foo.s3example.org
Date: Sun, 12 Sept 2021 17:51:00 GMT
Authorization: authorization string
Content-Type: text/plain
Content-Length: 4567
x-amz-meta-author: Alex

[4567 bytes of object data]
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="downloading-an-object" tabindex="-1"><a class="header-anchor" href="#downloading-an-object" aria-hidden="true">#</a> Downloading an object</h3><p>Buckets have no directory hierarchy, buy we can create a logical hierarchy by concatenating bucket name and object name to simulate a folder structure.</p><p>Example GET request for fetching an object:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>GET /bucket-to-share/script.txt HTTP/1.1
Host: foo.s3example.org
Date: Sun, 12 Sept 2021 18:30:01 GMT
Authorization: authorization string
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><figure><img src="`+g+'" alt="download-object" tabindex="0" loading="lazy"><figcaption>download-object</figcaption></figure><ul><li>Client sends an HTTP GET request to the load balancer, ie<code>GET /bucket-to-share/script.txt</code></li><li>API service queries IAM to verify the user has correct permissions to read the bucket</li><li>Once validated, UUID of object is retrieved from metadata store</li><li>Object payload is retrieved from data store based on UUID and returned to the client</li></ul><p>// sprint 1</p><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><h3 id="data-store" tabindex="-1"><a class="header-anchor" href="#data-store" aria-hidden="true">#</a> Data store</h3><p>Here&#39;s how the API service interacts with the data store:</p><figure><img src="'+b+'" alt="data-store-interactions" tabindex="0" loading="lazy"><figcaption>data-store-interactions</figcaption></figure><p>The data store&#39;s main components:</p><figure><img src="'+m+'" alt="data-store-main-components" tabindex="0" loading="lazy"><figcaption>data-store-main-components</figcaption></figure><p>The data routing service provides a RESTful or gRPC API to access the data node cluster. It is a stateless service, which scales by adding more servers.</p><p>It&#39;s main responsibilities are:</p><ul><li>querying the placement service to get the best data node to store data</li><li>reading data from data nodes and returning it to the API service</li><li>Writing data to data nodes</li></ul><p>The placement service determines which data nodes should store an object. It maintains a virtual cluster map, which determines the physical topology of a cluster.</p><figure><img src="'+f+'" alt="virtual-cluster-map" tabindex="0" loading="lazy"><figcaption>virtual-cluster-map</figcaption></figure><p>The service also sends heartbeats to all data nodes to determine if they should be removed from the virtual cluster.</p><p>Since this is a critical service, it is recommended to maintain a cluster of 5 or 7 replicas, synchronized via Paxos or Raft consensus algorithms. Eg a 7 node cluster can tolerate 3 nodes failing.</p><p>Data nodes store the actual object data. Reliability and durability is ensured by replicating data to multiple data nodes.</p><p>Each data node has a daemon running, which sends heartbeats to the placement service.</p><p>The heartbeat includes:</p><ul><li>How many disk drives (HDD or SSD) does the data node manage?</li><li>How much data is stored on each drive?</li></ul><h3 id="data-persistence-flow" tabindex="-1"><a class="header-anchor" href="#data-persistence-flow" aria-hidden="true">#</a> Data persistence flow</h3><figure><img src="'+y+'" alt="data-persistence-flow" tabindex="0" loading="lazy"><figcaption>data-persistence-flow</figcaption></figure><ul><li>API service forwards the object data to data store</li><li>Data routing service sends the data to the primary data node</li><li>Primary data node saves the data locally and replicates it to two secondary data nodes. Response is sent after successful replication.</li><li>The UUID of the object is returned to the API service.</li></ul><p>Caveats:</p><ul><li>Given an object UUID, it&#39;s replication group is deterministically chosen by using consistent hashing</li><li>In step 4, the primary data node replicates the object data before returning a response. This favors strong consistency over higher latency.</li></ul><figure><img src="'+v+'" alt="consistency-vs-latency" tabindex="0" loading="lazy"><figcaption>consistency-vs-latency</figcaption></figure><h3 id="how-data-is-organized" tabindex="-1"><a class="header-anchor" href="#how-data-is-organized" aria-hidden="true">#</a> How data is organized</h3><p>One simple approach to managing data is to store each object in a separate file.</p><p>This works, but is not performant with many small files in a file system:</p><ul><li>Data blocks on HDD are wasted, because every file uses the whole block size. Typical block size is 4kb.</li><li>Many files means many inodes. Operating systems don&#39;t deal well with too many inodes and there is also a max inode limit.</li></ul><p>These issues can be addressed by merging many small files into bigger ones via a write-ahead log (WAL). Once the file reaches its capacity (typically a few GB), a new file is created:</p><figure><img src="'+w+'" alt="wal-optimization" tabindex="0" loading="lazy"><figcaption>wal-optimization</figcaption></figure><p>The downside of this approach is that write access to the file needs to be serialized. Multiple cores accessing the same file must wait for each other. To fix this, we can confine files to specific cores to avoid lock contention.</p><h3 id="object-lookup" tabindex="-1"><a class="header-anchor" href="#object-lookup" aria-hidden="true">#</a> Object lookup</h3><p>To support storing multiple objects in the same file, we need to maintain a table, which tells the data node:</p><ul><li><code>object_id</code></li><li><code>filename</code>where object is stored</li><li><code>file_offset</code>where object starts</li><li><code>object_size</code></li></ul><p>We can deploy this table in a file-based db like RocksDB or a traditional relational database. Since the access pattern is low write+high read, a relational database works better.</p><p>How should we deploy it? We could deploy the db and scale it separately in a cluster, accessed by all data nodes.</p><p>Downsides:</p><ul><li>we&#39;d need to aggressively scale the cluster to serve all requests</li><li>there&#39;s additional network latency between data node and db cluster</li></ul><p>An alternative is to take advantage of the fact that data nodes are only interested to data related to them, so we can deploy the relational db within the data node itself.</p><p>SQLite is a good option as it&#39;s a lightweight file-based relational database.</p><h3 id="updated-data-persistence-flow" tabindex="-1"><a class="header-anchor" href="#updated-data-persistence-flow" aria-hidden="true">#</a> Updated data persistence flow</h3><figure><img src="'+j+'" alt="updated-data-persistence-flow" tabindex="0" loading="lazy"><figcaption>updated-data-persistence-flow</figcaption></figure><ul><li>API Service sends a request to save a new object</li><li>Data node service appends the new object at the end of a file, named &quot;/data/c&quot;</li><li>A new record for the object is inserted into the object mapping table</li></ul><h3 id="durability" tabindex="-1"><a class="header-anchor" href="#durability" aria-hidden="true">#</a> Durability</h3><p>Data durability is an important requirement in our design. In order to achieve 6 nines of durability, every failure case needs to be properly examined.</p><p>First problem to address is hardware failures. We can achieve that by replicating data nodes to minimize probability of failure. But in addition to that, we also ought to replicate across different failure domains (cross-rack, cross-dc, separate networks, etc). A critical event can cause multiple hardware failures within the same domain:</p><figure><img src="'+k+'" alt="failure-domain-isolation" tabindex="0" loading="lazy"><figcaption>failure-domain-isolation</figcaption></figure><p>Assuming annual failure rate of a typical HDD is 0.81%, making three copies gives us 6 nines of durability.</p><p>Replicating the data nodes like that grants us the durability we want, but we could also leverage erasure coding to reduce storage costs.</p><p>Erasure coding enables us to use parity bits, which allow us to reconstruct lost bits in the event of a failure:</p><figure><img src="'+_+'" alt="erasure-coding" tabindex="0" loading="lazy"><figcaption>erasure-coding</figcaption></figure><p>Imagine those bits are data nodes. If two of them go down, they can be recovered using the remaining four ones.</p><p>There are different erasure coding schemes. In our case, we could use 8+4 erasure coding, split across different failure domains to maximize reliability:</p><figure><img src="'+x+'" alt="erasure-coding-across-failure-domains" tabindex="0" loading="lazy"><figcaption>erasure-coding-across-failure-domains</figcaption></figure><p>Erasure coding enables us to achieve a much lower storage cost (50% improvement) at the expense of access speed due to the data routing service having to collect data from multiple locations:</p><figure><img src="'+I+'" alt="erasure-coding-vs-replication" tabindex="0" loading="lazy"><figcaption>erasure-coding-vs-replication</figcaption></figure><p>Other caveats:</p>',103),U=e("li",null,"Replication requires 200% storage overhead (in case of 3 replicas) vs. 50% via erasure coding",-1),H={href:"https://github.com/Backblaze/erasure-coding-durability",target:"_blank",rel:"noopener noreferrer"},O=e("li",null,"Erasure coding requires more computation to calculate and store parities",-1),E=a('<p>In sum, replication is more useful for latency-sensitive applications, whereas erasure coding is attractive for storage cost efficiency and durability. Erasure coding is also much harder to implement.</p><h3 id="correctness-verification" tabindex="-1"><a class="header-anchor" href="#correctness-verification" aria-hidden="true">#</a> Correctness verification</h3><p>If a disk fails entirely, then the failure is easy to detect. This is less straightforward in the event part of the disk memory gets corrupted.</p><p>To detect this, we can use checksums - a hash of the file contents, which can be used to verify the file&#39;s integrity.</p><p>In our case, we&#39;ll store checksums for each file and each object:</p><figure><img src="'+T+'" alt="checksums-for-correctness" tabindex="0" loading="lazy"><figcaption>checksums-for-correctness</figcaption></figure><p>In the case of erasure coding (8+4), we&#39;ll need to fetch each of the 8 pieces of data separately and verify each of their checksums.</p><p>// sprint 2</p><h3 id="metadata-data-model" tabindex="-1"><a class="header-anchor" href="#metadata-data-model" aria-hidden="true">#</a> Metadata data model</h3><p>Table schemas:</p><figure><img src="'+z+'" alt="metadata-data-model" tabindex="0" loading="lazy"><figcaption>metadata-data-model</figcaption></figure><p>Queries we need to support:</p><ul><li>Find an object ID by name</li><li>Insert/delete object based on name</li><li>List objects in a bucket sharing the same prefix</li></ul><p>There is usually a limit on the number of buckets a user can create, hence, the size of the buckets table is small and can fit into a single db server. But we still need to scale the server for read throughput.</p><p>The object table will probably not fit into a single database server, though. Hence, we can scale the table via sharding:</p><ul><li>Sharding by bucket_id will lead to hotspot issues as a bucket can have billions of objects</li><li>Sharding by bucket_id makes the load more evenly distributed, but our queries will be slow</li><li>We choose sharding by<code>hash(bucket_name, object_name)</code>since most queries are based on the object/bucket name.</li></ul><p>Even with this sharding scheme, though, listing objects in a bucket will be slow.</p><h3 id="listing-objects-in-a-bucket" tabindex="-1"><a class="header-anchor" href="#listing-objects-in-a-bucket" aria-hidden="true">#</a> Listing objects in a bucket</h3><p>In a single database, listing an object based on its prefix (looks like a directory) works like this:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>SELECT * FROM object WHERE bucket_id = &quot;123&quot; AND object_name LIKE `abc/%`\n</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This is challenging to fulfill when the database is sharded. To achieve it, we can run the query on every shard and aggregate the results in-memory. This makes pagination challenging though, since different shards contain a different result size and we need to maintain separate limit/offset for each.</p><p>We can leverage the fact that typically object stores are not optimized for listing objects, so we can sacrifice listing performance. We can also create a denormalized table for listing objects, sharded by bucket ID. That would make our listing query sufficiently fast as it&#39;s isolated to a single database instance.</p><h3 id="object-versioning" tabindex="-1"><a class="header-anchor" href="#object-versioning" aria-hidden="true">#</a> Object versioning</h3><p>Versioning works by having another<code>object_version</code>column which is of type TIMEUUID, enabling us to sort records based on it.</p><p>Each new version produces a new<code>object_id</code>:</p><figure><img src="'+S+'" alt="object-versioning" tabindex="0" loading="lazy"><figcaption>object-versioning</figcaption></figure><p>Deleting an object creates a new version with a special<code>object_id</code>indicating that the object was deleted. Queries for it return 404:</p><figure><img src="'+D+'" alt="deleting-versioned-object" tabindex="0" loading="lazy"><figcaption>deleting-versioned-object</figcaption></figure><h3 id="optimizing-uploads-of-large-files" tabindex="-1"><a class="header-anchor" href="#optimizing-uploads-of-large-files" aria-hidden="true">#</a> Optimizing uploads of large files</h3><p>Uploading large files can be optimized by using multipart uploads - splitting a big file into several chunks, uploaded independently:</p><figure><img src="'+A+'" alt="multipart-upload" tabindex="0" loading="lazy"><figcaption>multipart-upload</figcaption></figure><ul><li>Client calls service to initiate a multipart upload</li><li>Data store returns an upload ID which uniquely identifies the upload</li><li>Client splits the large file into several chunks, uploaded independently using the upload id</li><li>When a chunk is uploaded, the data store returns an etag, which is a md5 checksum, identifying that upload chunk</li><li>After all parts are uploaded, client sends a complete multipart upload request, which includes upload_id, part numbers and all etags</li><li>Data store reassembles the object from its parts. The process can take a few minutes. After that, success response is returned to the client.</li></ul><p>Old parts, which are no longer useful can be removed at this point. We can introduce a garbage collector to deal with it.</p><h3 id="garbage-collection" tabindex="-1"><a class="header-anchor" href="#garbage-collection" aria-hidden="true">#</a> Garbage collection</h3><p>Garbage collection is the process of reclaiming storage space, which is no longer used. There are a few ways data becomes garbage:</p><ul><li>lazy object deletion - object is marked as deleted without actually getting deleted</li><li>orphan data - eg an upload failed mid-flight and old parts need to be deleted</li><li>corrupted data - data which failed checksum verification</li></ul><p>The garbage collector is also responsible for reclaiming unused space in replicas. With replication, data is deleted from both primaries and replicas. With erasure coding (8+4), data is deleted from all 12 nodes.</p><p>To facilitate the deletion, we&#39;ll use a process called compaction:</p><ul><li>Garbage collector copies objects which are not deleted from &quot;data/b&quot; to &quot;data/d&quot;</li><li><code>object_mapping</code>table is updated once copying is complete using a database transaction</li><li>To avoid making too many small files, compaction is done on files which grow beyond a certain threshold</li></ul><figure><img src="'+q+'" alt="compaction" tabindex="0" loading="lazy"><figcaption>compaction</figcaption></figure><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap Up</h2><p>Things we covered:</p><ul><li>Designing an S3-like object storage</li><li>Comparing differences between object, block and file storages</li><li>Covered uploading, downloading, listing, versioning of objects in a bucket</li><li>Deep dived in the design - data store and metadata store, replication and erasure coding, multipart uploads, sharding</li></ul>',43);function L(M,B){const i=o("ExternalLinkIcon");return n(),r("div",null,[C,e("ul",null,[U,e("li",null,[t("Erasure coding"),e("a",H,[t("gives us 11 nines of durability"),l(i)]),t("vs 6 nines via replication")]),O]),E])}const G=s(P,[["render",L],["__file","44_s3_like_object_storage.html.vue"]]);export{G as default};
