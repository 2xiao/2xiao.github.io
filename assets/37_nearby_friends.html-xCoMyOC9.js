import{_ as e,o as a,c as i,a as s}from"./app-rLUZugrR.js";const t="/assets/system-design-196-crAjBTY1.png",n="/assets/system-design-197-Hx6XWzSl.png",o="/assets/system-design-198-5KLT9Wti.png",r="/assets/system-design-199-C7aAf2iX.png",l="/assets/system-design-200-dKjLmGrj.png",d="/assets/system-design-201-Y9tO6Gnt.png",c="/assets/system-design-202-dxBjHRGP.png",h="/assets/system-design-203-zSD2q74R.png",p="/assets/system-design-204-i_Eku0Gz.png",u="/assets/system-design-205-fY1Heplb.png",b={},f=s('<h1 id="nearby-friends" tabindex="-1"><a class="header-anchor" href="#nearby-friends" aria-hidden="true">#</a> Nearby Friends</h1><p>This chapter focuses on designing a scalable backend for an application which enables user to share their location and discover friends who are nearby.</p><p>The major difference with the proximity chapter is that in this problem, locations constantly change, whereas in that one, business addresses more or less stay the same.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><p>Some questions to drive the interview:</p><ul><li>C: How geographically close is considered to be &quot;nearby&quot;?</li><li>I: 5 miles, this number should be configurable</li><li>C: Is distance calculated as straight-line distance vs. taking into consideration eg a river in-between friends</li><li>I: Yes, that is a reasonable assumption</li><li>C: How many users does the app have?</li><li>I: 1bil users and 10% of them use the nearby friends feature</li><li>C: Do we need to store location history?</li><li>I: Yes, it can be valuable for eg machine learning</li><li>C: Can we assume inactive friends will disappear from the feature in 10min</li><li>I: Yes</li><li>C: Do we need to worry about GDPR, etc?</li><li>I: No, for simlicity&#39;s sake</li></ul><h3 id="functional-requirements" tabindex="-1"><a class="header-anchor" href="#functional-requirements" aria-hidden="true">#</a> Functional requirements</h3><ul><li>Users should be able to see nearby friends on their mobile app. Each friend has a distance and timestamp, indicating when the location was updated</li><li>Nearby friends list should be updated every few seconds</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>Low latency - it&#39;s important to receive location updates without too much delay</li><li>Reliability - Occassional data point loss is acceptable, but system should be generally available</li><li>Eventual consistency - Location data store doesn&#39;t need strong consistency. Few seconds delay in receiving location data in different replicas is acceptable</li></ul><h3 id="back-of-the-envelope" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope" aria-hidden="true">#</a> Back-of-the-envelope</h3><p>Some estimations to determine potential scale:</p><ul><li>Nearby friends are friends within 5mile radius</li><li>Location refresh interval is 30s. Human walking speed is slow, hence, no need to update location too frequently.</li><li>On average, 100mil users use the feature every day \\w 10% concurrent users, ie 10mil</li><li>On average, a user has 400 friends, all of them use the nearby friends feature</li><li>App displays 20 nearby friends per page</li><li>Location Update QPS = 10mil / 30 == ~334k updates per second</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><p>Before exploring API and data model design, we&#39;ll study the communication protocol we&#39;ll use as it&#39;s less ubiquitous than traditional request-response communication model.</p><h3 id="high-level-design" tabindex="-1"><a class="header-anchor" href="#high-level-design" aria-hidden="true">#</a> High-level design</h3><p>At a high-level we&#39;d want to establish effective message passing between peers. This can be done via a peer-to-peer protocol, but that&#39;s not practical for a mobile app with flaky connection and tight power consumption constraints.</p><p>A more practical approach is to use a shared backend as a fan-out mechanism towards friends you want to reach:</p><figure><img src="'+t+'" alt="fan-out-backend" tabindex="0" loading="lazy"><figcaption>fan-out-backend</figcaption></figure><p>What does the backend do?</p><ul><li>Receives location updates from all active users</li><li>For each location update, find all active users which should receive it and forward it to them</li><li>Do not forward location data if distance between friends is beyond the configured threshold</li></ul><p>This sounds simple but the challenge is to design the system for the scale we&#39;re operating with.</p><p>We&#39;ll start with a simpler design at first and discuss a more advanced approach in the deep dive:</p><figure><img src="'+n+'" alt="simple-high-level-design" tabindex="0" loading="lazy"><figcaption>simple-high-level-design</figcaption></figure><ul><li>The load balancer spreads traffic across rest API servers as well as bidirectional web socket servers</li><li>The rest API servers handles auxiliary tasks such as managing friends, updating profiles, etc</li><li>The websocket servers are stateful servers, which forward location update requests to respective clients. It also manages seeding the mobile client with nearby friends locations at initialization (discussed in detail later).</li><li>Redis location cache is used to store most recent location data for each active user. There is a TTL set on each entry in the cache. When the TTL expires, user is no longer active and their data is removed from the cache.</li><li>User database stores user and friendship data. Either a relational or NoSQL database can be used for this purpose.</li><li>Location history database stores a history of user location data, not necessarily used directly within nearby friends feature, but instead used to track historical data for analytical purposes</li><li>Redis pubsub is used as a lightweight message bus which enables different topics for each user channel for location updates.</li></ul><figure><img src="'+o+'" alt="redis-pubsub-usage" tabindex="0" loading="lazy"><figcaption>redis-pubsub-usage</figcaption></figure><p>In the above example, websocket servers subscribe to channels for the users which are connected to them &amp; forward location updates whenever they receive them to appropriate users.</p><h3 id="periodic-location-update" tabindex="-1"><a class="header-anchor" href="#periodic-location-update" aria-hidden="true">#</a> Periodic location update</h3><p>Here&#39;s how the periodic location update flow works:</p><figure><img src="'+r+'" alt="periodic-location-update" tabindex="0" loading="lazy"><figcaption>periodic-location-update</figcaption></figure><ul><li>Mobile client sends a location update to the load balancer</li><li>Load balancer forwards location update to the websocket server&#39;s persistent connection for that client</li><li>Websocket server saves location data to location history database</li><li>Location data is updated in location cache. Websocket server also saves location data in-memory for subsequent distance calculations for that user</li><li>Websocket server publishes location data in user&#39;s channel via redis pub sub</li><li>Redis pubsub broadcasts location update to all subscribers for that user channel, ie servers responsible for the friends of that user</li><li>Subscribed web socket servers receive location update, calculate which users the update should be sent to and sends it</li></ul><p>Here&#39;s a more detailed version of the same flow:</p><figure><img src="'+l+'" alt="detailed-periodic-location-update" tabindex="0" loading="lazy"><figcaption>detailed-periodic-location-update</figcaption></figure><p>On average, there&#39;s going to be 40 location updates to forward as a user has 400 friends on average and 10% of them are online at a time.</p><h3 id="api-design" tabindex="-1"><a class="header-anchor" href="#api-design" aria-hidden="true">#</a> API Design</h3><p>Websocket Routines we&#39;ll need to support:</p><ul><li>periodic location update - user sends location data to websocket server</li><li>client receives location update - server sends friend location data and timestamp</li><li>websocket client initialization - client sends user location, server sends back nearby friends location data</li><li>Subscribe to a new friend - websocket server sends a friend ID mobile client is supposed to track eg when friend appears online for the first time</li><li>Unsubscribe a friend - websocket server sends a friend ID, mobile client is supposed to unsubscribe from due to eg friend going offline</li></ul><p>HTTP API - traditional request/response payloads for auxiliary responsibilities.</p><h3 id="data-model" tabindex="-1"><a class="header-anchor" href="#data-model" aria-hidden="true">#</a> Data model</h3><ul><li>The location cache will store a mapping between<code>user_id</code>and<code>lat,long,timestamp</code>. Redis is a great choice for this cache as we only care about current location and it supports TTL eviction which we need for our use-case.</li><li>Location history table stores the same data but in a relational table \\w the four columns stated above. Cassandra can be used for this data as it is optimized for write-heavy loads.</li></ul><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>Let&#39;s discuss how we scale the high-level design so that it works at the scale we&#39;re targetting.</p><h3 id="how-well-does-each-component-scale" tabindex="-1"><a class="header-anchor" href="#how-well-does-each-component-scale" aria-hidden="true">#</a> How well does each component scale?</h3><ul><li>API servers - can be easily scaled via autoscaling groups and replicating server instances</li><li>Websocket servers - we can easily scale out the ws servers, but we need to ensure we gracefully shutdown existing connections when tearing down a server. Eg we can mark a server as &quot;draining&quot; in the load balancer and stop sending connections to it, prior to being finally removed from the server pool</li><li>Client initialization - when a client first connects to a server, it fetches the user&#39;s friends, subscribes to their channels on redis pubsub, fetches their location from cache and finally forwards to client</li><li>User database - We can shard the database based on user_id. It might also make sense to expose user/friends data via a dedicated service and API, managed by a dedicated team</li><li>Location cache - We can shard the cache easily by spinning up several redis nodes. Also, the TTL puts a limit on the max memory we could have taken up at a time. But we still want to handle the large write load</li><li>Redis pub/sub server - we leverage the fact that no memory is consumed if there are channels initialized but are not in use. Hence, we can pre-allocate channels for all users who use the nearby friends feature to avoid having to deal with eg bringing up a new channel when a user comes online and notifying active websocket servers</li></ul><h3 id="scaling-deep-dive-on-redis-pub-sub-component" tabindex="-1"><a class="header-anchor" href="#scaling-deep-dive-on-redis-pub-sub-component" aria-hidden="true">#</a> Scaling deep-dive on redis pub/sub component</h3><p>We will need around 200gb of memory to maintain all pub/sub channels. This can be achieved by using 2 redis servers with 100gb each.</p><p>Given that we need to push ~14mil location updates per second, we will however need at least 140 redis servers to handle that amount of load, assuming that a single server can handle ~100k pushes per second.</p><p>Hence, we&#39;ll need a distributed redis server cluster to handle the intense CPU load.</p><p>In order to support a distributed redis cluster, we&#39;ll need to utilize a service discovery component, such as zookeeper or etcd, to keep track of which servers are alive.</p><p>What we need to encode in the service discovery component is this data:</p><figure><img src="'+d+'" alt="channel-distribution-data" tabindex="0" loading="lazy"><figcaption>channel-distribution-data</figcaption></figure><p>Web socket servers use that encoded data, fetched from zookeeper to determine where a particular channel lives. For efficiency, the hash ring data can be cached in-memory on each websocket server.</p><p>In terms of scaling the server cluster up or down, we can setup a daily job to scale the cluster as needed based on historical traffic data. We can also overprovision the cluster to handle spikes in loads.</p><p>The redis cluster can be treated as a stateful storage server as there is some state maintained for the channels and there is a need for coordination with subscribers so that they hand-off to newly provisioned nodes in the cluster.</p><p>We have to be mindful of some potential issues during scaling operations:</p><ul><li>There will be a lot of resubscription requests from the web socket servers due to channels being moved around</li><li>Some location updates might be missed from clients during the operation, which is acceptable for this problem, but we should still minimize it from happening. Consider doing such operation when traffic is at lowest point of the day.</li><li>We can leverage consistent hashing to minimize amount of channels moved in the event of adding/removing servers</li></ul><figure><img src="'+c+'" alt="consistent-hashing" tabindex="0" loading="lazy"><figcaption>consistent-hashing</figcaption></figure><h3 id="adding-removing-friends" tabindex="-1"><a class="header-anchor" href="#adding-removing-friends" aria-hidden="true">#</a> Adding/removing friends</h3><p>Whenever a friend is added/removed, websocket server responsible for affected user needs to subscribe/unsubscribe from the friend&#39;s channel.</p><p>Since the &quot;nearby friends&quot; feature is part of a larger app, we can assume that a callback on the mobile client side can be registered whenever any of the events occur and the client will send a message to the websocket server to do the appropriate action.</p><h3 id="users-with-many-friends" tabindex="-1"><a class="header-anchor" href="#users-with-many-friends" aria-hidden="true">#</a> Users with many friends</h3><p>We can put a cap on the total number of friends one can have, eg facebook has a cap of 5000 max friends.</p><p>The websocket server handling the &quot;whale&quot; user might have a higher load on its end, but as long as we have enough web socket servers, we should be okay.</p><h3 id="nearby-random-person" tabindex="-1"><a class="header-anchor" href="#nearby-random-person" aria-hidden="true">#</a> Nearby random person</h3><p>What if the interviewer wants to update the design to include a feature where we can occasionally see a random person pop up on our nearby friends map?</p><p>One way to handle this is to define a pool of pubsub channels, based on geohash:</p><figure><img src="'+h+'" alt="geohash-pubsub" tabindex="0" loading="lazy"><figcaption>geohash-pubsub</figcaption></figure><p>Anyone within the geohash subscribes to the appropriate channel to receive location updates for random users:</p><figure><img src="'+p+'" alt="location-updates-geohash" tabindex="0" loading="lazy"><figcaption>location-updates-geohash</figcaption></figure><p>We could also subscribe to several geohashes to handle cases where someone is close but in a bordering geohash:</p><figure><img src="'+u+'" alt="geohash-borders" tabindex="0" loading="lazy"><figcaption>geohash-borders</figcaption></figure><h3 id="alternative-to-redis-pub-sub" tabindex="-1"><a class="header-anchor" href="#alternative-to-redis-pub-sub" aria-hidden="true">#</a> Alternative to Redis pub/sub</h3><p>An alternative to using Redis for pub/sub is to leverage Erlang - a general programming language, optimized for distributed computing applications.</p><p>With it, we can spawn millions of small, erland processes which communicate with each other. We can handle both websocket connections and pub/sub channels within the distributed erlang application.</p><p>A challenge with using Erlang, though, is that it&#39;s a niche programming language and it could be hard to source strong erlang developers.</p><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap Up</h2><p>We successfully designed a system, supporting the nearby friends features.</p><p>Core components:</p><ul><li>Web socket servers - real-time comms between client and server</li><li>Redis - fast read and write of location data + pub/sub channels</li></ul><p>We also explored how to scale restful api servers, websocket servers, data layer, redis pub/sub servers and we also explored an alternative to using Redis Pub/Sub. We also explored a &quot;random nearby person&quot; feature.</p>',80),g=[f];function m(w,v){return a(),i("div",null,g)}const k=e(b,[["render",m],["__file","37_nearby_friends.html.vue"]]);export{k as default};
