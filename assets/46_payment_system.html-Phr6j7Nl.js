import{_ as e,o as t,c as a,a as i}from"./app-rLUZugrR.js";const n="/assets/system-design-390-Cqd_SdJy.png",s="/assets/system-design-391-y_llrYF2.png",r="/assets/system-design-392-pjXQWHO_.png",o="/assets/system-design-393-1tWi5yQy.png",l="/assets/system-design-394-7w8tZMCa.png",c="/assets/system-design-395-efOtJwuc.png",d="/assets/system-design-396-OONcwM90.png",p="/assets/system-design-397-RTfhkFOo.png",h="/assets/system-design-398-Xyp3kULA.png",m="/assets/system-design-399--1_btHtf.png",u={},y=i('<h1 id="payment-system" tabindex="-1"><a class="header-anchor" href="#payment-system" aria-hidden="true">#</a> Payment System</h1><p>We&#39;ll design a payment system in this chapter, which underpins all of modern e-commerce.</p><p>A payment system is used to settle financial transactions, transferring monetary value.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><ul><li>C: What kind of payment system are we building?</li><li>I: A payment backend for an e-commerce system, similar to Amazon.com. It handles everything related to money movement.</li><li>C: What payment options are supported - Credit cards, PayPal, bank cards, etc?</li><li>I: The system should support all these options in real life. For the purposes of the interview, we can use credit card payments.</li><li>C: Do we handle credit card processing ourselves?</li><li>I: No, we use a third-party provider like Stripe, Braintree, Square, etc.</li><li>C: Do we store credit card data in our system?</li><li>I: Due to compliance reasons, we do not store credit card data directly in our systems. We rely on third-party payment processors.</li><li>C: Is the application global? Do we need to support different currencies and international payments?</li><li>I: The application is global, but we assume only one currency is used for the purposes of the interview.</li><li>C: How many payment transactions per day do we support?</li><li>I: 1mil transactions per day.</li><li>C: Do we need to support the payout flow to eg payout to payers each month?</li><li>I: Yes, we need to support that</li><li>C: Is there anything else I should pay attention to?</li><li>I: We need to support reconciliations to fix any inconsistencies in communicating with internal and external systems.</li></ul><h3 id="functional-requirements" tabindex="-1"><a class="header-anchor" href="#functional-requirements" aria-hidden="true">#</a> Functional requirements</h3><ul><li>Pay-in flow - payment system receives money from customers on behalf of merchants</li><li>Pay-out flow - payment system sends money to sellers around the world</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>Reliability and fault-tolerance. Failed payments need to be carefully handled</li><li>A reconciliation between internal and external systems needs to be setup.</li></ul><h3 id="back-of-the-envelope-estimation" tabindex="-1"><a class="header-anchor" href="#back-of-the-envelope-estimation" aria-hidden="true">#</a> Back-of-the-envelope estimation</h3><p>The system needs to process 1mil transactions per day, which is 10 transactions per second.</p><p>This is not a high throughput for any database system, so it&#39;s not the focus of this interview.</p><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><p>At a high-level, we have three actors, participating in money movement:</p><figure><img src="'+n+'" alt="high-level-flow" tabindex="0" loading="lazy"><figcaption>high-level-flow</figcaption></figure><h3 id="pay-in-flow" tabindex="-1"><a class="header-anchor" href="#pay-in-flow" aria-hidden="true">#</a> Pay-in flow</h3><p>Here&#39;s the high-level overview of the pay-in flow:</p><figure><img src="'+s+`" alt="pay-in-flow-high-level" tabindex="0" loading="lazy"><figcaption>pay-in-flow-high-level</figcaption></figure><ul><li>Payment service - accepts payment events and coordinates the payment process. It typically also does a risk check using a third-party provider for AML violations or criminal activity.</li><li>Payment executor - executes a single payment order via the Payment Service Provider (PSP). Payment events may contain several payment orders.</li><li>Payment service provider (PSP) - moves money from one account to another, eg from buyer&#39;s credit card account to e-commerce site&#39;s bank account.</li><li>Card schemes - organizations that process credit card operations, eg Visa MasterCard, etc.</li><li>Ledger - keeps financial record of all payment transactions.</li><li>Wallet - keeps the account balance for all merchants.</li></ul><p>Here&#39;s an example pay-in flow:</p><ul><li>user clicks &quot;place order&quot; and a payment event is sent to the payment service</li><li>payment service stores the event in its database</li><li>payment service calls the payment executor for all payment orders, part of that payment event</li><li>payment executor stores the payment order in its database</li><li>payment executor calls external PSP to process the credit card payment</li><li>After the payment executor processes the payment, the payment service updates the wallet to record how much money the seller has</li><li>wallet service stores updated balance information in its database</li><li>payment service calls the ledger to record all money movements</li></ul><h3 id="apis-for-payment-service" tabindex="-1"><a class="header-anchor" href="#apis-for-payment-service" aria-hidden="true">#</a> APIs for payment service</h3><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>POST /v1/payments
{
  &quot;buyer_info&quot;: {...},
  &quot;checkout_id&quot;: &quot;some_id&quot;,
  &quot;credit_card_info&quot;: {...},
  &quot;payment_orders&quot;: [{...}, {...}, {...}]
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Example<code>payment_order</code>:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
  &quot;seller_account&quot;: &quot;SELLER_IBAN&quot;,
  &quot;amount&quot;: &quot;3.15&quot;,
  &quot;currency&quot;: &quot;USD&quot;,
  &quot;payment_order_id&quot;: &quot;globally_unique_payment_id&quot;
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Caveats:</p><ul><li>The<code>payment_order_id</code>is forwarded to the PSP to deduplicate payments, ie it is the idempotency key.</li><li>The amount field is<code>string</code>as<code>double</code>is not appropriate for representing monetary values.</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>GET /v1/payments/{:id}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This endpoint returns the execution status of a single payment, based on the<code>payment_order_id</code>.</p><h3 id="payment-service-data-model" tabindex="-1"><a class="header-anchor" href="#payment-service-data-model" aria-hidden="true">#</a> Payment service data model</h3><p>We need to maintain two tables -<code>payment_events</code>and<code>payment_orders</code>.</p><p>For payments, performance is typically not an important factor. Strong consistency, however, is.</p><p>Other considerations for choosing the database:</p><ul><li>Strong market of DBAs to hire to administer the databaseS</li><li>Proven track-record where the database has been used by other big financial institutions</li><li>Richness of supporting tools</li><li>Traditional SQL over NoSQL/NewSQL for its ACID guarantees</li></ul><p>Here&#39;s what the<code>payment_events</code>table contains:</p><ul><li><code>checkout_id</code>- string, primary key</li><li><code>buyer_info</code>- string (personal note - prob a foreign key to another table is more appropriate)</li><li><code>seller_info</code>- string (personal note - same remark as above)</li><li><code>credit_card_info</code>- depends on card provider</li><li><code>is_payment_done</code>- boolean</li></ul><p>Here&#39;s what the<code>payment_orders</code>table contains:</p><ul><li><code>payment_order_id</code>- string, primary key</li><li><code>buyer_account</code>- string</li><li><code>amount</code>- string</li><li><code>currency</code>- string</li><li><code>checkout_id</code>- string, foreign key</li><li><code>payment_order_status</code>- enum (<code>NOT_STARTED</code>,<code>EXECUTING</code>,<code>SUCCESS</code>,<code>FAILED</code>)</li><li><code>ledger_updated</code>- boolean</li><li><code>wallet_updated</code>- boolean</li></ul><p>Caveats:</p><ul><li>there are many payment orders, linked to a given payment event</li><li>we don&#39;t need the<code>seller_info</code>for the pay-in flow. That&#39;s required on pay-out only</li><li><code>ledger_updated</code>and<code>wallet_updated</code>are updated when the respective service is called to record the result of a payment</li><li>payment transitions are managed by a background job, which checks updates of in-flight payments and triggers an alert if a payment is not processed in a reasonable timeframe</li></ul><h3 id="double-entry-ledger-system" tabindex="-1"><a class="header-anchor" href="#double-entry-ledger-system" aria-hidden="true">#</a> Double-entry ledger system</h3><p>The double-entry accounting mechanism is key to any payment system. It is a mechanism of tracking money movements by always applying money operations to two accounts, where one&#39;s account balance increases (credit) and the other decreases (debit):</p><table><thead><tr><th>Account</th><th>Debit</th><th>Credit</th></tr></thead><tbody><tr><td>buyer</td><td>$1</td><td></td></tr><tr><td>seller</td><td></td><td>$1</td></tr></tbody></table><p>Sum of all transaction entries is always zero. This mechanism provides end-to-end traceability of all money movements within the system.</p><h3 id="hosted-payment-page" tabindex="-1"><a class="header-anchor" href="#hosted-payment-page" aria-hidden="true">#</a> Hosted payment page</h3><p>To avoid storing credit card information and having to comply with various heavy regulations, most companies prefer utilizing a widget, provided by PSPs, which store and handle credit card payments for you:</p><figure><img src="`+r+'" alt="hosted-payment-page" tabindex="0" loading="lazy"><figcaption>hosted-payment-page</figcaption></figure><h3 id="pay-out-flow" tabindex="-1"><a class="header-anchor" href="#pay-out-flow" aria-hidden="true">#</a> Pay-out flow</h3><p>The components of the pay-out flow are very similar to the pay-in flow.</p><p>Main differences:</p><ul><li>money is moved from e-commerce site&#39;s bank account to merchant&#39;s bank account</li><li>we can utilize a third-party account payable provider such as Tipalti</li><li>There&#39;s a lot of bookkeeping and regulatory requirements to handle with regards to pay-outs as well</li></ul><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>This section focuses on making the system faster, more robust and secure.</p><h3 id="psp-integration" tabindex="-1"><a class="header-anchor" href="#psp-integration" aria-hidden="true">#</a> PSP Integration</h3><p>If our system can directly connect to banks or card schemes, payment can be made without a PSP. These kinds of connections are very rare and uncommon, typically done at large companies which can justify the investment.</p><p>If we go down the traditional route, a PSP can be integrated in one of two ways:</p><ul><li>Through API, if our payment system can collect payment information</li><li>Through a hosted payment page to avoid dealing with payment information regulations</li></ul><p>Here&#39;s how the hosted payment page workflow works:</p><figure><img src="'+o+'" alt="hosted-payment-page-workflow" tabindex="0" loading="lazy"><figcaption>hosted-payment-page-workflow</figcaption></figure><ul><li>User clicks &quot;checkout&quot; button in the browser</li><li>Client calls the payment service with the payment order information</li><li>After receiving payment order information, the payment service sends a payment registration request to the PSP.</li><li>The PSP receives payment info such as currency, amount, expiration, etc, as well as a UUID for idempotency purposes. Typically the UUID of the payment order.</li><li>The PSP returns a token back which uniquely identifies the payment registration. The token is stored in the payment service database.</li><li>Once token is stored, the user is served with a PSP-hosted payment page. It is initialized using the token as well as a redirect URL for success/failure.</li><li>User fills in payment details on the PSP page, PSP processes payment and returns the payment status</li><li>User is now redirected back to the redirectURL. Example redirect url -<code>https://your-company.com/?tokenID=JIOUIQ123NSF&amp;payResult=X324FSa</code></li><li>Asynchronously, the PSP calls our payment service via a webhook to inform our backend of the payment result</li><li>Payment service records the payment result based on the webhook received</li></ul><h3 id="reconciliation" tabindex="-1"><a class="header-anchor" href="#reconciliation" aria-hidden="true">#</a> Reconciliation</h3><p>The previous section explains the happy path of a payment. Unhappy paths are detected and reconciled using a background reconciliation process.</p><p>Every night, the PSP sends a settlement file which our system uses to compare the external system&#39;s state against our internal system&#39;s state.</p><figure><img src="'+l+'" alt="settlement-report" tabindex="0" loading="lazy"><figcaption>settlement-report</figcaption></figure><p>This process can also be used to detect internal inconsistencies between eg the ledger and the wallet services.</p><p>Mismatches are handled manually by the finance team. Mismatches are handled as:</p><ul><li>classifiable, hence, it is a known mismatch which can be adjusted using a standard procedure</li><li>classifiable, but can&#39;t be automated. Manually adjusted by the finance team</li><li>unclassifiable. Manually investigated and adjusted by the finance team</li></ul><h3 id="handling-payment-processing-delays" tabindex="-1"><a class="header-anchor" href="#handling-payment-processing-delays" aria-hidden="true">#</a> Handling payment processing delays</h3><p>There are cases, where a payment can take hours to complete, although it typically takes seconds.</p><p>This can happen due to:</p><ul><li>a payment being flagged as high-risk and someone has to manually review it</li><li>credit card requires extra protection, eg 3D Secure Authentication, which requires extra details from card holder to complete</li></ul><p>These situations are handled by:</p><ul><li>waiting for the PSP to send us a webhook when a payment is complete or polling its API if the PSP doesn&#39;t provide webhooks</li><li>showing a &quot;pending&quot; status to the user and giving them a page, where they can check-in for payment updates. We could also send them an email once their payment is complete</li></ul><h3 id="communication-among-internal-services" tabindex="-1"><a class="header-anchor" href="#communication-among-internal-services" aria-hidden="true">#</a> Communication among internal services</h3><p>There are two types of communication patterns services use to communicate with one another - synchronous and asynchronous.</p><p>Synchronous communication (ie HTTP) works well for small-scale systems, but suffers as scale increases:</p><ul><li>low performance - request-response cycle is long as more services get involved in the call chain</li><li>poor failure isolation - if PSPs or any other service fails, user will not receive a response</li><li>tight coupling - sender needs to know the receiver</li><li>hard to scale - not easy to support sudden increase in traffic due to not having a buffer</li></ul><p>Asynchronous communication can be divided into two categories.</p><p>Single receiver - multiple receivers subscribe to the same topic and messages are processed only once:</p><figure><img src="'+c+'" alt="single-receiver" tabindex="0" loading="lazy"><figcaption>single-receiver</figcaption></figure><p>Multiple receivers - multiple receivers subscribe to the same topic, but messages are forwarded to all of them:</p><figure><img src="'+d+'" alt="multiple-receiver" tabindex="0" loading="lazy"><figcaption>multiple-receiver</figcaption></figure><p>Latter model works well for our payment system as a payment can trigger multiple side effects, handled by different services.</p><p>In a nutshell, synchronous communication is simpler but doesn&#39;t allow services to be autonomous. Async communication trades simplicity and consistency for scalability and resilience.</p><h3 id="handling-failed-payments" tabindex="-1"><a class="header-anchor" href="#handling-failed-payments" aria-hidden="true">#</a> Handling failed payments</h3><p>Every payment system needs to address failed payments. Here are some of the mechanism we&#39;ll use to achieve that:</p><ul><li>Tracking payment state - whenever a payment fails, we can determine whether to retry/refund based on the payment state.</li><li>Retry queue - payments which we&#39;ll retry are published to a retry queue</li><li>Dead-letter queue - payments which have terminally failed are pushed to a dead-letter queue, where the failed payment can be debugged and inspected.</li></ul><figure><img src="'+p+'" alt="failed-payments" tabindex="0" loading="lazy"><figcaption>failed-payments</figcaption></figure><h3 id="exactly-once-delivery" tabindex="-1"><a class="header-anchor" href="#exactly-once-delivery" aria-hidden="true">#</a> Exactly-once delivery</h3><p>We need to ensure a payment gets processed exactly-once to avoid double-charging a customer.</p><p>An operation is executed exactly-once if it is executed at-least-once and at-most-once at the same time.</p><p>To achieve the at-least-once guarantee, we&#39;ll use a retry mechanism:</p><figure><img src="'+h+'" alt="retry-mechanism" tabindex="0" loading="lazy"><figcaption>retry-mechanism</figcaption></figure><p>Here are some common strategies on deciding the retry intervals:</p><ul><li>immediate retry - client immediately sends another request after failure</li><li>fixed intervals - wait a fixed amount of time before retrying a payment</li><li>incremental intervals - incrementally increase retry interval between each retry</li><li>exponential back-off - double retry interval between subsequent retries</li><li>cancel - client cancels the request. This happens when the error is terminal or retry threshold is reached</li></ul><p>As a rule of thumb, default to an exponential back-off retry strategy. A good practice is for the server to specify a retry interval using a<code>Retry-After</code>header.</p><p>An issue with retries is that the server can potentially process a payment twice:</p><ul><li>client clicks the &quot;pay button&quot; twice, hence, they are charged twice</li><li>payment is successfully processed by PSP, but not by downstream services (ledger, wallet). Retry causes the payment to be processed by the PSP again</li></ul><p>To address the double payment problem, we need to use an idempotency mechanism - a property that an operation applied multiple times is processed only once.</p><p>From an API perspective, clients can make multiple calls which produce the same result. Idempotency is managed by a special header in the request (eg<code>idempotency-key</code>), which is typically a UUID.</p><figure><img src="'+m+'" alt="idempotency-example" tabindex="0" loading="lazy"><figcaption>idempotency-example</figcaption></figure><p>Idempotency can be achieved using the database&#39;s mechanism of adding unique key constraints:</p><ul><li>server attempts to insert a new row in the database</li><li>the insertion fails due to a unique key constraint violation</li><li>server detects that error and instead returns the existing object back to the client</li></ul><p>Idempotency is also applied at the PSP side, using the nonce, which was previously discussed. PSPs will take care to not process payments with the same nonce twice.</p><h3 id="consistency" tabindex="-1"><a class="header-anchor" href="#consistency" aria-hidden="true">#</a> Consistency</h3><p>There are several stateful services called throughout a payment&#39;s lifecycle - PSP, ledger, wallet, payment service.</p><p>Communication between any two services can fail. We can ensure eventual data consistency between all services by implementing exactly-once processing and reconciliation.</p><p>If we use replication, we&#39;ll have to deal with replication lag, which can lead to users observing inconsistent data between primary and replica databases.</p><p>To mitigate that, we can serve all reads and writes from the primary database and only utilize replicas for redundancy and fail-over. Alternatively, we can ensure replicas are always in-sync by utilizing a consensus algorithm such as Paxos or Raft. We could also use a consensus-based distributed database such as YugabyteDB or CockroachDB.</p><h3 id="payment-security" tabindex="-1"><a class="header-anchor" href="#payment-security" aria-hidden="true">#</a> Payment security</h3><p>Here are some mechanisms we can use to ensure payment security:</p><ul><li>Request/response eavesdropping - we can use HTTPS to secure all communication</li><li>Data tampering - enforce encryption and integrity monitoring</li><li>Man-in-the-middle attacks - use SSL \\w certificate pinning</li><li>Data loss - replicate data across multiple regions and take data snapshots</li><li>DDoS attack - implement rate limiting and firewall</li><li>Card theft - use tokens instead of storing real card information in our system</li><li>PCI compliance - a security standard for organizations which handle branded credit cards</li><li>Fraud - address verification, card verification value (CVV), user behavior analysis, etc</li></ul><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap Up</h2><p>Other talking points:</p><ul><li>Monitoring and alerting</li><li>Debugging tools - we need tools which make it easy to understand why a payment has failed</li><li>Currency exchange - important when designing a payment system for international use</li><li>Geography - different regions might have different payment methods</li><li>Cash payment - very common in places like India and Brazil</li><li>Google/Apple Pay integration</li></ul>',115),g=[y];function f(v,b){return t(),a("div",null,g)}const _=e(u,[["render",f],["__file","46_payment_system.html.vue"]]);export{_ as default};
