import{_ as s,r as n,o as r,c as o,b as t,d as e,e as l,a as i}from"./app-53EDpe6z.js";const d="/assets/system-design-262-f9ZCy6FR.png",c="/assets/system-design-263-IFfoV_dn.png",h="/assets/system-design-264-jRd5b_ip.png",m="/assets/system-design-265-5QtJGs7k.png",u="/assets/system-design-266-vtFWiNDP.png",p="/assets/system-design-267-OGlHt-36.png",g="/assets/system-design-268-TV8OZPUk.png",f="/assets/system-design-269-4iaGcp6b.png",b="/assets/system-design-270-8SyLU_FS.png",v="/assets/system-design-271-mErhqm5D.png",y="/assets/system-design-272-Hw3y55d6.png",w="/assets/system-design-273-MB9_Ygig.png",x="/assets/system-design-274-XLi6BvDT.png",_="/assets/system-design-275-AuEdQnR4.png",T="/assets/system-design-276-hE1FRar3.png",k="/assets/system-design-277-_ZuOKM_A.png",q="/assets/system-design-278-TKH0jotm.png",z="/assets/system-design-279-1-ATZsAo.png",D="/assets/system-design-280-zTwe_nwJ.png",I="/assets/system-design-281-e8Axbsky.png",P="/assets/system-design-282-dDgyFWaO.png",A="/assets/system-design-283-p-E4PYAb.png",S={},C=i('<h1 id="metrics-monitoring-and-alerting-system" tabindex="-1"><a class="header-anchor" href="#metrics-monitoring-and-alerting-system" aria-hidden="true">#</a> Metrics Monitoring and Alerting System</h1><p>This chapter focuses on designing a highly scalable metrics monitoring and alerting system, which is critical for ensuring high availability and reliability.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the Problem and Establish Design Scope</h2><p>A metrics monitoring system can mean a lot of different things - eg you don&#39;t want to design a logs aggregation system, when the interviewer is interested in infra metrics only.</p><p>Let&#39;s try to understand the problem first:</p><ul><li>C: Who are we building the system for? An in-house monitoring system for a big tech company or a SaaS like DataDog?</li><li>I: We are building for internal use only.</li><li>C: Which metrics do we want to collect?</li><li>I: Operational system metrics - CPU load, Memory, Data disk space. But also high-level metrics like requests per second. Business metrics are not in scope.</li><li>C: What is the scale of the infrastructure we&#39;re monitoring?</li><li>I: 100mil daily active users, 1000 server pools, 100 machines per pool</li><li>C: How long should we keep the data?</li><li>I: Let&#39;s assume 1y retention.</li><li>C: May we reduce metrics data resolution for long-term storage?</li><li>I: Keep newly received metrics for 7 days. Roll them up to 1m resolution for next 30 days. Further roll them up to 1h resolution after 30 days.</li><li>C: What are the supported alert channels?</li><li>I: Email, phone, PagerDuty or webhooks.</li><li>C: Do we need to collect logs such as error or access logs?</li><li>I: No</li><li>C: Do we need to support distributed system tracing?</li><li>I: No</li></ul><h3 id="high-level-requirements-and-assumptions" tabindex="-1"><a class="header-anchor" href="#high-level-requirements-and-assumptions" aria-hidden="true">#</a> High-level requirements and assumptions</h3><p>The infrastructure being monitored is large-scale:</p><ul><li>100mil DAU</li><li>1000 server pools _ 100 machines _ ~100 metrics per machine -&gt; ~10mil metrics</li><li>1-year data retention</li><li>Data retention policy - raw for 7d, 1-minute resolution for 30d, 1h resolution for 1y</li></ul><p>A variety of metrics can be monitored:</p><ul><li>CPU load</li><li>Request count</li><li>Memory usage</li><li>Message count in message queues</li></ul><h3 id="non-functional-requirements" tabindex="-1"><a class="header-anchor" href="#non-functional-requirements" aria-hidden="true">#</a> Non-functional requirements</h3><ul><li>Scalability - System should be scalable to accommodate more metrics and alerts</li><li>Low latency - System needs to have low query latency for dashboards and alerts</li><li>Reliability - System should be highly reliable to avoid missing critical alerts</li><li>Flexibility - System should be able to easily integrate new technologies in the future</li></ul><p>What requirements are out of scope?</p><ul><li>Log monitoring - the ELK stack is very popular for this use-case</li><li>Distributed system tracing - this refers to collecting data about a request lifecycle as it flows through multiple services within the system</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose High-Level Design and Get Buy-In</h2><h3 id="fundamentals" tabindex="-1"><a class="header-anchor" href="#fundamentals" aria-hidden="true">#</a> Fundamentals</h3><p>There are five core components involved in a metrics monitoring and alerting system:</p><figure><img src="'+d+'" alt="metrics-monitoring-core-components" tabindex="0" loading="lazy"><figcaption>metrics-monitoring-core-components</figcaption></figure><ul><li>Data collection - collect metrics data from different sources</li><li>Data transmission - transfer data from sources to the metrics monitoring system</li><li>Data storage - organize and store incoming data</li><li>Alerting - Analyze incoming data, detect anomalies and generate alerts</li><li>Visualization - Present data in graphs, charts, etc</li></ul><h3 id="data-model" tabindex="-1"><a class="header-anchor" href="#data-model" aria-hidden="true">#</a> Data model</h3><p>Metrics data is usually recorded as a time-series, which contains a set of values with timestamps. The series can be identified by name and an optional set of tags.</p><p>Example 1 - What is the CPU load on production server instance i631 at 20:00?</p><figure><img src="'+c+'" alt="metrics-example-1" tabindex="0" loading="lazy"><figcaption>metrics-example-1</figcaption></figure><p>The data can be identified by the following table:</p><figure><img src="'+h+`" alt="metrics-example-1-data" tabindex="0" loading="lazy"><figcaption>metrics-example-1-data</figcaption></figure><p>The time series is identified by the metric name, labels and a single point in at a specific time.</p><p>Example 2 - What is the average CPU load across all web servers in the us-west region for the last 10min?</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>CPU.load host=webserver01,region=us-west 1613707265 50

CPU.load host=webserver01,region=us-west 1613707265 62

CPU.load host=webserver02,region=us-west 1613707265 43

CPU.load host=webserver02,region=us-west 1613707265 53

...

CPU.load host=webserver01,region=us-west 1613707265 76

CPU.load host=webserver01,region=us-west 1613707265 83
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This is an example data we might pull from storage to answer that question. The average CPU load can be calculated by averaging the values in the last column of the rows.</p><p>The format shown above is called the line protocol and is used by many popular monitoring software in the market - eg Prometheus, OpenTSDB.</p><p>What every time series consists of:</p><figure><img src="`+m+'" alt="time-series-data-example" tabindex="0" loading="lazy"><figcaption>time-series-data-example</figcaption></figure><p>A good way to visualize how data looks like:</p><figure><img src="'+u+'" alt="time-series-data-viz" tabindex="0" loading="lazy"><figcaption>time-series-data-viz</figcaption></figure><ul><li>The x axis is the time</li><li>the y axis is the dimension you&#39;re querying - eg metric name, tag, etc.</li></ul><p>The data access pattern is write-heavy and spiky reads as we collect a lot of metrics, but they are infrequently accessed, although in bursts when eg there are ongoing incidents.</p><p>The data storage system is the heart of this design.</p><ul><li>It is not recommended to use a general-purpose database for this problem, although you could achieve good scale \\w expert-level tuning.</li><li>Using a NoSQL database can work in theory, but it is hard to devise a scalable schema for effectively storing and querying time-series data.</li></ul><p>There are many databases, specifically tailored for storing time-series data. Many of them support custom query interfaces which allow for effective querying of time-series data.</p><ul><li>OpenTSDB is a distributed time-series database, but it is based on Hadoop and HBase. If you don&#39;t have that infrastructure provisioned, it would be hard to use this tech.</li><li>Twitter uses MetricsDB, while Amazon offers Timestream.</li><li>The two most popular time-series databases are InfluxDB and Prometheus.</li><li>They are designed to store large volumes of time-series data. Both of them are based on in-memory cache + on-disk storage.</li></ul><p>Example scale of InfluxDB - more than 250k writes per second when provisioned with 8 cores and 32gb RAM:</p><figure><img src="'+p+'" alt="influxdb-scale" tabindex="0" loading="lazy"><figcaption>influxdb-scale</figcaption></figure><p>It is not expected for you to understand the internals of a metrics database as it is niche knowledge. You might be asked only if you&#39;ve mentioned it on your resume.</p><p>For the purposes of the interview, it is sufficient to understand that metrics are time-series data and to be aware of popular time-series databases, like InfluxDB.</p><p>One nice feature of time-series databases is the efficient aggregation and analysis of large amounts of time-series data by labels. InfluxDB, for example, builds indexes for each label.</p><p>It is critical, however, to keep the cardinality of labels low - ie, not using too many unique labels.</p><h3 id="high-level-design" tabindex="-1"><a class="header-anchor" href="#high-level-design" aria-hidden="true">#</a> High-level Design</h3><figure><img src="'+g+'" alt="high-level-design" tabindex="0" loading="lazy"><figcaption>high-level-design</figcaption></figure><ul><li>Metrics source - can be application servers, SQL databases, message queues, etc.</li><li>Metrics collector - Gathers metrics data and writes to time-series database</li><li>Time-series database - stores metrics as time-series. Provides a custom query interface for analyzing large amounts of metrics.</li><li>Query service - Makes it easy to query and retrieve data from the time-series DB. Could be replaced entirely by the DB&#39;s interface if it&#39;s sufficiently powerful.</li><li>Alerting system - Sends alert notifications to various alerting destinations.</li><li>Visualization system - Shows metrics in the form of graphs/charts.</li></ul><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design Deep Dive</h2><p>Let&#39;s deep dive into several of the more interesting parts of the system.</p><h3 id="metrics-collection" tabindex="-1"><a class="header-anchor" href="#metrics-collection" aria-hidden="true">#</a> Metrics collection</h3><p>For metrics collection, occasional data loss is not critical. It&#39;s acceptable for clients to fire and forget.</p><figure><img src="'+f+'" alt="metrics-collection" tabindex="0" loading="lazy"><figcaption>metrics-collection</figcaption></figure><p>There are two ways to implement metrics collection - pull or push.</p><p>Here&#39;s how the pull model might look like:</p><figure><img src="'+b+'" alt="pull-model-example" tabindex="0" loading="lazy"><figcaption>pull-model-example</figcaption></figure><p>For this solution, the metrics collector needs to maintain an up-to-date list of services and metrics endpoints. We can use Zookeeper or etcd for that purpose - service discovery.</p><p>Service discovery contains contains configuration rules about when and where to collect metrics from:</p><figure><img src="'+v+'" alt="service-discovery-example" tabindex="0" loading="lazy"><figcaption>service-discovery-example</figcaption></figure><p>Here&#39;s a detailed explanation of the metrics collection flow:</p><figure><img src="'+y+'" alt="metrics-collection-flow" tabindex="0" loading="lazy"><figcaption>metrics-collection-flow</figcaption></figure><ul><li>Metrics collector fetches configuration metadata from service discovery. This includes pulling interval, IP addresses, timeout &amp; retry params.</li><li>Metrics collector pulls metrics data via a pre-defined http endpoint (eg<code>/metrics</code>). This is typically done by a client library.</li><li>Alternatively, the metrics collector can register a change event notification with the service discovery to be notified once the service endpoint changes.</li><li>Another option is for the metrics collector to periodically poll for metrics endpoint configuration changes.</li></ul><p>At our scale, a single metrics collector is not enough. There must be multiple instances. However, there must also be some kind of synchronization among them so that two collectors don&#39;t collect the same metrics twice.</p><p>One solution for this is to position collectors and servers on a consistent hash ring and associate a set of servers with a single collector only:</p><figure><img src="'+w+'" alt="consistent-hash-ring" tabindex="0" loading="lazy"><figcaption>consistent-hash-ring</figcaption></figure><p>With the push model, on the other hand, services push their metrics to the metrics collector proactively:</p><figure><img src="'+x+'" alt="push-model-example" tabindex="0" loading="lazy"><figcaption>push-model-example</figcaption></figure><p>In this approach, typically a collection agent is installed alongside service instances. The agent collects metrics from the server and pushes them to the metrics collector.</p><figure><img src="'+_+'" alt="metrics-collector-agent" tabindex="0" loading="lazy"><figcaption>metrics-collector-agent</figcaption></figure><p>With this model, we can potentially aggregate metrics before sending them to the collector, which reduces the volume of data processed by the collector.</p><p>On the flip side, metrics collector can reject push requests as it can&#39;t handle the load. It is important, hence, to add the collector to an auto-scaling group behind a load balancer.</p><p>so which one is better? There are trade-offs between both approaches and different systems use different approaches:</p><ul><li>Prometheus uses a pull architecture</li><li>Amazon Cloud Watch and Graphite use a push architecture</li></ul><p>Here are some of the main differences between push and pull:</p><table><thead><tr><th></th><th>Pull</th><th>Push</th></tr></thead><tbody><tr><td>Easy debugging</td><td>The /metrics endpoint on application servers used for pulling metrics can be used to view metrics at any time. You can even do this on your laptop. Pull wins.</td><td>If the metrics collector doesn’t receive metrics, the problem might be caused by network issues.</td></tr><tr><td>Health check</td><td>If an application server doesn’t respond to the pull, you can quickly figure out if an application server is down. Pull wins.</td><td>If the metrics collector doesn’t receive metrics, the problem might be caused by network issues.</td></tr><tr><td>Short-lived jobs</td><td></td><td>Some of the batch jobs might be short-lived and don’t last long enough to be pulled. Push wins. This can be fixed by introducing push gateways for the pull model [22].</td></tr><tr><td>Firewall or complicated network setups</td><td>Having servers pulling metrics requires all metric endpoints to be reachable. This is potentially problematic in multiple data center setups. It might require a more elaborate network infrastructure.</td><td>If the metrics collector is set up with a load balancer and an auto-scaling group, it is possible to receive data from anywhere. Push wins.</td></tr><tr><td>Performance</td><td>Pull methods typically use TCP.</td><td>Push methods typically use UDP. This means the push method provides lower-latency transports of metrics. The counterargument here is that the effort of establishing a TCP connection is small compared to sending the metrics payload.</td></tr><tr><td>Data authenticity</td><td>Application servers to collect metrics from are defined in config files in advance. Metrics gathered from those servers are guaranteed to be authentic.</td><td>Any kind of client can push metrics to the metrics collector. This can be fixed by whitelisting servers from which to accept metrics, or by requiring authentication.</td></tr></tbody></table><p>There is no clear winner. A large organization probably needs to support both. There might not be a way to install a push agent in the first place.</p><h3 id="scale-the-metrics-transmission-pipeline" tabindex="-1"><a class="header-anchor" href="#scale-the-metrics-transmission-pipeline" aria-hidden="true">#</a> Scale the metrics transmission pipeline</h3><figure><img src="'+T+'" alt="metrics-transmission-pipeline" tabindex="0" loading="lazy"><figcaption>metrics-transmission-pipeline</figcaption></figure><p>The metrics collector is provisioned in an auto-scaling group, regardless if we use the push or pull model.</p><p>There is a chance of data loss if the time-series DB is down, however. To mitigate this, we&#39;ll provision a queuing mechanism:</p><figure><img src="'+k+'" alt="queuing-mechanism" tabindex="0" loading="lazy"><figcaption>queuing-mechanism</figcaption></figure><ul><li>Metrics collectors push metrics data into kafka</li><li>Consumers or stream processing services such as Apache Storm, Flink or Spark process the data and push it to the time-series DB</li></ul><p>This approach has several advantages:</p><ul><li>Kafka is used as a highly-reliable and scalable distributed message platform</li><li>It decouples data collection and data processing from one another</li><li>It can prevent data loss by retaining the data in Kafka</li></ul><p>Kafka can be configured with one partition per metric name, so that consumers can aggregate data by metric names. To scale this, we can further partition by tags/labels and categorize/prioritize metrics to be collected first.</p><figure><img src="'+q+'" alt="metrics-collection-kafka" tabindex="0" loading="lazy"><figcaption>metrics-collection-kafka</figcaption></figure>',88),B={href:"https://www.vldb.org/pvldb/vol8/p1816-teller.pdf",target:"_blank",rel:"noopener noreferrer"},M=i('<h3 id="where-aggregations-can-happen" tabindex="-1"><a class="header-anchor" href="#where-aggregations-can-happen" aria-hidden="true">#</a> Where aggregations can happen</h3><p>Metrics can be aggregated at several places. There are trade-offs between different choices:</p><ul><li>Collection agent - client-side collection agent only supports simple aggregation logic. Eg collect a counter for 1m and send it to the metrics collector.</li><li>Ingestion pipeline - To aggregate data before writing to the DB, we need a stream processing engine like Flink. This reduces write volume, but we lose data precision as we don&#39;t store raw data.</li><li>Query side - We can aggregate data when we run queries via our visualization system. There is no data loss, but queries can be slow due to a lot of data processing.</li></ul><h3 id="query-service" tabindex="-1"><a class="header-anchor" href="#query-service" aria-hidden="true">#</a> Query Service</h3><p>Having a separate query service from the time-series DB decouples the visualization and alerting system from the database, which enables us to decouple the DB from clients and change it at will.</p><p>We can add a Cache layer here to reduce the load to the time-series database:</p><figure><img src="'+z+`" alt="cache-layer-query-service" tabindex="0" loading="lazy"><figcaption>cache-layer-query-service</figcaption></figure><p>We can also avoid adding a query service altogether as most visualization and alerting systems have powerful plugins to integrate with most time-series databases. With a well-chosen time-series DB, we might not need to introduce our own caching layer as well.</p><p>Most time-series DBs don&#39;t support SQL simply because it is ineffective for querying time-series data. Here&#39;s an example SQL query for computing an exponential moving average:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>select id,
       temp,
       avg(temp) over (partition by group_nr order by time_read) as rolling_avg
from (
  select id,
         temp,
         time_read,
         interval_group,
         id - row_number() over (partition by interval_group order by time_read) as group_nr
  from (
    select id,
    time_read,
    &quot;epoch&quot;::timestamp + &quot;900 seconds&quot;::interval * (extract(epoch from time_read)::int4 / 900) as interval_group,
    temp
    from readings
  ) t1
) t2
order by time_read;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Here&#39;s the same query in Flux - query language used in InfluxDB:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>from(db:&quot;telegraf&quot;)
  |&gt; range(start:-1h)
  |&gt; filter(fn: (r) =&gt; r._measurement == &quot;foo&quot;)
  |&gt; exponentialMovingAverage(size:-10s)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="storage-layer" tabindex="-1"><a class="header-anchor" href="#storage-layer" aria-hidden="true">#</a> Storage layer</h3><p>It is important to choose the time-series database carefully.</p><p>According to research published by Facebook, ~85% of queries to the operational store were for data from the past 26h.</p><p>If we choose a database, which harnesses this property, it could have significant impact on system performance. InfluxDB is one such option.</p><p>Regardless of the database we choose, there are some optimizations we might employ.</p><p>Data encoding and compression can significantly reduce the size of data. Those features are usually built into a good time-series database.</p><figure><img src="`+D+'" alt="double-delta-encoding" tabindex="0" loading="lazy"><figcaption>double-delta-encoding</figcaption></figure><p>In the above example, instead of storing full timestamps, we can store timestamp deltas.</p><p>Another technique we can employ is down-sampling - converting high-resolution data to low-resolution in order to reduce disk usage.</p><p>We can use that for old data and make the rules configurable by data scientists, eg:</p><ul><li>7d - no down-sampling</li><li>30d - down-sample to 1min</li><li>1y - down-sample to 1h</li></ul><p>For example, here&#39;s a 10-second resolution metrics table:</p><table><thead><tr><th>metric</th><th>timestamp</th><th>hostname</th><th>Metric_value</th></tr></thead><tbody><tr><td>cpu</td><td>2021-10-24T19:00:00Z</td><td>host-a</td><td>10</td></tr><tr><td>cpu</td><td>2021-10-24T19:00:10Z</td><td>host-a</td><td>16</td></tr><tr><td>cpu</td><td>2021-10-24T19:00:20Z</td><td>host-a</td><td>20</td></tr><tr><td>cpu</td><td>2021-10-24T19:00:30Z</td><td>host-a</td><td>30</td></tr><tr><td>cpu</td><td>2021-10-24T19:00:40Z</td><td>host-a</td><td>20</td></tr><tr><td>cpu</td><td>2021-10-24T19:00:50Z</td><td>host-a</td><td>30</td></tr></tbody></table><p>down-sampled to 30-second resolution:</p><table><thead><tr><th>metric</th><th>timestamp</th><th>hostname</th><th>Metric_value (avg)</th></tr></thead><tbody><tr><td>cpu</td><td>2021-10-24T19:00:00Z</td><td>host-a</td><td>19</td></tr><tr><td>cpu</td><td>2021-10-24T19:00:30Z</td><td>host-a</td><td>25</td></tr></tbody></table><p>Finally, we can also use cold storage to use old data, which is no longer used. The financial cost for cold storage is much lower.</p><h3 id="alerting-system" tabindex="-1"><a class="header-anchor" href="#alerting-system" aria-hidden="true">#</a> Alerting system</h3><figure><img src="'+I+`" alt="alerting-system" tabindex="0" loading="lazy"><figcaption>alerting-system</figcaption></figure><p>Configuration is loaded to cache servers. Rules are typically defined in YAML format. Here&#39;s an example:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>- name: instance_down
  rules:

  # Alert for any instance that is unreachable for &gt;5 minutes.
  - alert: instance_down
    expr: up == 0
    for: 5m
    labels:
      severity: page
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The alert manager fetches alert configurations from cache. Based on configuration rules, it also calls the query service at a predefined interval. If a rule is met, an alert event is created.</p><p>Other responsibilities of the alert manager are:</p><ul><li>Filtering, merging and deduplicating alerts. Eg if an alert of a single instance is triggered multiple times, only one alert event is generated.</li><li>Access control - it is important to restrict alert-management operations to certain individuals only</li><li>Retry - the manager ensures that the alert is propagated at least once.</li></ul><p>The alert store is a key-value database, like Cassandra, which keeps the state of all alerts. It ensures a notification is sent at least once. Once an alert is triggered, it is published to Kafka.</p><p>Finally, alert consumers pull alerts data from Kafka and send notifications over to different channels - Email, text message, PagerDuty, webhooks.</p><p>In the real-world, there are many off-the-shelf solutions for alerting systems. It is difficult to justify building your own system in-house.</p><h3 id="visualization-system" tabindex="-1"><a class="header-anchor" href="#visualization-system" aria-hidden="true">#</a> Visualization system</h3><p>The visualization system shows metrics and alerts over a time period. Here&#39;s an dashboard built with Grafana:</p><figure><img src="`+P+'" alt="grafana-dashboard" tabindex="0" loading="lazy"><figcaption>grafana-dashboard</figcaption></figure><p>A high-quality visualization system is very hard to build. It is hard to justify not using an off-the-shelf solution like Grafana.</p><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap up</h2><p>Here&#39;s our final design:</p><figure><img src="'+A+'" alt="final-design" tabindex="0" loading="lazy"><figcaption>final-design</figcaption></figure>',45);function W(F,H){const a=n("ExternalLinkIcon");return r(),o("div",null,[C,t("p",null,[e("The main downside of using Kafka for this problem is the maintenance/operation overhead. An alternative is to use a large-scale ingestion system like"),t("a",B,[e("Gorilla"),l(a)]),e(". It can be argued that using that would be as scalable as using Kafka for queuing.")]),M])}const U=s(S,[["render",W],["__file","40_metrics_monitoring_and_alerting_system.html.vue"]]);export{U as default};
