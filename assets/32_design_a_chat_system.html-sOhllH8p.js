import{_ as r,r as t,o as l,c,b as e,d as s,e as a,w as h,a as i}from"./app-53EDpe6z.js";const d="/assets/system-design-106-Mee12Ky0.png",p="/assets/system-design-107-K9n93j6v.png",g="/assets/system-design-108-ft08GXrJ.png",u="/assets/system-design-109-GQmYoSSW.png",f="/assets/system-design-110-cv-gI9nq.png",m="/assets/system-design-111-nxQSzWHZ.png",v="/assets/system-design-112-2VPOVu81.png",y="/assets/system-design-113-zh6UkXi2.png",b="/assets/system-design-114-D4YnxcWw.png",w="/assets/system-design-115-vzqNJoNY.png",_="/assets/system-design-116-nJgNveQ-.png",k="/assets/system-design-117-5Nj5WQHf.png",x="/assets/system-design-118-qo8G8DOs.png",I="/assets/system-design-119-81taNOVX.png",q="/assets/system-design-120-WBCkn07y.png",S="/assets/system-design-121-00RhbAve.png",T="/assets/system-design-122-BSQPESSI.png",W={},z=i('<h1 id="design-a-chat-system" tabindex="-1"><a class="header-anchor" href="#design-a-chat-system" aria-hidden="true">#</a> Design a Chat System</h1><p>We&#39;ll be designing a chat system similar to Messenger, WhatsApp, etc.</p><p>In this case, it is very important to nail down the exact requirements because chat systems can differ a lot - eg ones focused on group chats vs. one-on-one conversations.</p><h2 id="step-1-understand-the-problem-and-establish-design-scope" tabindex="-1"><a class="header-anchor" href="#step-1-understand-the-problem-and-establish-design-scope" aria-hidden="true">#</a> Step 1 - Understand the problem and establish design scope</h2><ul><li>C: What kind of chat app should we design? One-on-one convos or group chat?</li><li>I: It should support both cases.</li><li>C: Mobile app, web app, both?</li><li>I: Both</li><li>C: What&#39;s the app scale? Startup or massive application?</li><li>I: It should support 50mil DAU</li><li>C: For group chat, what is the member limit?</li><li>I: 100 people</li><li>C: What features are important? Eg attachments?</li><li>I: 1-on-1 and group chats. Online indicator. Text messages only.</li><li>C: Is there message size limit?</li><li>I: Text length is less than 100,000 chars long.</li><li>C: End-to-end encryption required?</li><li>I: Not required, but will discuss if time permits.</li><li>C: How long should chat history be stored?</li><li>I: Forever</li></ul><p>Summary of features we&#39;ll focus on:</p><ul><li>One-on-one chat with low delivery latency</li><li>Small group chats (100 ppl)</li><li>Online presence</li><li>Same account can be logged in via multiple devices.</li><li>Push notifications</li><li>Scale of 50mil DAU</li></ul><h2 id="step-2-propose-high-level-design-and-get-buy-in" tabindex="-1"><a class="header-anchor" href="#step-2-propose-high-level-design-and-get-buy-in" aria-hidden="true">#</a> Step 2 - Propose high-level design and get buy-in</h2><p>Let&#39;s understand how clients and servers communicate first.</p><ul><li>In this system, clients can be mobile devices or web browsers.</li><li>They don&#39;t connect to each other directly. They are connected to a server.</li></ul><p>Main functions the chat service should support:</p><ul><li>Receive messages from clients</li><li>Find the right recipients for a message and relay it</li><li>If recipient is not online, hold messages for them until they get back online.</li></ul><figure><img src="'+d+'" alt="store-relay-message" tabindex="0" loading="lazy"><figcaption>store-relay-message</figcaption></figure><p>When clients connect to the server, they can do it via one or more network protocols. One option is HTTP. That is okay for the sender-side, but not okay for receiver-side.</p><p>There are multiple options to handle a server-initiated message for the client - polling, long-polling, web sockets.</p><h3 id="polling" tabindex="-1"><a class="header-anchor" href="#polling" aria-hidden="true">#</a> Polling</h3><p>Polling requires the client to periodically ask the server for status updates:</p><figure><img src="'+p+'" alt="polling" tabindex="0" loading="lazy"><figcaption>polling</figcaption></figure><p>This is easy to implement but it can be costly as there are many requests, which often yield no results</p><h3 id="long-polling" tabindex="-1"><a class="header-anchor" href="#long-polling" aria-hidden="true">#</a> Long polling</h3><figure><img src="'+g+'" alt="long-polling" tabindex="0" loading="lazy"><figcaption>long-polling</figcaption></figure><p>With long polling, clients hold the connection open while waiting for an event to occur on the server-side. This still has some wasted requests if users don&#39;t chat much, but it is more efficient than polling.</p><p>Other caveats:</p><ul><li>Server has no good way to determine if client is disconnected.</li><li>senders and receivers might be connected to different servers.</li></ul><h3 id="websocket" tabindex="-1"><a class="header-anchor" href="#websocket" aria-hidden="true">#</a> WebSocket</h3><p>Most common approach when bidirectional communication is needed:</p><figure><img src="'+u+'" alt="web-sockets" tabindex="0" loading="lazy"><figcaption>web-sockets</figcaption></figure><p>The connection is initiated by the client and starts as HTTP, but can be upgraded after handshake. In this setup, both clients and servers can initiate messages.</p><p>One caveat with web sockets is that this is a persistent protocol, making the servers stateful. Efficient connection management is necessary when using it.</p><h3 id="high-level-design" tabindex="-1"><a class="header-anchor" href="#high-level-design" aria-hidden="true">#</a> High-level design</h3><p>Although we mentioned how web sockets can be useful for exchanging messages, most other standard features of our chat can use the normal request/response protocol over HTTP.</p><p>Given this remark, our service can be broken down into three parts - stateless API, stateful websocket API and third-party integration for notifications:</p><figure><img src="'+f+'" alt="high-level-design" tabindex="0" loading="lazy"><figcaption>high-level-design</figcaption></figure><h3 id="stateless-services" tabindex="-1"><a class="header-anchor" href="#stateless-services" aria-hidden="true">#</a> Stateless Services</h3><p>Traditional public-facing request/response services are used to manage login, signup, user profile, etc.</p><p>These services sit behind a load-balancer, which distributes requests across a set of service replicas.</p><p>The service discovery service, in particular, is interesting and will be discussed more in-depth in the deep dive.</p><h3 id="stateful-service" tabindex="-1"><a class="header-anchor" href="#stateful-service" aria-hidden="true">#</a> Stateful Service</h3><p>The only stateful service is our chat service. It is stateful as it maintain a persistent connection with clients which connect to it.</p><p>In this case, a client doesn&#39;t switch to other chat services as long as the existing one stays alive.</p><p>Service discovery coordinates closely with the chat services to avoid overload.</p><h3 id="third-party-integration" tabindex="-1"><a class="header-anchor" href="#third-party-integration" aria-hidden="true">#</a> Third-party Integration</h3><p>It is important for a chat application to support push notifications in order to get notified when someone sends you a message.</p>',43),A=i('<h3 id="scalability" tabindex="-1"><a class="header-anchor" href="#scalability" aria-hidden="true">#</a> Scalability</h3><p>On a small scale, we can fit everything in a single server.</p><p>With 1mil concurrent users, assuming each connection takes up 10k memory, a single server will need to use 10GB of memory to service them all.</p><p>Despite this, we shouldn&#39;t propose a single-server setup as it raises a red flag in the interviewer. One big drawback of a single server design is the single point of failure.</p><p>It is fine, however, to start from a single-server design and extend it later as long as you explicitly state that during the interview.</p><p>Here&#39;s our refined high-level design:</p><figure><img src="'+m+'" alt="refined-high-level-design" tabindex="0" loading="lazy"><figcaption>refined-high-level-design</figcaption></figure><ul><li>clients maintain a persistent web socket connection with a chat server for real-time messaging</li><li>The chat servers facilitate message sending/receiving</li><li>Presense servers manage online/offline status</li><li>API servers handle traditional request/response-based responsibilities - login, sign up, change profile, etc.</li><li>Notification servers manage push notifications</li><li>Key-value store is used for storing chat history. When offline user goes online, they will see their chat history and missed messages.</li></ul><h3 id="storage" tabindex="-1"><a class="header-anchor" href="#storage" aria-hidden="true">#</a> Storage</h3><p>One important decision for the storage/data layer is whether we should go with a SQL or NoSQL database.</p><p>To make the decision, we need to examine the read/write access patterns.</p><p>Traditional data such as user profile, settings, user friends list can be stored in a traditional relational database. Replication and sharding are common techniques to meet scalability needs for relational databases.</p><p>Chat history data, on the other hand, is very specific kind of data of chat systems due to its read/write pattern:</p>',13),C={href:"https://www.theverge.com/2016/4/12/11415198/facebook-messenger-whatsapp-number-messages-vs-sms-f8-2016",target:"_blank",rel:"noopener noreferrer"},O=e("li",null,"Only recent chats are accessed frequently. Users typically don't go too far back in chat history.",-1),P=e("li",null,"Although chat history is accessed infrequently, we should still be able to search within it as users can use a search bar for random access.",-1),D=e("li",null,"Read to write ratio is 1:1 on chat apps.",-1),B=i('<p>Selecting the correct storage system for this kind of data is crucial. Author recommends using a key-value store:</p><ul><li>they allow easy horizontal scaling</li><li>they provide low latency access to data</li><li>Relational databases don&#39;t handle long-tail (less-frequently accessed but large part of a distribution) of data well. When indexes grow large, random access is expensive.</li><li>Key-value stores are widely adopted for chat systems. Facebook and Discord both use key-value stores. Facebook uses HBase, Discord uses Cassandra.</li></ul><h3 id="data-models" tabindex="-1"><a class="header-anchor" href="#data-models" aria-hidden="true">#</a> Data models</h3><p>Let&#39;s take a look at the data model for our messages.</p><p>Message table for one-on-one chat:</p><figure><img src="'+v+'" alt="one-on-one-chat-table" tabindex="0" loading="lazy"><figcaption>one-on-one-chat-table</figcaption></figure><p>One caveat is that we&#39;ll use the primary key (message_id) instead of created_at to determine message sequence as messages can be sent at the same time.</p><p>Message table for a group chat:</p><figure><img src="'+y+'" alt="group-chat-table" tabindex="0" loading="lazy"><figcaption>group-chat-table</figcaption></figure><p>In the above table,<code>(channel_id, message_id)</code>is the primary key, while<code>channel_id</code>is also the sharding key.</p><p>One interesting discussion is how should the<code>message_id</code>be generated, as it is used for message ordering. It should have two important attributes:</p><ul><li>IDs must be unique</li><li>IDs must be sortable by time</li></ul><p>One option is to use the<code>auto_increment</code>feature of relational databases. But that&#39;s not supported in key-value stores. An alternative is to use Snowflake - Twitter&#39;s algorithm for generating 64-byte IDs which are globally unique and sortable by time.</p><p>Finally, we could also use a local sequence number generator, which is unique only within a group. We can afford this because we only need to guarantee message sequence within a chat, but not between different chats.</p><h2 id="step-3-design-deep-dive" tabindex="-1"><a class="header-anchor" href="#step-3-design-deep-dive" aria-hidden="true">#</a> Step 3 - Design deep-dive</h2><p>In a system design interview, typically you are asked to go deeper into some of the components.</p><p>In this case, we&#39;ll go deeper into the service discovery component, messaging flows and online/offline indicator.</p><h3 id="service-discovery" tabindex="-1"><a class="header-anchor" href="#service-discovery" aria-hidden="true">#</a> Service discovery</h3><p>The primary goal of service discovery is to choose the best server based on some criteria - eg geographic location, server capacity, etc.</p><p>Apache Zookeeper is a popular open-source solution for service discovery. It registers all available chat servers and picks the best one based on a predefined criteria.</p><figure><img src="'+b+'" alt="service-discovery" tabindex="0" loading="lazy"><figcaption>service-discovery</figcaption></figure><ul><li>User A tries to login to the app</li><li>Load balancer sends request to API servers.</li><li>After authentication, service discovery chooses the best chat server for user A. In this case, chat server 2 is chosen.</li><li>User A connects to chat server 2 via web sockets protocol.</li></ul><h3 id="message-flows" tabindex="-1"><a class="header-anchor" href="#message-flows" aria-hidden="true">#</a> Message flows</h3><p>The message flows are an interesting topic to deep dive into. We&#39;ll explore one on one chats, message synchronization and group chat.</p><h3 id="_1-on-1-chat-flow" tabindex="-1"><a class="header-anchor" href="#_1-on-1-chat-flow" aria-hidden="true">#</a> 1 on 1 chat flow</h3><figure><img src="'+w+'" alt="one-on-one-chat-flow" tabindex="0" loading="lazy"><figcaption>one-on-one-chat-flow</figcaption></figure><ul><li>User A sends a message to chat server 1</li><li>Chat server 1 obtains a message_id from Id generator</li><li>Chat server 1 sends the message to the &quot;message sync&quot; queue.</li><li>Message is stored in a key-value store.</li><li>If User B is online, message is forwarded to chat server 2, where User B is connected.</li><li>If offline, push notification is sent via the push notification servers.</li><li>Chat server 2 forwards the message to user B.</li></ul><h3 id="message-synchronization-across-devices" tabindex="-1"><a class="header-anchor" href="#message-synchronization-across-devices" aria-hidden="true">#</a> Message synchronization across devices</h3><figure><img src="'+_+'" alt="message-sync" tabindex="0" loading="lazy"><figcaption>message-sync</figcaption></figure><ul><li>When user A logs in via phone, a web socket is established for that device with chat server 1.</li><li>Each device maintains a variable called<code>cur_max_message_id</code>, keeping track of latest message received on given device.</li><li>Messages whose recipient ID is currently logged in (via any device) and whose message_id is greater than<code>cur_max_message_id</code>are considered new</li></ul><h3 id="small-group-chat-flow" tabindex="-1"><a class="header-anchor" href="#small-group-chat-flow" aria-hidden="true">#</a> Small group chat flow</h3><p>Group chats are a bit more complicated:</p><figure><img src="'+k+'" alt="group-chat-flow" tabindex="0" loading="lazy"><figcaption>group-chat-flow</figcaption></figure><p>Whenever User A sends a message, the message is copied across each message queue of participants in the group (User B and C).</p><p>Using one inbox per user is a good choice for small group chats as:</p><ul><li>it simplifies message sync since each user only need to consult their own queue.</li><li>storing a message copy in each participant&#39;s inbox is feasible for small group chats.</li></ul><p>This is not acceptable though, for larger group chats.</p><p>As for the recipient, in their queue, they can receive messages from different group chats:</p><figure><img src="'+x+'" alt="recipient-group-chat" tabindex="0" loading="lazy"><figcaption>recipient-group-chat</figcaption></figure><h3 id="online-presence" tabindex="-1"><a class="header-anchor" href="#online-presence" aria-hidden="true">#</a> Online presence</h3><p>Presence servers manage the online/offline indication in chat applications.</p><p>Whenever the user logs in, their status is changed to &quot;online&quot;:</p><figure><img src="'+I+'" alt="user-login-online" tabindex="0" loading="lazy"><figcaption>user-login-online</figcaption></figure><p>Once the user send a logout message to the presence servers (and subsequently disconnects), their status is changed to &quot;offline&quot;:</p><figure><img src="'+q+'" alt="user-logout-offline" tabindex="0" loading="lazy"><figcaption>user-logout-offline</figcaption></figure><p>One caveat is handling user disconnection. A naive approach to handle that is to mark a user as &quot;offline&quot; when they disconnect from the presence server. This makes for a poor user experience as a user could frequently disconnect and reconnect to presence servers due to poor internet.</p><p>To mitigate this, we&#39;ll introduce a heartbeat mechanism - clients periodically send a heartbeat to the presence servers to indicate online status. If a heartbeat is not received within a given time frame, user is marked offline:</p><figure><img src="'+S+'" alt="user-heartbeat" tabindex="0" loading="lazy"><figcaption>user-heartbeat</figcaption></figure><p>How does a user&#39;s friend find out about a user&#39;s presence status though?</p><p>We&#39;ll use a fanout mechanism, where each friend pair have a queue assigned and status changes are sent to the respective queues:</p><figure><img src="'+T+'" alt="presence-status-fanout" tabindex="0" loading="lazy"><figcaption>presence-status-fanout</figcaption></figure><p>This is effective for small group chats. WeChat uses a similar approach and its user group is capped to 500 users.</p><p>If we need to support larger groups, a possible mitigation is to fetch presence status only when a user enters a group or refreshes the members list.</p><h2 id="step-4-wrap-up" tabindex="-1"><a class="header-anchor" href="#step-4-wrap-up" aria-hidden="true">#</a> Step 4 - Wrap up</h2><p>We managed to build a chat system which supports both one-on-one and group chats.</p><ul><li>We used web sockets for real-time communication between clients and servers.</li></ul><p>System components:</p><ul><li>chat servers (real-time messages)</li><li>presence servers (online/offline status)</li><li>push notification servers</li><li>key-value stores for chat history</li><li>API servers for everything else</li></ul><p>Additional talking points:</p><ul><li>Extend chat app to support media - video, images, voice. Compression, cloud storage and thumbnails can be discussed.</li><li>End-to-end encryption - only sender and receiver can read messages.</li><li>Caching messages on client-side is effective to reduce server-client data transfer.</li><li>Improve load time - Slack built a geographically distributed network to cache user data, channels, etc for better load time.</li><li>Error handling</li><li>Chat server error - what happens if a chat server goes down. Zookeeper can facilitate a hand off to another chat server.</li><li>Message resend mechanism - retrying and queueing are common approaches for re-sending messages.</li></ul>',60);function U(M,N){const n=t("RouterLink"),o=t("ExternalLinkIcon");return l(),c("div",null,[z,e("p",null,[s("This component won't be discussed extensively as it's already covered in the"),a(n,{to:"/system-design/30_design_a_notification_system.html"},{default:h(()=>[s("Design a notification system chapter")]),_:1}),s(".")]),A,e("ul",null,[e("li",null,[s("Amount of data is enormous,"),e("a",C,[s("a study"),a(o)]),s("revealed that Facebook and WhatsApp process 60bil messages per day.")]),O,P,D]),B])}const H=r(W,[["render",U],["__file","32_design_a_chat_system.html.vue"]]);export{H as default};
